# 2026-09-08 合成目标与接收端验证

## 后续复测结果（本日最新）

用户重建 SSH 后，G1 连接恢复。更新前已备份远端遥操目录至 `/tmp/vla_preview_backup_20260908_QxlsBB/teleoperation_before_update.tar.gz`。今日修改已同步到 `/home/galbot/vla_g1_readonly_20260907`，本机/G1 共 24 个 Python/shell 文件 SHA256 完全一致。

G1 Python 3.8.10：39 项遥操测试、2 项只读适配测试和 401 帧虚拟时间合成测试通过。本日网络预览没有初始化 Galbot SDK，起点固定为合成 0.4 rad，限位也是测试值 [-1.5, 1.5] rad，不能用作真机当前状态。

真实跨机测试：`10.34.216.132 → 10.34.216.136:17071`，会话 `cd868e11-71f1-44bb-8ffd-4e2c4ddd546f`。401 条轨迹消息全部接受；随后故意静默 400 ms，再发送 1 条位置消息，该消息被拒绝，最后 disable 生效。统计：

- accepted_positions=401，rejected_messages=1（故意注入），watchdog_timeouts=1（故意断流）。
- 最大正常发送间隔 20.755128 ms，最大已接受消息接收间隔 209.689 ms。
- 最大预览输出变化率 0.030000000 rad/s。
- 输出最小 0.3825467074800567 rad，最大 0.41745329251994334 rad（相对起点 ±1°），最终 0.4 rad。
- 接收进程限时结束，退出码 0。没有真机运动调用。

本次 8 秒左右短测仅验证该次预览链路。209.689 ms 接收间隔说明仍有链路/调度抖动；300 ms 不能据此视为长期稳定或真机停止已验收。

GP001 环境只读检查：宿主 Python 3.11.13、remote_control_lite、encryption_runtime 文件齐全；宿主 USB 清单未见 GP001，/dev/ttyACM0 和 /dev/serial/by-id 不存在。当前助手进程无授权变量，不代表用户其他终端也未设置。运行中的 synthnova 容器具有授权变量及旧 /dev/ttyACM0 节点，但未验证授权有效性或设备读流，节点本身不能证明 GP001 仍在线。

下一步继续宿主直读路径，先恢复 GP001 USB 识别，再检查宿主现有正常授权环境。不需要使用 SynthNova 仿真或进入容器；没有打开串口、提取凭据或新增依赖。下方“网络不可达”是本日较早停点，现已解决。

VERIFIED（本机 Python 自动测试）：39 项遥操测试通过。合成起点为 0.4 rad，目标序列为 0 → +1° → 0 → -1° → 0，以 20 ms 虚拟时钟间隔执行 401 帧；这不是墙钟耗时、网络实时性或机械臂轨迹实测。

通过 JSON 编解码后进入 ReceiverGate，输出范围 [-1°, +1°]，最终回到合成起点，最大预览输出变化率 0.03 rad/s，116 帧被接收端限速。最后停止输入，在虚拟时间超过 300 ms 后失能。

新增行为：接收端按本地经过时间限制目标变化；首次位置必须与本地起点一致；accept 本身检查超时，迟到包不能刷新已过期的使能；相同接收时间的突发帧没有额外位移额度；回退时钟失能；已结束会话拒绝重放；畸形数组统一抛出协议错误；UDP 预览的启动授权仅消费一次，不随新会话自动重新授权。

复现（从开发工作区运行）：

```bash
python3 -B -m unittest discover -s teleoperation/tests -q
python3 -B -m teleoperation.synthetic_preview
```

UNRESOLVED：今日 SSH 到 10.34.216.136:22 返回 No route to host，未上传今日修改或执行远端测试；G1 上仍是昨日版本，不能继续宣称两端哈希一致。恢复连接后需同步并在 G1 上复测，再做跨机合成轨迹测试。

修正昨日结论：

- 200 ms 超时的根因未定位。ping 和平均 CPU 空闲率不能排除 UDP 延迟、进程瞬时停顿或日志阻塞，不能确定其属于误触发。
- 300 ms 下的一次 50/50 短测通过，只支持该次结果；该次最大间隔仅 23.255 ms，并未证明 300 ms 相比 200 ms 是成功原因。
- 当前 50 Hz 发送的是位置预览消息，不是已实现的独立心跳通道。
- 0.03 × 0.3 = 0.009 rad 是理想匀速计算，不是机械臂停止距离上界。实际停止还取决于 SDK 执行语义、调度、轨迹队列和制动响应。

尚未解决：源数据端到端年龄与积压消息识别、连续使能、认证、反馈闭环、进程退出时真机停止、SDK 实际速度/停止语义及 GP001 主动方向。接收端限速只约束预览输出，不能替代机器人端速度保障。今天仍只支持无运动预览。