# S1 右臂 joint7 单关节微动

日期：2026-08-20
安全等级：L4 真实运动（需要现场逐步确认）

## 唯一动作范围

- 设备：右机械臂；
- 关节：`right_arm_joint7`；
- 动作：运行时实测起点 `+0.02 rad`，然后在第二次人工确认后返回同一
  次运行保存的实测起点；
- 速度上限：`0.03 rad/s`；
- 不控制：左臂、夹爪、躯干、头部和底盘；
- 不循环测试任何其他关节。

## 保护

- 获取 `/home/galbot/ie_lab/control.lock`；
- 要求当前控制器为 `right_arm_pvt_ctrl`；
- 五次检查右臂速度；
- 读取当前标定 URDF 限位并保留 10° 余量；
- 动作前保存完整右臂状态；
- 前向和返回分别要求不同的人工口令；
- 前向失败、误差过大或任何异常时不自动返回；
- 每阶段更新 `/home/galbot/Lsy03/right_joint7_micro_*.json`。

## S1 目标路径

`/home/galbot/Lsy03/micro_move_right_joint7.py`

## 2026-08-20 20:42 实际结果

- 运行产物：
  `/home/galbot/Lsy03/right_joint7_micro_20260820_204215.json`；
- 起点：`0.11148924239273071 rad`；
- 目标：`0.13148924239273072 rad`；
- SDK 返回：`ControlStatus.FAULT`；
- 命令后实测：`0.11148924239273071 rad`；
- `VERIFIED`：joint7 没有发生可测得运动，误差仍为完整的 `0.02 rad`；
- 其他右臂关节前后最大变化约 `4.8e-5 rad`，属于采样波动；
- 脚本按设计没有自动返回，也没有继续控制其他设备。

对应 SDK 日志：

`/home/galbot/galbot_sdk_log/s1/galbot_sdk_python3.8_2026-08-20-20-42-11_30588_30588.log`

日志在命令时报告：

- 多个左右臂电机状态字不满足 `status_word & 0x6F == 0x27`；
- 多个 EtherCAT 从站 `al_state=4`，而日志期望 `8`；
- 左右臂均报告 `ErrorSafetyMedium`；
- 另有左夹爪和左相机错误项；
- SDK 随后将本次请求判定为 fatal error 并正常释放资源。

结论：本次失败不是 joint7 目标越限，也不是实际移动后恢复失败；命令发出时
机器人底层设备/总线并未处于可接受的运行状态。试教按钮、急停、整机工作
模式或其他原因与该状态的因果关系仍为 **待验证**。在确认整机为正常
`WORKING_MODE` 且底层故障消失前，不重试运动，不用控制器切换/重载来碰运气。