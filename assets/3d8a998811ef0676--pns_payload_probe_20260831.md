# S1 carried-4317 forward-probe PNS/fusion log inspection

Date: 2026-08-31
Method: user-run query-only CLI followed by authorized read-only SSH log inspection
Motion/state boundary: no controller switch, navigation submission, relocalization,
stop call, configuration edit, process signal, or physical motion was performed by
the inspection.

## Query

The user ran `probe-relative` for the exact `base_link` forward `1.0 m` target,
three checks, with the 4317 fusion filter already registered and the 4317 PNS
collision object previously attached successfully.

- Start map pose: approximately `(0.573625, 0.392794, yaw=-0.044674 rad)`.
- Goal map pose: approximately `(1.572625, 0.348135, yaw=-0.044674 rad)`.
- All three calls returned false and the CLI classified the target as
  `stably_unreachable`.
- The CLI sent no motion or state-changing call.

## Source logs

- PNS: `/userdata/log/service_navigation_plan/Info_20260831-082217.4599`
- PNS errors: `/userdata/log/service_navigation_plan/Error_20260831-082218.4599`
- Fusion/nvblox: `/userdata/log/galbot_fusion/Info_20260831-082218.240.txt`

## Verified PNS attachment

At `14:05:03.268`, PNS accepted `sdk_box_4317` under `base_link` with physical
size `0.4 x 0.3 x 0.175 m`, the recorded 7D pose, and the four configured ignored
end-effector links. PNS logged:

- `attach object sdk_box_4317 to link base_link, total sphere num: 315`
- final attachment task status `2` (success)

Each of the three later reachability checks logged
`has object 1: sdk_box_4317`. Therefore the attached object was present in PNS
during this probe. `left: None, right: None` refers to arm tool attachments and
does not mean that the generic SDK box was absent.

## Three collision results

All checks evaluated the same target base pose. The directly reported collision
object was the native robot link `omni_chassis_base_link`, sphere `11`, at
approximately `(1.34808, 0.346523, 0.6872)` in map coordinates. Sphere radius
was `0.2 m`, safe margin `0`.

| Check time | ESDF distance (m) | Result |
| --- | ---: | --- |
| 14:10:48.932 | 0.164164 | target state collision |
| 14:10:49.291 | 0.163081 | target state collision |
| 14:10:49.642 | 0.191371 | target state collision |

All three distances were below the sphere radius. Each final evaluator record
was `eval=0, start=1, goal=0, path_collision=0, has_solution=1`. The refusal was
therefore caused by target-pose validity, not a collision reported along the
intermediate path. The logs did not report `sdk_box_4317` itself as the colliding
object.

## ESDF source boundary

For the three checks, PNS used ESDF updates timestamped about 120 ms before each
evaluation and logged `cache esdf`. It also repeatedly logged
`global esdf map is nullptr`. This is strong evidence that the collision values
came from the current local `EsdfGrid` stream published by
`galbot_fusion_main`/NVIDIA nvblox rather than from the saved disk global ESDF.

The line `evaluate at dyn map: 0` must not be interpreted as “disk static ESDF.”
Its exact vendor-internal meaning remains unverified; it likely distinguishes an
additional dynamic layer or evaluation mode from the continuously updated local
nvblox ESDF snapshot.

## Geometric correlation with the current carried crate

Transforming the registered box center from `base_link` into map using the
query start pose gives approximately:

`(1.302581, 0.367936, 0.641112)`

The target sphere-11 center is only `0.068212 m` from this point. Expressed in
the box's local axes, the sphere-center offset is approximately:

`(-0.01817, -0.04590, 0.04708) m`

These magnitudes are all inside the physical box half-extents
`(0.20, 0.15, 0.0875) m`, not merely inside the larger fusion-filter box.

This does not mean PNS collided the robot model with its attached collision
object: the named collision remained `omni_chassis_base_link` versus ESDF. It
does show that the rejected future chassis sphere lands inside the current
world-space location of the carried crate.

## Fusion filter behavior observed

At `14:05:03.266`, fusion accepted `sdk_box_4317` with filter size
`0.60 x 0.50 x 0.375 m` under `base_link`, reported two active boxes including
the built-in robot box, and continued publishing TSDF/ESDF frames. The inspected
log records adding the filter but no retrospective TSDF/ESDF clear operation.

## Conclusion

The best-supported explanation is that crate observations fused before filter
registration remained in the live nvblox TSDF/ESDF. The new filter prevents or
reduces future crate-point integration but does not have verified retrospective
clearing semantics. When PNS hypothetically places the chassis 1 m forward,
native sphere 11 lands in that old world-space crate region and the goal is
rejected.

This is a high-confidence inference from synchronized geometry and logs, not a
vendor-confirmed implementation guarantee. Do not bypass reachability checks,
disable collision checking, remove the carried-object collision body, or clear
any map region without a separate reviewed procedure and fresh authorization.