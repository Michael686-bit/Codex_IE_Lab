# V5 线程 A/B 复现实验 — 2026-09-12

结果：同一右腕冻结 Observation 上，加速可重复。已获用户当前 SSH 授权，
在 S1 Orin 执行 A/B 各一次预运行和各五次正式运行，共 12 次。

## 固定输入与方法

方案见 [实验方案](../../docs/s1_v5_thread_ab_reproduction_20260912.md)。
镜像 `_03` 的 Image ID 为
`sha256:651bcf7087b9ba1ee2702a4b58ef488936c45251e2a661db8b41d547042bae89`。
输入为 `online_docker_20260912_right_03/container_input_bundle`，前后哈希一致。
远端原始输出：`/home/galbot/Lsy03/box_pose_pipeline/runs/thread_ab_20260912T083736Z`。

两组均使用 6 CPU 配额、8 GiB 内存、非 root、断网、只读根/输入，按不可变镜像 ID
启动；每次仅挂载该次独立输出目录。A 不新增线程变量，镜像默认也没有四个变量；
B 将 OPENBLAS_NUM_THREADS、OMP_NUM_THREADS、MKL_NUM_THREADS、NUMEXPR_NUM_THREADS
设为 1。实际库线程池数量未采样，不能把默认线程数写成已验证的某个整数。
正式顺序为 AB、BA、AB、BA、AB。每次是新容器，预运行仅排除首轮影响，
不是常驻进程预热测试。外部单调墙钟从 docker run 开始到退出，不含随后 inspect/清理。

## 实测

| 配对 | A 默认线程算法秒数 | B 单线程算法秒数 | 加速比 |
|---|---:|---:|---:|
| 1 | 32.3263 | 15.4127 | 2.0974 |
| 2 | 31.0431 | 15.5635 | 1.9946 |
| 3 | 33.1482 | 15.5085 | 2.1374 |
| 4 | 31.8608 | 15.4876 | 2.0572 |
| 5 | 29.9132 | 15.4393 | 1.9375 |

算法中位数 A/B 为 31.8608 / 15.4876 s，中位数之比 2.0572，耗时下降 51.39%。
容器外部墙钟中位数 A/B 为 32.7771 / 16.2590 s。
预运行 A/B 算法为 29.3168 / 15.3704 s，不计入正式统计。

12 次均退出 0、accepted、无 OOM。`pose_backend_result.json` 和
`backend_native_result.json` 均与原 `_03` 基线解析后的 JSON 完全相同，
中心和矩阵差为 0，包含竞争假设等诊断字段。实际容器 inspect 隔离核验通过。
镜像和冻结输入前后未变。测试容器全部清理，未留驻 SSH；原厂容器仍 healthy，
current 仍为 `releases/v5_2_v5_3b_live_20260910_01`。

## 复现入口与证据

- [run_remote.py](run_remote.py)：只使用 Python 标准库，核验镜像后运行受限容器。
  通过已授权的 SSH 执行 `python3 -u - < run_remote.py`（重定向在本机），
  生成唯一时间戳输出目录；密码仅由 sshpass 读取仓库外凭据文件。
- 每次精确 docker 命令参数在归档的 `command.json`；保留 stdout/stderr、
  inspect、算法完整结果、host_before/after 温度/CPU 频率/负载快照。
- 下载归档后运行：
  `python3 experiments/20260912_v5_thread_ab/analyze.py experiments/20260912_v5_thread_ab/thread_ab_20260912T083736Z`。
- [机器统计](thread_ab_20260912T083736Z/summary.json)，[原始轮次](thread_ab_20260912T083736Z/runs.json)。
- cgroup throttling 未逐次采样；不能分解调度、同步、限流各自占比。

结论只覆盖本冻结样本、镜像和设备当时负载；不代表其他帧性能或绝对精度。
未修改镜像、算法或原启动脚本。未采集相机、调用 SAM3/LingBot/FDP、SDK 或运动接口。
此结果支持后续以单线程配置作为该样本性能基线；距 1 s 仍需约 15.5 倍加速。