# S1 只读健康检查 — 2026-09-18 16:29–16:30 CST

用户授权检查当前 S1 CPU/GPU 和系统问题。通过独立短时 SSH 检查 galbot@10.34.216.17（galbot-echo，5.10.216-tegra，12 CPU）。未运行 SDK、运动、重启、配置修改或容器操作；SSH 已退出。以下仅为本次时间窗口观测，不作为长期健康保证。

## 结果

- 30 秒 mpstat：整体 busy 46.21%，2 秒窗口约 31.03–79.28%，iowait 平均 0.02%。
- 15 个 tegrastats 样本：GPU 平均 50.33%，4–99%；CPU 温度 67.656–69.593°C，GPU 61.906–67.656°C。采样后单次 CPU 温度约 70.4°C。复核各 throttle-alert cooling state=0，内核未查到 OOM/硬件 I/O/GPU fault/实际过热告警；启动时 thermal governor 注册行不是降频事件。CV -256°C 为无效读数，未纳入温度结论。
- RAM 28370–36651 MiB / 62782 MiB（约 27.7–35.8 GiB / 61.3 GiB），swap 始终 0；末次 free 可用 25 GiB。不同工具 used 口径不同，不把截图与 free 数字直接作内存增长比较。
- 初始 df：根/data/userdata 同一 NVMe 文件系统，使用 25%，可用 1.3T；inode 使用 1%。
- 头部雷达 PID 4792 平均 100.20% 单核口径；6 秒线程复核 TID 4997 平均 99.83%，运行在 CPU 11。截图 4792 和 4997 是同一进程及其线程，不能加总为两个独立满核进程。持续满核值得排查，但本次未验证是正常轮询还是异常忙循环，也未读取实际雷达帧率。
- 其他原厂进程 30 秒平均 CPU（单核100%）：fusion 48.63%，localization 40.37%，navigation 30.58%，motion plan 22.85%。
- 同期有计算容器 codex-fdp-opt-nunj4f-final，镜像 codex/fdp-orin-opt:20260918-v2；CPU quota 6 核、memory 8 GiB、OOMKilled=false。PID 81254 属该容器，在 pidstat 平均表中 CPU 159.56%。GPU 峰值与其并发，但未作逐进程 GPU 归因，不能证明全部来自此容器。
- pps_ts2spe.service 自 15:07:39 开机阶段失败：无法打开 /dev/pps0；复核该设备不存在。对 S1 当前同步链路的影响待验证，不能据此直接认定相机同步失效。
- 内核记录两台 RealSense D435i 在 16:25:24 USB disconnect，16:26:04 重新枚举；16:26:29 其中一路再次出现传输错误/重连，16:26:33 仍有 UVC 控制查询 -32/-71 报错。可能与服务/设备重置有关，根因待验证。16:27:00 至 16:30:42 无新增 warning 级内核日志；没有读取相机帧，不能保证出图已恢复。

## 证据与命令

- `read_only_check.sh`：经 SSH stdin 执行的只读采样脚本，没有写入远端。主要为 mpstat -P ALL 2 15、pidstat -u -r -p ALL 5 6、timeout 31s tegrastats --interval 2000，以及 systemctl/journalctl/ps/docker ps/sysfs 查询。
- `sample.log`：采样原始输出，三路并发 stdout 偶有交错；使用完整行及独立字段解析。
- `followup.log`：pidstat -t -u -p 4792 2 3、ps -L、限定 docker inspect 字段、16:27 后内核 warning、free、thermal cooling state。
- 首次 SSH 的 uptime/free/df/systemctl/journalctl 快照保留于任务工具记录；本报告记录主要数值。

建议先定位雷达 TID 4997 满核是否为设计行为；相机检查需结合当时人工重启记录及当前帧时间戳。PPS 服务需对照该型号配置判断是否应启用。本轮未进行修复。