# G1 当前无线网络检查

检查日期：2026-09-09，约 16:55–16:59（Asia/Shanghai）。无运动调用、未修改网络配置。

路径：PC 10.34.216.132 → wlp7s0 → G1 10.34.216.136；eno1 DOWN。SSID robot，信道 56（5 GHz），PC 信号快照 -68 dBm。无线网关 10.34.216.1。网卡显示的 540 Mbit/s 不是实测吞吐。

三分钟左右并行探测：两目标各 900 个 ICMP 包，间隔 200 ms；G1 TCP 22 端口每秒建立一次连接，共 180 次。未登录、未发送动作。统计 P95/P99 使用 nearest-rank，Ping 数值来自命令输出精度。

| 指标 | G1 ICMP RTT | 网关 ICMP RTT | G1 TCP 建连 |
|---|---:|---:|---:|
| 成功/总数 | 900/900 | 900/900 | 180/180 |
| 平均 ms | 80.784 | 12.671 | 78.110 |
| P50 ms | 33.800 | 7.910 | 28.861 |
| P95 ms | 207.000 | 35.600 | 207.140 |
| P99 ms | 222.000 | 121.000 | 264.238 |
| 最大 ms | 371.000 | 243.000 | 283.985 |

G1 RTT >100 ms：355/900（39.44%）；>200 ms：71/900（7.89%）；>300 ms：3/900（0.33%）。ICMP 丢包 0%，TCP 建连失败 0。

判断：链路连通，但长尾延迟显著。当前不建议将正式连续遥操数采建立在 PC 经该 Wi-Fi 实时下发目标的链路上。可继续只读、无运动预览；机器人本地执行和本地记录可降低对该链路的实时依赖，但本地执行若仍依赖无线遥操输入或远程推理，延迟问题仍然存在。断流停止和应用层观测/动作时效尚未验收。

定位推断：PC→网关也有抖动，G1 路径的延迟更明显；应进一步检查机器人无线连接、省电、AP 转发和系统调度。无法凭本轮数据确定具体根因。

限制：未实测相机流/吞吐、SDK 反馈年龄、动作应用延迟、50 Hz 应用消息到达间隔或真机停止。已有 SSH 密钥认证失败，没有可复用 G1 SSH 会话；TCP 端口可达不代表登录成功。RTT 不等于单向延迟，也不等于应用消息接收空窗；最大 RTT >300 ms 不能直接推导接收器必然触发 300 ms 看门狗。

原始结果：g1.txt、gateway.txt、tcp.json；统计：summary.json；探测代码：probe.py。