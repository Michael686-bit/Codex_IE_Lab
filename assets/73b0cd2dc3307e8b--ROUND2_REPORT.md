# 第二轮结果：4317 多平面侧壁分解与几何中心估计

历史 D01 单帧记录。2026-09-08 已修复坐标投影/细化后门禁等实现问题并完成
D02–D12 固定参数批量评估，见 [最新报告](ROUND2_BATCH_REPORT_20260908.md)。
本文件中的 v2 候选不能用来断言“XY/yaw 已解决，只剩 2.50 mm 高度问题”。

日期：2026-09-07。主输出：`outputs/round2_02/`；`round2_01` 保留为接入后
首次运行记录，未覆盖。

## 结论

第二轮在第一轮旁新增独立分支，没有替换或放宽原门禁。D01 的已确认 LingBot
案例中共选择 11,024 个近竖直壁面像素，4,098 个体素点被分解成 7 个平面。
固定 `0.40 × 0.30 × 0.175 m` 尺寸搜索找到三个面支持的唯一候选解释，其中
宽度方向的相对两面均被观测，长度方向观测到一面；有限面范围检查通过。

正式结果仍为拒绝：

```text
ok=false
rejection_reasons=["multiplane_wall_heights_inconsistent_with_model"]
```

三个匹配侧壁给出的箱体中心 Z 可行区间分别约为：

- `[0.501347, 0.530090] m`
- `[0.480056, 0.508120] m`
- `[0.479978, 0.498848] m`

联合下界约 `0.501347 m`，联合上界约 `0.498848 m`，相差约 2.50 mm，区间
为空。当前不能判断原因是外壁/内壁/筋条归属、LingBot 深度偏差、局部倾斜，
还是 175 mm 先验与实际可见表面不一致，因此不发布正式位姿。

## 诊断候选（不是有效位姿）

- 箱体中心：`[1.398724, 0.236702, 0.494088] m`
- yaw：约 `89.359°`
- 观测面：`length:+1`、`width:-1`、`width:+1`
- 对照 FDP：仅用于差异观察，不是真值。

本轮说明无 FDP 的多侧壁联合约束能够恢复明确的 XY/yaw 候选，并消除第一轮
“只按整体壁点拟合矩形”的部分混合问题；它尚未证明中心 Z 或抓取精度可靠。

## 实现与验证

- 新增 `bin_rim_pose/multiplane_center.py`。
- 采用确定性二维线 RANSAC、竖直/切向跨度门禁、正交方向搜索、固定尺寸四面
  匹配、有限面包含率检查及高度区间交集。
- 两个相邻面不足以唯一确定中心，必须至少三个面且包含一组相对面。
- 25 项测试全部通过；新增测试覆盖三面恢复、两面欠约束拒绝、中心高度交集，
  以及 D01 高度冲突时保留候选但拒绝发布。
- D01 完整离线运行正常结束，三分支均拒绝时退出码为 2。
- 多平面分支在本机 x86、单 OpenBLAS 线程单次约 0.693 s；不是 Orin 性能。
- 输入和参照哈希运行前后不变；没有网络推理、机器人访问或运动命令。

## D01～D12 数据状态

D01～D12 原始采集已经只读复制到本机，并通过尺寸、清单和 SHA-256 审计；
全部记录 `motion_commands_sent=false`。D02～D12 仍只有原始 RGB-D、K、TF 和
采集元数据，没有逐帧确认 mask，不能自动复用 D01 的 SAM index。

下一步应先为 D02～D12 补齐场景标签和逐帧目标确认，再运行同一几何分支。
高度冲突应通过跨视角与 raw/LingBot 单变量对照定位，不应直接增加 2.50 mm
容差来制造通过结果。

## 同 mask 的 raw/LingBot 深度诊断

保持 D01 的人工确认 mask、K、捕获 TF 和全部算法参数不变，仅替换深度：

- LingBot 深度分解出 7 个平面，形成上述唯一三面候选，但因高度不一致拒绝；
- raw 深度分解出 10 个平面，却没有形成满足尺寸、有限面包含率、三面和相对面
  门禁的候选，拒绝为 `no_dimension_consistent_three_face_hypothesis`。

因此本帧尚不能直接删掉 LingBot：原始深度至少在当前参数下没有提供足够一致的
三面几何。该结论只适用于 D01，不证明 LingBot 深度更接近真实几何；后续要在
D02～D12 的逐帧确认 mask 上重复同一消融。