# 30秒相机时序检查：录制完整，时序质量未通过

会话：`lerobot_runs/camera_timing_20260922_162948_488647`，ID `9eedaa46-af37-494a-9ac8-0fccfb166630`。以下是本次实际原件的结果，不沿用14:18短录结论。

**完整性通过**

- PC逻辑记录30.036秒，G1原始记录30.691秒，包含正常结束后0.6秒尾部收集。
- 17流齐全，26011条记录，16个完成分片。accepted=written=26011，pending_bytes=0，cleanup_error=null。
- 原件经有线取回，分片与负载哈希、会话身份、序号、源时间单调性全部通过。
- 1883帧RGB、922帧uint16深度全部解码/尺寸检查通过；媒体负载约2.016GB。
- PC本次只记录6条协调事件，dropped=0、pending=0、error=null；没有PC机器人SDK、遥操或实际下发动作。

**头部相机实际低帧率是首要问题**

| 数据流 | 保存数量 | 源时间观测Hz | 数据年龄P95 | 相对左腕RGB近邻配对差P95 |
|---|---:|---:|---:|---:|
| 头左RGB | 178 | 5.775 | 125.9ms | 83.4ms |
| 头右RGB | 178 | 5.775 | 126.9ms | 83.4ms |
| 左腕RGB | 461 | 15.000左右 | 34.9ms | 0（锚点） |
| 右腕RGB | 461 | 15.000左右 | 35.6ms | 29.5ms |
| 左腕深度 | 461 | 15.000左右 | 37.0ms | 0 |
| 右腕深度 | 461 | 15.000左右 | 37.8ms | 29.5ms |
| 左前环视 | 302 | 9.82 | 398.3ms | 45.2ms |
| 右前环视 | 303 | 9.85 | 397.5ms | 45.0ms |
| 左右臂关节 | 各1853 | 60.45 | 18.1ms | 8.0ms |
| 左右腕力 | 3499/3492 | 114.20/113.97 | 14.2/14.3ms | 6.0ms |

年龄=G1读取wall时间−源时间戳，是表观数据年龄，包含缓存、采样调度和上游时钟误差；不等于已经校准的纯传输延迟。配对允许离线取后到帧，只比较软件源时间戳。

头部177个相邻帧间隔全部超过100ms，范围152.018–188.010ms，中位数175.993ms；接收间隔中位数176.309ms。因此本段表现为持续低更新率，而不是少数偶发缺口。全部178帧负载均不同，未把同一JPEG重复写入冒充新帧。

相比此前头部约15Hz、配对P95约6–8ms，这次已经退化到约5.8Hz、配对P95约83ms。双腕仍约15Hz，源间隔最大约66.77ms。

只读查看的磁盘配置 `/userdata/user_config/base/front_head_camera_capture`：`frame_rate=15`、`auto_exposure=1`、`jpeg_qua=95`、1280×960。配置目标不等于实际输出率；本次尚未读取运行时曝光值或发布帧计数。自动曝光、驱动、编码/发布链路与SDK缓存均是排查方向，不能仅凭配置确认根因。未修改配置或重启服务。

现有驱动statistics日志只有控制服务订阅统计，未提供图像发布率，因此不作为“驱动端已证实5.8Hz”的证据。主日志中的DDS类型提示此前也出现过，不能据此单独归因。

**画面有变化，但三视角共同事件不足**

关键帧显示有人走动、抬手/脚。头部、左腕和环视可见相关变化，但拍摄角度/目标部位不同；右腕主要看到固定地面、墙边及夹爪，未辨识到足够清晰的共同标记。

各相机变化曲线可用于定位待人工复核时段，但不同视角的运动强度峰值不是同一物理事件，不应用峰值差或无约束互相关硬算延迟。本次没有产出“头部与双腕物理同步通过”的结论。

头部左右目、同侧腕RGB/深度、左右环视的已配对源时间戳相同；这仍然只是软件配对一致，不是曝光同步证明。头部约176ms的采样间隔也不足以单靠这段视频验证数毫秒级同步。

代表帧与曲线：

- `results/contact_sheet.png`：每相机在0/7.5/15/22.5/30秒附近的图像。
- `results/largest_changes.png`：各自最大画面变化，图中已注明可能不是同一事件。
- `results/visual_signals.png`、`results/visual_signals.json`：亮度、帧差和源时间间隔。

**时钟和连续性仍需处理**

- 共40个跨机采样，其中1个初始RTT约263ms，不能用于精确映射；RTT≤5ms的39个样本偏移约97.4–113.3ms，中位附近约110ms。
- 分段映射标记了记录开始后约1.01–3.03秒、4.03–5.04秒的速率/偏移变化区间，未跨异常区间强行插值。
- 腕力、关节和夹爪部分源时间戳领先本地读取时间，最小表观年龄约−10ms，说明上游源时间基准仍不能简单视为G1主机时钟。
- 环视约0.4秒的表观年龄仍存在；尾部额外采集改善了末尾覆盖，但不会消除实时观测年龄。
- 关节最大源间隔77.8ms，夹爪78.4ms，环视200ms；这些区间仍须标记或拒收。

**当前组帧规则未通过**

使用现有可复用对齐工具，目标为10Hz、头左＋双腕＋双臂状态＋双夹爪，301个候选时刻中严格通过0个。

主要拒收项：头部帧间隔超过100ms（269行）；时钟区间速率/偏移变化（30行）；两端覆盖及个别状态时间差/缺口另有拒收。理由可在同一行重叠，不能直接相加。

仅看近邻时间差、不检查帧间隔/时钟不确定度的初步试算能匹配106/301行，但不能作为最终合格数。严格规则拒收不等于原始文件损坏；它表示本段不满足当前15Hz头部预期及三视角10Hz采样要求。本次没有降低规则或靠重复帧制造10Hz新观测，也没有导出训练数据集。

对齐证据：`lerobot_runs/alignment_camera_timing_20260922_162948_v1/plan.json`。

**下一步建议**

1. 优先查清头部从15Hz变为约5.8Hz发生在哪一级。先对比头部单独订阅与全量记录下的实际时间戳间隔，再只读核对运行时曝光、编码/发布负载及SDK缓存；配置调整需依据结果另行决定。
2. 同步排查约0.4秒环视年龄和源时钟领先问题。
3. 头部帧率稳定后，让固定标记的同一清晰事件进入待检查相机共同视野；若三路无交集，分别做头部—左腕、头部—右腕的成对实验，并保留相同参考相机。
4. 物理时序、并发采集对反馈的影响处理后，再安排实际动作日志联合试录及VLA训练样本验收。

原始文件可用于诊断与重做离线分析，当前没有action，仍不具备直接训练VLA的条件。