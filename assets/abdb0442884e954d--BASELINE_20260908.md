# PC 有线直控基线（2026-09-08）

本记录覆盖步骤 1–3：固定运行环境、GP001 实时采集质量和 G1 连续只读状态。此阶段没有调用 G1 运动、轨迹、夹爪或底盘接口。

## 步骤 1：运行环境

- PC 有线接口：`eno1`，`192.168.1.99/24`
- XCU：`192.168.1.66`，有线 Ping 0% 丢包
- HPU/Orin：`192.168.1.88`，有线 Ping 0% 丢包
- Docker：`29.1.3`，host 网络
- GP001：`/dev/ttyACM0`，`root:dialout`，权限 `0660`
- 容器 Python：`3.11.15`
- LeRobot：`0.4.4`
- PC Galbot SDK：`1.9.1`
- G1 固件：`GBS_1.17.0.0.rc7`
- 官方兼容性检查：`MATCH`
- SDK 运行库挂载：项目 `.vendor/galbot-pc-sdk` → 容器 `/opt/galbot`
- GP001 官方模块通过 `SYNTHNOVA_MODULE_ROOT` 只在授权会话中挂载；凭据不写入项目。

容器入口：

```bash
SYNTHNOVA_MODULE_ROOT=/path/to/synthnova_delivery/module \
  bash docker/run.sh bash
```

## 步骤 2：GP001 采集

采集文件：[gp001_step2_20260908.jsonl](</home/lsy03/文档/ChatGPT/VLA G1 项目/lerobot_runs/gp001_step2_20260908.jsonl>)

质量报告由 `teleoperation.gp001_quality_report` 生成：

- 有效帧：935
- 无效帧：0
- session：1
- 序号回退：0
- 帧间隔 P50：10.25 ms
- 帧间隔 P95：10.47 ms
- 最大帧间隔：20.65 ms
- 质量结论：`quality_ok=true`

这批数据确认了 GP001 双臂 7 维状态可以被容器稳定读取。驱动偶尔打印原始错误帧十六进制，但本批没有形成无效采集帧。

## 步骤 3：G1 连续只读状态

最终报告：[g1_step3_20260908_v3.json](</home/lsy03/文档/ChatGPT/VLA G1 项目/lerobot_runs/g1_step3_20260908_v3.json>)

- 读取帧：975
- 主机读取频率：97.5 Hz
- 主机间隔 P50：10.28 ms
- 主机间隔 P95：10.39 ms
- 最大主机间隔：12.02 ms
- 超过 20 ms 的停顿：0
- SDK 源时间戳回退：0
- 重复源时间戳：375
- 反馈年龄 P95：11.90 ms
- 最大反馈年龄：55.19 ms
- `motion_api_called=false`

重复源时间戳表示 SDK 在连续调用中重复返回同一反馈帧；本次没有出现时间戳倒退。后续联合映射应使用主机接收时间判断数据新鲜度，同时保留 SDK 源时间戳用于诊断。

## 当前阶段结论

PC 有线网络、Docker、PC SDK、GP001 实时输入和 G1 连续状态读取已经形成可重复基线。下一阶段才进入 GP001 与 G1 的联合映射预览；真机运动和正式 VLA episode 采集仍未开始。