# G1 无线应用层补测

2026-09-09 17:06 起。通过 galbot-136-ssh 技能保存的凭据登录成功：galbot@10.34.216.136，hostname galbot-echo。未输出凭据。未调用 SDK、初始化控制器、运动或修改网络配置。仅运行限时临时 UDP 回显诊断进程；进程在收到结束消息后输出统计并退出，SSH 进程退出码 0。远端未创建文件。

## 机器人端只读检查

- 回程：10.34.216.136/wlan0 → 10.34.216.132。
- G1：SSID robot，信道64，5320 MHz，信号 -62 dBm，Wi-Fi power save on。
- PC：SSID robot，信道56。两端连接不同无线电信道，可能涉及 AP/无线电间转发；未检查 AP 拓扑，不能断言具体转发路径。
- G1 nproc=11，短时 vmstat CPU idle 74–77%；load average 约8.6。不能据此排除单线程/瞬时调度停顿。

## 方法

50 Hz 目标发送，实际约48.7–48.9 Hz，固定小包 PC→G1→PC UDP 回显，包含随机令牌和序号。服务器只回显本机地址且令牌匹配的包。机器人收到包时用本地单调时钟记录接收间隔；RTT 用 PC 单调时钟记录，不要求跨机时钟同步。三段测试均无其他人为网络配置变化。

模拟图像负载为 G1→PC 每10ms尝试发送10个1200字节UDP包，目标9.6Mbps；实收8.982Mbps。它不含真实相机采集、编码、图像时效或磁盘写入成本，不能视为相机数采验收或最大吞吐测试。

| 阶段 | 时长 | 回显成功/发送 | RTT平均 | RTT P95 | RTT P99 | RTT最大 | G1最大收包间隔 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 无额外负载 | 45s | 2193/2193 | 18.019ms | 61.085ms | 153.054ms | 242.521ms | 246.930ms |
| 模拟图像回传 | 30s | 1463/1463 | 13.386ms | 28.144ms | 92.159ms | 176.378ms | 150.068ms |
| 停止模拟回传后 | 15s | 733/733 | 25.667ms | 128.888ms | 250.366ms | 332.224ms | 342.985ms |

共4389次回显，均在各段接收窗口内收到；不等于均按时收到。G1接收间隔超过100/200/300ms的次数：首段14/2/0、模拟回传段3/0/0、末段7/3/1。统计排除每段第一包，避免把测试段间停发算作正常运行间隔。

## 判断

持续50Hz小包时平均延迟明显好于先前5Hz ICMP和1Hz TCP建连测试；但测试时间/协议不同，不能单独归因为发包频率。模拟回传时表现较好，与省电/链路活跃程度相关是待验证假设，不能认定增加负载必然改善。

目前具备开展受控低速预览和进一步验证的网络基础，但尚不建议正式连续遥操作数采。实测343ms机器人端收包空窗超过历史300ms候选看门狗时限，若该类空窗出现在使用该阈值的实际接收器上，应触发断流保护；本次未运行原接收器或SDK，未验证实际停止。不要以放宽超时作为解决方式。

后续优先单因素A/B：临时关闭G1 Wi-Fi省电后原样复测；再验证两端固定到合适AP/无线电的表现。此轮未实施这些配置变更。通过后仍需真实相机流、时间戳对齐、SDK反馈/停止及较长持续测试。

本结论补充先前 network_checks/20260909_165545/评估.md：SSH登录已恢复，应用层已补测，但真实相机和SDK仍未验证。