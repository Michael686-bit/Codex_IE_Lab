# 对齐与LeRobot观测预览阶段结果

本阶段已完成可复用的离线主机时钟映射、带质量检查的组帧、真实观测的LeRobot导出和重载。没有运行机器人动作；没有生成或填充动作标签。物理曝光同步和正式训练适用性仍未验证。

输入会话：`lerobot_runs/dual_arm_capture_20260922_141805_472377`。

正式保留的本阶段输出：

- 对齐方案：`lerobot_runs/alignment_20260922_141805_v2/plan.json`。
- LeRobot预览：`lerobot_runs/lerobot_observation_preview_20260922_v2/`。
- 逐帧原始索引：数据集中的 `source_rows.jsonl`。
- 重载结果与源片段关系：数据集中的 `capture_provenance.json`。

原始两段共39个10Hz候选时刻。加入时钟不确定度、状态最大间隔40ms和边界覆盖要求后，分别通过21/25、7/14，共28行。第2段比此前纯时间差试算通过数更少，是因为加入了上述额外约束，没有改写源数据或放宽阈值。

排除两个孤立单帧区间后，导出26帧，分为4个连续派生片段（7、14、2、3帧）。它们来自两段原始录制，不是四次真机任务；片段内时间重置为0，原始PC单调时间、G1时间、网格序号、时间差和源文件位置全部保留。

验证结果：

- 三路原尺寸图像：头左960×1280，左右腕720×1280。
- 16维状态：左右臂各7关节，左右夹爪开度；按关节名称恢复顺序。
- 全部26帧在LeRobot重新加载，状态数值、图像尺寸和有限性检查通过。
- DataLoader批次状态形状为 `[2,16]`。
- 数据集没有 `action` 字段，`training_ready=false`，明确标为 observation preview。
- 新增/既有会话与对齐测试15项通过；受影响双臂、一键控制与采集接口测试17项通过。

另外补齐了采集启动等待提示、周期数采状态、预热过程时钟采样；正常结束后后台额外采集0.6秒以等待延迟数据，但逻辑episode时间不延长，导出按源时间裁剪。0.6秒是否覆盖所有长尾仍需现场验证。

下一步使用 `bash teleoperation/run_camera_timing_check.sh --seconds 30`，由操作者制造共同可见的变化事件。这条新入口只录G1传感器，不使用GP001、不初始化PC机器人SDK、不发送运动指令。

前次启动G1采集时导致PC反馈停顿的问题尚未解决；本阶段没有把离线映射安装到控制器，也没有放宽300ms保护阈值。完成视觉时序和并发采集问题验证后，再安排带动作的低速联合录制。

操作说明：`acquisition/lerobot_capture/ALIGNMENT_USAGE.md`。