# G1 左臂只读状态验证

日期：2026-09-07

## 部署与边界

- G1：`galbot-echo`，历史地址 `10.34.216.136`。
- 独立目录：`/home/galbot/vla_g1_readonly_20260907`。
- 上传归档 SHA256：`0c0aa2cedffc23d4cdd2293247899280149e77d509bcee6c4bac0f2502a68224`。
- 初次运行发现启动脚本缺少 `GALBOT_CONFIG_DIR` 后，仅修正并重新上传该脚本；随后本机与 G1 上全部 `.py/.sh` 逐文件 SHA256 再次核对，全部一致。
- G1 端离线测试：遥操映射/协议 28/28，通过；只读适配 2/2，通过。
- 运行程序不提供运动接口；输出声明 `motion_api_called=false`。

## 真机只读结果

SDK 1.9.0，Python 3.8.10，活动控制器 `left_arm_pvt_ctrl`。左臂 7 个反馈速度均为 `0 rad/s`，状态时间戳持续更新。活动 URDF 与标定 URDF 的限位已取保守交集。

| 关节 | 位置 rad | 下限 rad | 上限 rad | 结论 |
|---|---:|---:|---:|---|
| left_arm_joint1 | 3.004326190 | -3.004326190 | 3.004326190 | 位于上限 |
| left_arm_joint2 | 1.278165698 | -1.608062789 | 1.608062789 | 未开放 |
| left_arm_joint3 | 2.826356411 | -2.916972222 | 2.916972222 | 未开放 |
| left_arm_joint4 | -2.318058968 | -2.567993878 | 1.869862177 | 未开放 |
| left_arm_joint5 | -2.916972222 | -2.916972222 | 2.916972222 | 位于下限 |
| left_arm_joint6 | 0.735398163 | -0.822664626 | 0.735398163 | 位于上限 |
| left_arm_joint7 | 0.717038810 | -1.538202778 | 1.538202778 | 第一阶段候选 |

第二次策略预览读取到 joint7 `0.716991723 rad`（`41.080600°`），速度 `0`。两次读数相差约 `0.0000471 rad`（`0.00270°`）。接收策略保持相对起点 ±1°、最大速度 0.03 rad/s；后续跨机 dry-run 发现 200 ms TTL 会误触发，当前候选调整为 300 ms，尚未用于运动。

## 结论

真实 G1 起点、限位、状态新鲜度和活动控制器已接入只读策略预览。当前姿态的 joint1、joint5、joint6 位于硬限位，禁止从第 7 关节直接扩展到完整左臂。真机运动仍未执行。

跨机历史 JSONL dry-run 已完成；最终候选 50 Hz 心跳、300 ms TTL，50/50 消息接受且无误触发。用户选择跳过需要厂商授权的新 GP001 采集，因此主动方向仍未实测。下一步可用合成第 7 关节正负轨迹验证限速、越界拒绝和断流，再决定是否进入真实 GP001 或真机运动阶段。