# GP001 与 G1 控制证据归档

记录建立：2026-09-07。用途：承接本次对话中的测试结果；本次文档初始化没有重新执行测试。数值来自会话工具输出或用户粘贴日志，远端原始文件尚未复制/计算哈希。

## G1 单关节（用户现场执行）

2026-09-07，Galbot G1，历史 SSH 目标 `galbot@10.34.216.136`，SDK 1.9.0，Python 3.8.10。自有冒烟测试脚本调用官方 SDK；不是厂商原样示例。

用户在本机通过交互 SSH 启动，下述命令实际运行于 G1 的 `/home/galbot/g1_arm_smoke_test_20260904`。这些是历史命令记录，不是自动复测授权。

```bash
./run_g1_arm_smoke_test.sh --execute --arm left --joint-index 7 --delta-deg 1.0 --speed-rad-s 0.03 --hold-s 2
./run_g1_arm_smoke_test.sh --execute --arm right --joint-index 7 --delta-deg 1.0 --speed-rad-s 0.03 --hold-s 2
```

| 项目 | 左臂 | 右臂 |
|---|---:|---:|
| 运动日志时间 | 15:15:51 起 | 15:19:41 起 |
| 起始角度（最终目标日志） | 1.858° | 23.571° |
| 指令位移 | +1° | +1° |
| 微动反馈位移 | +0.9668° | +1.0163° |
| 微动目标误差 | 0.0332° | 0.0163° |
| 回程目标误差 | 0.0069° | 0.0233° |
| 回程时其他关节最大漂移 | 0.0028° | 0.0028° |

两侧保持、微动和回程均返回 `ControlStatus.SUCCESS`，SDK 清理完成。

远端日志路径（在 G1 上，不是本机链接）：

- `/home/galbot/g1_arm_smoke_test_20260904/logs/g1_arm_smoke_test_20260907_151551.log`
- `/home/galbot/g1_arm_smoke_test_20260904/logs/g1_arm_smoke_test_20260907_151941.log`
- dry-run：`/home/galbot/g1_arm_smoke_test_20260904/logs/g1_arm_smoke_test_20260907_151319.log`

本机源码来源：[冒烟测试说明](</home/lsy03/文档/ChatGPT/VLA G1 项目/g1_arm_smoke_test/README.md>)。尚未复制进正式仓库。

当时左臂 joint5 距限位约 1.56°、joint6 在上限；右臂 joint5 接近上限、joint6 在下限。第 7 关节测试通过不证明其余关节或整臂路径安全；当时姿态不能直接作为新实验实时状态。

## 夹爪

- 左夹爪：官方 `get_gripper_state.py` 回读 `width=0.05 m`、`is_moving=False`。用户说“左夹爪可以了”，但会话未提供左夹爪运动命令与结果，故动作验收待补。
- 右夹爪：将官方 `/opt/galbot/examples/g1/python/galbot_robot/set_gripper_command.py` 的组名从 `left_gripper` 替换为 `right_gripper` 后执行。两次目标分别为 0.02 m 和 0.10 m，速度参数 0.05 m/s，effort 参数 10，阻塞调用，两次均打印成功。
- 随后同样替换 getter 的查询组，回读 `width=0.1 m`、`is_moving=False`、`velocity≈-5.2471e-5`。输出中的 “Left gripper state” 是未替换的提示字符串，实际查询组为右侧。
- 两侧回读 `effort=100.0`。运行时 pyi 对 effort 标记为 N·m，官方示例注释使用 N，记录存在不一致；先前对话“力矩 10 N”不严谨。参数与反馈的物理含义及标定尚未核验，不据此报告真实夹持力。

右夹爪证据支持两次命令完成和最终宽度回读；未记录中途连续轨迹、力控、接触、故障停止或重复性验收。

## GP001 历史结果

9 月 4 日宿主机短测 100 帧全部有效、无坏帧，左右各 7 个有限值；设备时间戳和主机单调时间戳递增。设备时间跨度约 1.0395；若其单位为秒，对应约 95.2 Hz，单位与同步语义仍需单独核验。100 帧短测不等于持续稳定性测试。

源码来源：[GP001 读取说明](/home/lsy03/Lsy03_document/遥操作主臂解析协议/README.md)。历史日志位置为 `/home/lsy03/Lsy03_document/遥操作主臂解析协议/captures/gp001_host_test_20260904T091104.875408Z.jsonl`，本次没有重新检查日志内容。

SynthNova 历史记录显示虚拟双臂跟随、5 秒消费 494 帧、约 93–96 Hz、未报坏帧或越界。只能证明对应虚拟链路通过。

## 下一步和未验收项

先迁移并验证既有源码，再做 GP001 左臂第 7 关节映射预览和接收端保护测试。完整单臂运动、夹爪映射、持续使能、网络中断处理、相机/状态/动作同步和训练数据闭环均未验收。初始化期间没有改变机器人状态。