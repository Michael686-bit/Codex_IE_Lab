# 2026-09-15 标定板对比准备

范围：16 帧排除 L02_10 与 R03_10lift，余 14 帧全部保留。后端 V5-2+V5-3b、FDP、板参考。
用户确认 9×14，20 mm 方格，15 mm marker，板厚 2 mm；红色长边贴箱前侧长边，蓝色短边贴左短边，零间隙，箱内底面 A 角。白边距离尚缺。

`check_detection.py` 在本地复制 `/tmp/s1_charuco_review` 上运行，生成 marker 叠加图和 JSON。使用 OpenCV 4.5.4、5X5_1000 候选超集，不代表确认完整字典。9×14/14×9 默认布局插值仅诊断，未校验 ID 空间布局，不是有效位姿或通过门禁的角点数。R03 检出 56 marker；L01、误标 R01 左腕、L03 默认检测 0。所有失败帧保留。没有 PnP、服务推理或机器人访问。

mesh 原始轴包围盒尺寸约 402.12×175×300.25 mm。原始 y 方向主要平面 -175/-172 mm 相隔约 3 mm，为内外底面候选，尚未验证实际承托面及实物匹配。其他台阶平面存在，不直接将 3 mm 当实物底厚。原始几何分析见 `mesh_geometry_diagnostic.json`。暂未构造板到箱体变换。

后续缺项：红/蓝实体板边到图案边界的白边尺寸，完整 ID 布局核验，相机去畸变状态，mesh 与实物内底面对应。此前实物图可读 EU4316，当前模型为 4317/H4 来源，匹配待验证；不能把标定板结果称为独立真值。

## 用户排除更新

用户要求删除图片对应 L03_wrist_charuco_20260915_103720_943208227；图片匹配确认。将其移出有效集并归档，当前有效集 13 帧，原始检测 JSON 为历史 14 帧诊断。R03 首帧选择不变。