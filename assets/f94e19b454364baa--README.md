# V5 单次性能分析 — 2026-09-12

## 第二次相同条件重复 — 已完成

用户要求再运行一次以检查偶然性，仍使用同一 `run_remote.py`、镜像 ID、冻结输入
及单线程配置。归档为 [thread_profile_20260912T085814Z](thread_profile_20260912T085814Z/)。

| 阶段 | 第一次秒数 | 第二次秒数 |
|---|---:|---:|
| B 箱口候选 | 5.4759 | 5.3473 |
| C 多平面候选 | 8.1660 | 8.0924 |
| M mesh 粗初值 | 0.2298 | 0.2257 |
| O 主流程观测壁点 | 0.2211 | 0.2214 |
| V/F 14 次拟合及评价 | 4.0093 | 3.9441 |
| 其余 | 0.3027 | 0.2995 |
| 算法总时间 | 18.4047 | 18.1305 |

第二次容器外部墙钟为 19.0153 s，算法总时间较第一次低 1.49%；B/C 合计占比
74.128%，与第一次 74.122% 接近。壁面提取为 6.1192 s，矩形拟合累计 4.6415 s。
14 次 fit_visible、1526 次残差调用与第一次相同。正式/native JSON 均与原基线
完全相同，accepted、退出 0、无 OOM，输入/镜像未改变，隔离参数核验一致。
说明相同冻结样本下热点分布重复出现；两次带分析器结果不能证明跨场景普适性，
也不能替代无分析器基准。该轮无 SDK、相机、远端感知或运动调用。

## 第一次记录

用户要求只运行一次简单实验。使用同一 Orin、_03 不可变镜像、冻结右腕输入、6 CPU 和四个单线程环境变量。执行脚本为 `run_remote.py`，容器命令记录于归档 `command.json`。通过 Python cProfile 调用原镜像入口，没有修改算法或镜像。

算法计时 18.4047 s；容器外部墙钟 19.3167 s。相比前轮无分析器中位数 15.4876 s 约高 18.84%，差异包含分析器开销和跨轮运行波动，不能精确分摊。以下仅为带分析器单轮热点定位。

| 非重叠阶段 | 秒 | 占算法时间 |
|---|---:|---:|
| B 箱口候选 | 5.4759 | 29.75% |
| C 多平面候选 | 8.1660 | 44.37% |
| M mesh 粗初值（含内部观测点提取） | 0.2298 | 1.25% |
| O 主流程观测壁点 | 0.2211 | 1.20% |
| V/F 拟合及评价（12 初值＋2 表面假设） | 4.0093 | 21.78% |
| 其余：导入/加载/融合/最终判定/适配器等 | 0.3027 | 1.64% |

B 内 `fit_rectangle` 3 次累计 4.753 s；C 内 `extract_wall_planes` 6.182 s，`joint_hypotheses` 1.774 s。这些是父阶段内部耗时，不能与主表重复相加。

14 次 `fit_visible` 中可见性筛选/复核共 28 次、0.0538 s；残差函数 1526 次、3.360 s；最近邻 query 共 3080 次、2.828 s。这些同样是嵌套统计。cProfile 未将同函数的 12 个初值和两个表面假设逐次拆开，未单独获得反向建树构造耗时或最终门禁耗时。不要把这次函数聚合报告说成已实现全部逐段插桩。

结论：当前样本首要热点为 B/C 初值生成，合计约 74.12% 的带分析器算法时间。仅优化后端最近邻无法单独达到 1 s；下一步优先检查箱口矩形多次优化及多平面提取/假设枚举。不能将带分析器占比机械乘到无分析器 15.49 s，也不能承诺具体加速比。

验证：退出 0、accepted、无 OOM；正式及 native JSON 与原基线完全相同；输入/镜像前后哈希或 ID 未变。本次容器由 finally 清理，SSH 执行命令已退出；未调用相机、感知服务、SDK 或运动接口。

原始归档：[thread_profile_20260912T085513Z](thread_profile_20260912T085513Z/)，其中包含 profile.pstats、profile_top80.txt、summary.json、inspect.json、温度/负载快照及完整算法输出。