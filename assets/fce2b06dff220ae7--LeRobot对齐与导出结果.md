# 联合采集数据：时间对齐与LeRobot导出

原会话：dual_arm_capture_20260923_145410_548687 / episode_000001，正式长度86.74865147秒。

## 对齐结果

- 10Hz共868个候选时刻，观测通过705行（81.2%）。
- 867行可找到有效的16维已接受动作目标；首行缺少episode内已观察到的FOLLOWING样本，明确拒收。
- 观测与动作联合通过705行。按真实网格连续区间切分，排除67个孤立单帧，导出638帧、61个派生episode。最长片段32帧，首末时间跨度3.1秒。
- 61个片段来自同一条演示，不是61次独立成功任务。不能删除缺口后拼成长轨迹，也不能把同一原始任务的片段分到训练集和验证集。

| 数据相对10Hz目标时刻 | 绝对时间差P95 |
|---|---:|
| 头左RGB | 31.90ms |
| 左腕RGB | 32.33ms |
| 右腕RGB | 32.58ms |
| 双臂关节 | 7.57ms |
| 左/右夹爪状态 | 5.76/5.85ms |

以上为705个通过行的源时间戳配对结果，不是物理曝光校准。三相机彼此的源时间跨度P95=59.63ms，最大64.06ms，不能声称同一时刻拍摄。

动作使用网格时刻之前最近被SDK接受的轨迹端点及夹爪开度。机械臂命令年龄P95约102.06ms、最大132.96ms；该年龄表示目标何时被接受，不是机器人运动延迟。真实执行时刻没有被观测。此定义适于验证“当前观测+当时已下发目标”的格式与时间索引；如训练策略需要“当前可用观测→未来动作”，需另外确定因果时间约定，不能直接将最近邻离线配对当在线可用性证明。

## LeRobot候选数据内容

输出：lerobot_runs/lerobot_towel_candidate_20260923_145410。

- observation.state：[16]，14个实测关节角和2个夹爪开度，rad/m。
- action：[16]，14个实际接受的轨迹端点和2个夹爪目标开度，rad/m；未用实测state或GP原始目标填充。
- observation.images：头左、左腕、右腕，保留原尺寸。
- task和标准episode/frame/timestamp字段，10Hz。
- source_rows.jsonl记录每个导出行的原时刻、原始观测位置/哈希、命令ID/下发时刻。
- capture_provenance.json记录验证状态、来源、动作语义及限制；以该文件status=verified作为导出加载验证完成标志。

原始17路完整保留；本次LeRobot模型输入表是三视角+16维state/action，其余相机、深度、腕力及全身状态在原件中，没有删掉，也没有宣称已全部编码进三视角模型表。

## 当前用途

可以用来检查LeRobot格式、数据加载和模型接口。短片段较多，物理时序和在线因果约定尚未验收，保持training_ready=false；不宣称这是已经足够训练出可靠叠毛巾策略的数据集。

下一步应先确定策略目标时间（已下发目标还是未来目标），检查动态画面/动作事件，再评估短状态缺口内的有界插值或合适的采样网格，避免简单放宽阈值或重复旧帧。当前86.75秒原始演示完整保留，可以重新组帧，不需要因为此次候选导出被分段就丢弃原件。

复现命令与定义见acquisition/lerobot_capture/ACTION_EXPORT_USAGE.md。新增动作提取及既有对齐共10项测试通过。

## 已完成的实际加载验证

LeRobot v3.0导出完成，638帧、61个派生episode。逐帧重载638行，state/action逐值一致，三路图像形状/有限值检查通过；DataLoader的state和action批次形状均为[2,16]。capture_provenance.json的status=verified。