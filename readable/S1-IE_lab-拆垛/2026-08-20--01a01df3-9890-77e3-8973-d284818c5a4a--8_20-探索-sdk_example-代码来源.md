# 8_20 探索 sdk_example 代码来源

- 归档编号：`01a01df3-9890-77e3-8973-d284818c5a4a`
- 项目：S1 IE_lab 拆垛
- 创建时间：2026-08-20T14:54:58.716000+08:00
- 更新时间：2026-08-20T15:29:45.402000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

探索并了解/sdk_example 中的代码内容，请问都是你上次写的吗

## 2. 助手（进展）

我会先按仓库约定阅读基础文档，再检查 `/sdk_example`（也会确认是否实际名为 `sdk_examples`）的文件、Git 历史与作者信息。只能把“代码来源证据”和“聊天中的我”区分开判断，不会仅凭风格猜测。

## 3. 助手（进展）

当前仓库本身没有 `sdk_example/`；文档指向另一个学习工作区 `/home/lsy03/Lsy03_document/galbot_s1_SDK_learning/sdk_examples/`。我会对它做只读检查，不连接机器人，也不运行任何示例。接下来重点核对该工作区自己的说明、每个脚本用途、是否被 Git 跟踪、首次提交和未提交文件。

## 4. 助手（进展）

初步结论已经很清楚：这里共有 7 个 Python 脚本，其中 4 个有 Git 提交，提交身份都是 `Michael686`；另外 3 个恢复脚本从未提交。Git 的“作者”只是提交账户，并不能证明是人手写还是 AI 生成。现有项目记录明确把这些工作归入上次的 OpenCode 会话，我再用本地会话库做一次关键词溯源，确认各文件究竟是在什么对话里生成的。

## 5. 助手

我检查的是实际目录 `/home/lsy03/Lsy03_document/galbot_s1_SDK_learning/sdk_examples/`。当前项目中没有单数的 `/sdk_example`。

结论：这些脚本基本都是上次 OpenCode 会话中生成的自研学习代码，不是 Galbot 官方示例原件。但不能严谨地说是“现在这个 Codex 的我写的”；更准确地说，是上次的 OpenCode AI 助手在与你协作时生成或整理的，Git 提交账户是 `Michael686`。

共有 7 个 Python 脚本：

| 脚本 | 作用 | 来源证据 |
|---|---|---|
| `read_robot_state.py` | 只读设备、关节、IMU 状态 | 已提交，2026-08-15 |
| `test_head_micro_move.py` | 头部两个关节 `+0.05 rad` 后返回 | 已提交，2026-08-15 |
| `test_left_arm_micro_move.py` | 左臂第一个关节 `+0.1 rad` 后返回 | 已提交，2026-08-15 |
| `test_head_visible_move.py` | 头部前后约 `±0.4 rad` 摆动 | 已提交，2026-08-15 |
| `restore_pre_example2_pose.py` | 恢复 torso、head 和双臂 | 未提交，上次会话生成 |
| `restore_torso_home.py` | 单独把升降柱升到 `0.74` | 未提交，上次事故后的修正脚本 |
| `restore_arms_pre_example2.py` | 双臂恢复到一次历史测量姿态 | 未提交，上次会话生成 |

其中前四个脚本的 Git 提交作者都是：

```text
Michael686 <464189266@qq.com>
```

这只能说明“谁提交的”，不能证明代码是纯人工编写。结合 OpenCode 历史记录、文件生成顺序和项目文档，证据支持它们是上次 AI 协作生成、由用户验证或提交的代码。

需要特别注意：

- `read_robot_state.py` 是只读脚本，但仍会初始化 SDK 和连接机器人。
- 后面 6 个都涉及或可能涉及真机运动。
- `restore_pre_example2_pose.py` 是已确认的不安全历史脚本：它把没有测量过的 torso 位置猜成了 `0.0`，曾导致升降柱意外下降。不能运行或复用。
- `restore_arms_pre_example2.py` 中的双臂数据只是某一时刻测得的姿态，不代表官方 home、初始姿态或安全恢复姿态。
- `restore_torso_home.py` 中的 `0.74` 是事故后采用过的目标，也不能脱离具体机器人型号和当前状态直接再次使用。

所以简短回答是：**是的，这些 Python 学习脚本看起来都是上次 OpenCode AI 与你协作时写的或整理的；但它们不是都安全、正确或适合再次运行，尤其三个 `restore_*` 脚本必须作为历史实验代码看待。**

## 6. 用户

我现在本地是不是没有 S1的所有源码？我是不是应该去github上下载一下

## 7. 助手（进展）

我先核对本地现有的 SDK 参考目录、Git remote 和压缩包内容，再确认 GitHub 上是否存在官方、完整且与你的 S1/SDK 版本匹配的仓库。重点是区分“SDK 接口示例/存根”与“机器人底层全部源码”。

## 8. 助手（进展）

本地情况已确认：现有 `third_party/GalbotSDK` 只有约 1.1 MB、123 个参考文件，缺少运行时 `.so`、完整示例、配置和依赖；那个 135 MB 的 `GalbotSDK-main.zip` 也不是有效的完整 ZIP（很可能下载未完成或损坏）。因此本地确实没有完整 SDK 发行仓库，更不等于 S1 机器人“所有底层源码”。我现在核对官方 GitHub 仓库的公开范围和版本说明。

## 9. 助手

是的，你本地目前没有完整的 S1 开发资料。

当前只有：

- 一个约 1.1 MB 的 `GalbotSDK` 参考子集，仅 123 个文件；
- Python API 类型存根和部分官方示例；
- 没有 SDK 运行时 `.so`、完整依赖、配置和部署工具；
- 135 MB 的 `GalbotSDK-main.zip` 已损坏或下载不完整，无法解压。

建议从 GitHub 下载，但要分清两件事：

1. [官方 GalbotSDK](https://github.com/GalaxyGeneralRobotics/GalbotSDK)
   包含完整的公开 SDK 发行内容、API 文档、示例、依赖和预编译运行库。官方当前标明 SDK `V1.9.1` 对应机器人环境 `GBS V1.17`。

2. [官方 S1 Description](https://github.com/GalaxyGeneralRobotics/galbot_s1_description)
   包含 S1 的 URDF、MJCF、USD 模型，适合结构分析、可视化和仿真。

不过，即使下载这两个仓库，也不等于获得“S1 所有源码”。通常仍不会包含：

- 电机驱动和固件源码；
- 实时控制器内部实现；
- 机器人系统镜像全部源码；
- 厂商未公开的导航、感知或硬件服务；
- IMC、IRMV_DUAL_ARM 和交大项目的全部代码。

我的建议是：**现在可以下载官方仓库作为只读参考，但暂时不要安装、部署到机器人或替换机器人上的 SDK。** 先通过只读方式确认机器人实际的 GBS/SDK 版本；历史记录只证明当时机器人能导入 SDK `1.9.0`，不能证明当前一定是 GBS `1.17`。

推荐目录结构：

```text
third_party/
├── GalbotSDK/                  # 官方完整 SDK 仓库
└── galbot_s1_description/      # 官方 S1 模型
```

下载最好使用 `git clone`，而不是再次下载 ZIP，这样能保留版本历史并切换到与机器人匹配的 tag/commit。下载本身不会连接或控制机器人。

## 10. 用户

给我命令我来下载

## 11. 助手

为避免覆盖现有的参考子集，下载到新的目录：

```bash
cd /home/lsy03/Lsy03_document/galbot_s1_SDK_learning/third_party

git clone https://github.com/GalaxyGeneralRobotics/GalbotSDK.git GalbotSDK_official_full

git clone https://github.com/GalaxyGeneralRobotics/galbot_s1_description.git galbot_s1_description
```

下载后检查：

```bash
cd /home/lsy03/Lsy03_document/galbot_s1_SDK_learning/third_party/GalbotSDK_official_full

git status
git log -1 --oneline
cat VERSION
du -sh .
```

查看 SDK 版本分支和标签：

```bash
git branch -a
git tag --sort=-version:refname | head -20
```

暂时不要执行 `install.sh`、`deploy_to_robot.sh` 或任何示例。下载和查看代码不会控制机器人。

## 12. 用户

lsy03@lsy03-System-Product-Name:~/Lsy03_document/note$ cd /home/lsy03/Lsy03_document/galbot_s1_SDK_learning/third_party

git clone https://github.com/GalaxyGeneralRobotics/GalbotSDK.git GalbotSDK_official_full

git clone https://github.com/GalaxyGeneralRobotics/galbot_s1_description.git galbot_s1_description
正克隆到 'GalbotSDK_official_full'...
remote: Enumerating objects: 24052, done.
remote: Counting objects: 100% (557/557), done.
remote: Compressing objects: 100% (265/265), done.
remote: Total 24052 (delta 385), reused 352 (delta 239), pack-reused 23495 (from 3)
接收对象中: 100% (24052/24052), 481.76 MiB | 1.95 MiB/s, 完成.
处理 delta 中: 100% (8707/8707), 完成.
正在更新文件: 100% (34073/34073), 完成.
正克隆到 'galbot_s1_description'...
remote: Enumerating objects: 471, done.
remote: Counting objects: 100% (22/22), done.
remote: Compressing objects: 100% (18/18), done.
remote: Total 471 (delta 3), reused 19 (delta 2), pack-reused 449 (from 2)
接收对象中: 100% (471/471), 29.78 MiB | 1.33 MiB/s, 完成.
处理 delta 中: 100% (42/42), 完成.
lsy03@lsy03-System-Product-Name:~/Lsy03_document/galbot_s1_SDK_learning/third_party$

是否下载完成？

## 13. 助手

是的，两个仓库都已完整克隆成功。

关键成功标志是：

```text
接收对象中: 100%
处理 delta 中: 100%
正在更新文件: 100%
```

且终端已正常返回提示符，没有报错。

可以进一步确认：

```bash
cd /home/lsy03/Lsy03_document/galbot_s1_SDK_learning/third_party

git -C GalbotSDK_official_full status
git -C GalbotSDK_official_full log -1 --oneline
cat GalbotSDK_official_full/VERSION

git -C galbot_s1_description status
git -C galbot_s1_description log -1 --oneline

du -sh GalbotSDK_official_full galbot_s1_description
```

正常情况下两个 `git status` 都会显示工作区干净。现在先不要执行 SDK 的安装、部署或机器人运动示例。

## 14. 用户

我下载的S1源码只是来学习或给你来做参考的，我不会全部上传到S1机器人，你先看看

## 15. 助手（进展）

明白，我只把这两个仓库作为本地只读参考：不安装、不部署、不连接 S1，也不运行任何可能产生运动的示例。我先核对版本、目录结构、公开源码与预编译库的边界，再给你一份适合后续学习的导航。

## 16. 助手

我已经只读检查过了，没有安装、部署、连接机器人或运行示例。

检查结果：

- `GalbotSDK_official_full` 下载完整，版本为 **1.9.1**，对应官方说明的 **GBS 1.17**。
- 包含 x86_64 和 aarch64 的预编译 `.so`、Python 绑定、API 头文件、完整文档及大量 S1 示例。
- 但它仍不是机器人底层实现的“全部源码”：核心 SDK 主要以预编译动态库提供；公开的 `.cpp/.py` 主要是示例程序，大量头文件来自打包依赖。
- `galbot_s1_description` 包含 S1 的 URDF、MJCF 和 USD 模型，适合学习机械结构、关节、坐标系和仿真。

最值得注意的是，SDK 1.9.1 更新日志明确写着：

> 修复 torso 零位不正确引起的相关问题。

这与上次 `restore_pre_example2_pose.py` 把 torso 错误设为 `0` 的事故高度相关，也再次说明旧恢复脚本不能继续使用。

建议的只读学习顺序：

1. [SDK 中文文档入口](/home/lsy03/Lsy03_document/galbot_s1_SDK_learning/third_party/GalbotSDK_official_full/docs/s1/zh/index.html)
2. [S1 Python 示例](/home/lsy03/Lsy03_document/galbot_s1_SDK_learning/third_party/GalbotSDK_official_full/examples/s1/python)
3. 先读 `galbot_robot` 中的状态接口
4. 再读 `galbot_motion` 的 FK、IK、Jacobian、碰撞检测和规划
5. 然后学习 `galbot_navigation` 与 `galbot_perception`
6. [S1 URDF](/home/lsy03/Lsy03_document/galbot_s1_SDK_learning/third_party/galbot_s1_description/urdf/galbot_s1.urdf) 用来核对关节名、结构和坐标系

后续我可以直接以这份官方 1.9.1 代码为参考，逐个解释接口，并与原来 `sdk_examples/` 中的自研脚本进行静态对照；不会默认执行其中任何程序。

## 17. 用户

查看当前上下文的使用情况

## 18. 助手（进展）

我会使用 `openai-docs` 技能确认 Codex 当前能否显示精确的上下文窗口占用；这是关于本应用自身能力的问题。

## 19. 助手

当前我无法读取本任务精确的 token 数或“上下文已使用百分比”；Codex 没有向我暴露这个实时计数。

目前状态：

- 对话上下文仍然完整可用；
- 已加载 S1 项目的核心文档、SDK 示例检查结果和新下载仓库的概要；
- 尚未出现上下文压缩导致的信息缺失；
- 后续如果内容过长，Codex 会自动压缩历史，我会依据仓库文档继续工作。

官方文档会公布模型的最大上下文窗口，但不能据此计算当前任务的实际占用。[OpenAI 模型文档](https://developers.openai.com/api/docs/models/gpt-5-codex)

## 20. 用户

直接读取当前任务日志中的最新 model_context_window 和 token 统计，确认重启后的配置是否已经生效。




已运行 session_file=$(rg -l --hidden --glob '*.jsonl' "$CODEX_SESSION_ID" /home/lsy03/.codex/sessions 2>/dev/null | head -n 1); printf '%s\n' "$session_file"; rg '"type":"token_count"' "$session_file" | tail -n 1







当前任务最新统计：
配置窗口：1,000,000 tokens
Codex 显示的有效上下文窗口：828,400 tokens
当前最近一次输入：84,301 tokens
使用比例：约 10.2%
剩余有效容量：约 744,099 tokens
说明 1M 配置已经生效；Codex 为系统指令、工具输出及响应空间预留了一部分，因此任务日志显示的实际可用窗口是约 828K。 是否正确

## 21. 助手（进展）

我会直接读取当前任务对应 JSONL 的最后一条 `token_count`，核对字段含义和计算，不修改任何配置或日志。

## 22. 助手

部分正确，但你列出的 token 数已经不是当前最新值。

我直接读取了当前任务日志，最新记录为：

```text
配置文件 model_context_window：1,000,000
日志 model_context_window：828,400
最近一次 input_tokens：72,379
最近一次 total_tokens：72,668
累计 total_tokens：877,907
```

按最近一次输入计算：

```text
72,379 ÷ 828,400 ≈ 8.74%
剩余约 756,021 tokens
```

若把最近一次输入和输出都计算在内：

```text
72,668 ÷ 828,400 ≈ 8.77%
剩余约 755,732 tokens
```

因此：

- **1M 配置已经生效**：正确。
- **有效上下文窗口为 828,400**：正确。
- **84,301、10.2%、剩余 744,099**：曾可能是某次统计，但不是我刚读取到的最新记录。
- `877,907 total_tokens` 是任务运行期间的累计消耗，不代表当前上下文已经占用 877K。
- “其余空间用于系统指令、工具和响应预留”是合理解释，但日志本身只明确给出 `828400`，没有逐项说明预留空间如何分配。
