# 查看上下文长度并调整至1M

- 归档编号：`01a01e07-ec64-7791-a9d2-1cc393bd1687`
- 项目：S1 IE_lab 拆垛
- 创建时间：2026-08-20T15:17:10.890000+08:00
- 更新时间：2026-08-20T15:28:50.200000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

查看当前对话的上下文长度，看能不能改成1 m

## 2. 助手（进展）

我会先查一下本地 Codex 配置与官方说明，确认“当前上下文长度”是否可见，以及 1M 是否是可配置项。这里会使用 openai-docs 技能，因为问题涉及 Codex 自身配置。

## 3. 助手（进展）

官方模型页确认：当前这类 GPT‑5.6 Sol 模型的原生上下文窗口是 1,050,000 tokens。接下来我检查本机当前任务实际选用的模型，以及有没有人为设置更小的窗口上限。

## 4. 助手

当前任务实测：

- 模型：`gpt-5.6-sol`
- 当前有效上下文上限：`258,400 tokens`
- 最近一次请求占用：约 `43,834 tokens`，约占 `17%`
- 模型原生窗口：`1,050,000 tokens`（约 1M）([OpenAI 模型说明](https://developers.openai.com/api/docs/models/gpt-5.6-sol))

可以配置成 1M。Codex 官方支持：

```toml
model_context_window = 1000000
```

写入 `~/.codex/config.toml` 即可；官方配置参考见 [Configuration Reference](https://learn.chatgpt.com/docs/config-file/config-reference)。

不过该设置通常只对**重新启动后或新建的任务**生效，无法把正在运行的当前任务即时扩展到 1M。当前配置文件尚未设置这一项。

## 5. 用户

你现在已经配置了吗

## 6. 助手

还没有。我刚才只进行了只读检查，没有修改 `~/.codex/config.toml`。

目前仍是 `258,400 tokens`；要改成 1M，需要添加：

```toml
model_context_window = 1000000
```

并重启 Codex 或新建任务后生效。

## 7. 用户

帮我添加

## 8. 助手（进展）

我会使用 openai-docs 技能按官方配置项执行，并修改用户级 `~/.codex/config.toml`。该文件在当前工作区之外，写入时系统会弹出一次权限确认。

## 9. 助手

已添加并验证：

```toml
model_context_window = 1000000
```

配置位置：[config.toml](/home/lsy03/.codex/config.toml:3)

请重启 Codex Desktop；新建任务后会按 1M 上下文配置运行。当前正在运行的任务不会即时切换。官方配置依据：[OpenAI Docs](https://learn.chatgpt.com/docs/config-file/config-reference)。

## 10. 用户

11

## 11. 助手

收到“11”。如果你是在测试消息，配置已完成；重启 Codex 后新任务会使用 1M 上下文。

## 12. 用户

查看当前上下文长度

## 13. 助手（进展）

我会直接读取当前任务日志中的最新 `model_context_window` 和 token 统计，确认重启后的配置是否已经生效。

## 14. 助手

当前任务最新统计：

- 配置窗口：`1,000,000 tokens`
- Codex 显示的有效上下文窗口：`828,400 tokens`
- 当前最近一次输入：`84,301 tokens`
- 使用比例：约 `10.2%`
- 剩余有效容量：约 `744,099 tokens`

说明 1M 配置已经生效；Codex 为系统指令、工具输出及响应空间预留了一部分，因此任务日志显示的实际可用窗口是约 `828K`。
