# 9_2 定位导航PCD点云图

- 归档编号：`01a064c6-a968-7ff2-a522-248ff168002a`
- 项目：S1 IE_lab 拆垛
- 创建时间：2026-09-03T08:58:56.236000+08:00
- 更新时间：2026-09-03T09:41:56.705000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

9_3 点云清理 以及 导航闭环

现在你能通过SSH 看 S1 内部吗？ 我们之前导航使用的PCD 点云图是用的哪个目录下的那一张？

## 2. 助手（进展）

我先按项目约定核对仓库说明、SDK/SSH 会话记录和导航相关文档。当前这一步只查本地记录，不会连接机器人，也不会发送任何运动命令。

## 3. 助手（进展）

本地记录已经指向 `/var/maps/room0804/global_cloud_cleaned.pcd`，但我还要区分“曾拷贝分析的文件”和“当时 PNS 实际加载的文件”。接下来我会做一次只读 SSH 核验：仅查看主机身份、地图目录和相关配置/进程参数，不初始化 SDK、不改定位状态、不启动导航；机械臂、夹爪、升降柱和底盘都不会动。

## 4. 助手（进展）

SSH 网络本身已能发起，但旧的复用连接 `/tmp/s1_codex_mux_20260831_lidar` 已不存在，直接连接又因没有可用密钥/密码而被 S1 拒绝。因此我现在还不能进入主机；地图文件名则已有本地日志与哈希记录可以明确回答。我再核对一下仓库保存的文件元数据，避免把原始 PCD 和清理版混淆。

## 5. 助手

目前 S1 的 SSH 端口可达，但我还不能登录：旧复用连接已失效，直接连接因缺少认证被拒绝。

此前导航使用的清理版点云是：

```text
/var/maps/room0804/global_cloud_cleaned.pcd
```

当时：

```text
/var/maps/cur -> /var/maps/room0804
```

所以程序也可能通过下面的等价路径读取：

```text
/var/maps/cur/global_cloud_cleaned.pcd
```

不是同目录下未清理的 `global_cloud.pcd`。本机保存的同一份副本是 [global_cloud_cleaned.pcd](</home/lsy03/文档/ChatGPT/S1/experiments/20260827_s1_navigation/s1_room0804/global_cloud_cleaned.pcd>)，大小 174,182,256 字节，哈希校验通过。

另外要区分：`global_cloud_cleaned.pcd` 是静态全局点云；导航规划实际还会使用 fusion 根据它和三路实时雷达生成的运行时 ESDF，并非只在 PCD 上直接闭环。

如需恢复 SSH，请在本机终端运行：

```bash
ssh -M -S /tmp/s1_codex_mux_20260903 \
  -o ControlMaster=yes -o ControlPersist=4h \
  -Nf galbot@10.34.216.17
```

认证完成后告诉我，我就可以继续只读核对 S1 当前的 `/var/maps/cur` 指向、实际加载参数和点云清理情况。

## 6. 用户

# Files pasted by the user:

## "galbot@galbot-echo:/data/galbot/bin$ ll total 422556 drwxrwxrwx 3 galbot galbot…": /home/lsy03/.codex/attachments/07e774f9-8e34-4045-9732-0ae4cff6065a/pasted-text.txt

## My request:
是不是少了什么环境？给我命令，我来修改

## 7. 助手（进展）

我先看你贴出的终端内容，判断是缺少 SDK 环境、SSH 环境，还是命令所在目录不对。这里只分析和给出修改命令，不会连接机器人或执行任何控制/运动。

## 8. 助手（进展）

从报错看，二进制本身存在，缺的是动态库搜索路径，不是少装 `mapping_server`。不过不要直接启动它：真机上的地图/定位服务通常由系统统一拉起，手工再开一份可能造成端口、DDS 或地图状态冲突。我先从已有记录中找厂家实际使用的环境变量和启动方式，再给你“只检查”和“临时修复”两组命令。

## 9. 助手

是，少了两项“当前终端环境”，并不是少装程序：

- `PATH` 中没有 `/data/galbot/bin`，所以直接输入 `mapping_server` 找不到。
- `LD_LIBRARY_PATH` 中没有 `/data/galbot/lib`，所以运行 `./mapping_server` 找不到 `libmap_engine_shared.so.2.1`。

在 S1 当前终端中执行：

```bash
source /home/galbot/ie_lab/bin/s1-env
export PATH="/data/galbot/bin:$PATH"
```

如果 `s1-env` 不存在，再用：

```bash
export PATH="/data/galbot/bin:$PATH"
export LD_LIBRARY_PATH="/data/galbot/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
```

然后只做检查，不要启动 `mapping_server`：

```bash
command -v mapping_server
ls -l /data/galbot/lib/libmap_engine_shared.so*
ldd /data/galbot/bin/mapping_server | grep 'not found' || echo "动态库检查通过"
pgrep -af mapping_server
```

不要把这些变量永久写进 `.bashrc`，也先不要执行 `mapping_server`。它是地图服务程序，不是单独的 PCD 清理工具；真机中很可能已有系统管理的实例，重复启动可能干扰定位与导航。

你已经成功登录 S1，说明 SSH 本身没问题。如果希望我也能通过复用连接进行只读检查，请在“本机的新终端”（不是 `galbot@galbot-echo` 里面）运行：

```bash
ssh -M -S /tmp/s1_codex_mux_20260903 \
  -o ControlMaster=yes \
  -o ControlPersist=4h \
  -Nf galbot@10.34.216.17
```

完成后告诉我，并把上述四条检查命令的输出贴过来。

## 10. 用户

/var/maps/room1102_0903/  这是新地图路径，我想保存到本地，并进行点云清理，给我命令我来执行

## 11. 助手（进展）

可以。整个流程都在本机完成：先从 S1 只读复制 `/var/maps/room1102_0903/`，再对本地副本处理，绝不修改机器人上的新地图。地图若仍在建图写入，先不要复制；下面我会把稳定性检查也放进命令里。点云清理不会让任何设备运动，也不调用 SDK、定位或导航。

## 12. 助手

先把新地图完整、只读地复制到本机；暂时不要直接覆盖或清理 S1 上的文件。请在“本机终端”执行，不是在 `galbot@galbot-echo` 终端执行。

### 1. 确认地图已经停止写入

```bash
S1_HOST=galbot@10.34.216.17
REMOTE_MAP=/var/maps/room1102_0903

ssh "$S1_HOST" \
  "find '$REMOTE_MAP' -type f -printf '%P|%s|%T@\n' | sort" \
  > /tmp/room1102_0903_snapshot_1.txt

sleep 15

ssh "$S1_HOST" \
  "find '$REMOTE_MAP' -type f -printf '%P|%s|%T@\n' | sort" \
  > /tmp/room1102_0903_snapshot_2.txt

diff -u \
  /tmp/room1102_0903_snapshot_1.txt \
  /tmp/room1102_0903_snapshot_2.txt
```

`diff` 没有输出，表示这 15 秒内文件没有变化，可以继续复制。如果有输出，说明地图可能仍在写入，先不要复制。

### 2. 复制到本地项目

```bash
LOCAL_MAP='/home/lsy03/文档/ChatGPT/S1/experiments/20260903_s1_navigation/s1_room1102_0903'

mkdir -p "$LOCAL_MAP"

rsync -aH --partial --info=progress2 \
  "${S1_HOST}:${REMOTE_MAP}/" \
  "$LOCAL_MAP/"
```

不要加入 `--delete`，这样不会误删本地文件。传输中断后重新运行同一条 `rsync` 即可续传。

### 3. 核验本地副本

```bash
find "$LOCAL_MAP" -maxdepth 2 -type f \
  -printf '%%P\t%s bytes\t%TY-%Tm-%Td %TH:%TM:%TS\n" \
  | sort

du -sh "$LOCAL_MAP"

find "$LOCAL_MAP" -maxdepth 1 -type f -name '*.pcd' \
  -exec sha256sum {} + \
  | tee "$LOCAL_MAP/PCD_SHA256_BEFORE_CLEANING.txt"
```

上面的 `find` 命令引号修正版如下，请使用这一条：

```bash
find "$LOCAL_MAP" -maxdepth 2 -type f \
  -printf '%P\t%s bytes\t%TY-%Tm-%Td %TH:%TM:%TS\n' \
  | sort
```

### 4. 查看 PCD 格式

```bash
for pcd in "$LOCAL_MAP"/*.pcd; do
  echo "===== $pcd ====="
  awk '1; /^DATA / {exit}' "$pcd"
done
```

先不要用 Open3D直接保存。旧地图的 PCD 含有 `time/ring/intensity/Original_cloud_index` 等厂家字段，Open3D重新保存可能只保留 XYZ，破坏 S1 所需的数据结构。

执行完后，把以下内容发给我：

- `ls -lh "$LOCAL_MAP"` 的输出；
- 第 4 步显示的所有 PCD 头；
- 哪一张是原始点云；
- 你希望删除的对象类型，例如人、箱子、散点、地面、天花板或指定区域。

拿到实际 PCD 格式后，我再给你一套“保留厂家字段、原文件不动、输出 `global_cloud_cleaned.pcd` 候选文件”的精确清理命令。
