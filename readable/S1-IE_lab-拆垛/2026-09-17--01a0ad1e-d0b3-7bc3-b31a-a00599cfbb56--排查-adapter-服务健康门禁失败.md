# 排查 adapter 服务健康门禁失败

- 归档编号：`01a0ad1e-d0b3-7bc3-b31a-a00599cfbb56`
- 项目：S1 IE_lab 拆垛
- 创建时间：2026-09-17T10:07:53.016000+08:00
- 更新时间：2026-09-17T14:17:30.515000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

346 time delay: 0.00565773 x 2.37726 y 0.393616 z -0.00598967 qx 0.00109959 qy -0.00168536 qz -0.00334562 qw 0.999992
^C
galbot\@galbot-echo:\~$ cd \~/ie\_lab/projects/s1\_continuous\_destack/deploy
galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack/deploy$ bash start\_continuous\_monitored.sh --execute
RUN\_ID: random\_20260917\_100652
Read-only monitoring: /home/galbot/ie\_lab/data/s1\_continuous\_destack/monitoring/random\_20260917\_100652
Starting fresh mission: preflight -> SAM -> autonomous survey/pick/unload.
development continuous RUN\_ID: random\_20260917\_100652
control lock acquired safely: /home/galbot/ie\_lab/control.lock (operator=codex-assisted-user)
development run/source: random\_20260917\_100652 7e1aed53d71fdb60c30ea67fbd5307f44a4c84c1118220b4cdbfe6184230f2fe
{"error": "adapter/service health gate failed", "error\_type": "GateRefused", "ok": false, "schema": "s1\_destack.cli\_error.v1"}
Mission exited (2); logs retained: /home/galbot/ie\_lab/data/s1\_continuous\_destack/monitoring/random\_20260917\_100652
galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack/deploy$&#x20;

给我看下什么问题

## 2. 助手（进展）

我先按项目约定检查本地仓库的说明、监控/部署代码和对应错误定义；暂不连接真机，也不执行任何运动命令。重点会追 `adapter/service health gate failed` 这个健康门禁为什么拒绝启动。

## 3. 助手（进展）

仓库说明确认了两点：这次日志对应的是远端 `s1_continuous_destack` 任务，但本地知识库目前没有直接收录这条 `GateRefused` 的具体原因；另外，项目要求把聊天里的结论当历史证据，不能直接当作当前真机状态。接下来我会定位本地是否有该部署脚本/门禁实现、失败 run 的归档或测试。

## 4. 助手（进展）

从已贴出的几行可以先确定：这是“启动前门禁拒绝”，不是算法报错，也不是任务运行中途失败。为了避免把历史的 401/服务配置问题误套到这次运行上，我再核对本地记录里曾出现过的服务认证与健康检查模式，然后给你一个最短的只读排查命令。

## 5. 助手

从这段日志看，问题发生在启动前置检查：

- `adapter/service health gate failed`：SAM/LingBot 等适配器或服务健康检查未通过。
- `GateRefused`、退出码 `2`：程序主动拒绝执行，不是算法崩溃。
- 失败发生在 `SAM -> survey/pick/unload` 之前，目前没有看到运动命令已执行。
- 前面的位姿输出和 `Ctrl+C` 只是监视程序被停止，不能证明服务正常。

最可能的原因是：服务未启动、地址/端口不可达、健康接口超时，或 API key 缺失/失效。历史上曾出现过 SAM3 `HTTP 401`，但这次仅凭顶层错误还不能确认就是认证问题。

先不要绕过 gate 或重复执行 `--execute`。请在机器人上运行以下只读检查：

```bash
RUN_ID=random_20260917_100652
D=/home/galbot/ie_lab/data/s1_continuous_destack/monitoring/$RUN_ID

find "$D" -maxdepth 3 -type f -printf '%P\n' | sort

rg -n -i \
  'gate|health|adapter|service|error|exception|401|403|404|timeout|refused|unreachable|connection' \
  "$D"
```

把输出发我（涉及 token/password 的内容打码），就能继续定位是哪个服务、哪个地址或哪项认证失败。

## 6. 用户

galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack/deploy$ rg -n -i \


> 'gate|health|adapter|service|error|exception|401|403|404|timeout|refused|unreachable|connection' \
> "$D"
> -bash: rg: command not found
> galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack/deploy$

你去机器人上给我查一下， 是不是有可能是FDP服务没有起来？

## 7. 助手（进展）

可以。我现在按“只读远程检查”处理：只查这次 run 的监控文件、FDP/SAM/LingBot 服务进程与健康接口，不调用 SDK、不发运动命令、不修改机器人文件。因为你已明确授权连接 S1，我先读取项目的 `galbot-s1-ssh` 操作规范，再开始检查。

## 8. 助手（进展）

远程只读检查已经连上了。当前有一个重要线索：这次 run 目录只有 `mission.log`、监视 CSV/JSON 和夹爪监视文件，没有生成任何 SAM/FDP 响应或位姿结果；本机也没有正在运行的 FDP/SAM/LingBot 进程，`curl` 命令不存在。我要继续读门禁实现和配置，并用 Python 标准库只做 HTTP 健康 GET，确认 FDP 是“没启动、不可达、返回错误”还是其实门禁检查的是别的服务。

## 9. 助手（进展）

门禁代码已经确认：`dev-run` 在任何任务步骤前调用 `adapter.health()`，而 `continuous_validation.yaml` 明确要求三个健康端点同时通过：

- SAM3：`10.34.216.11:7861/health`
- LingBot：`10.34.216.11:7865/health`
- FDP：`10.34.216.13:7876/health`

所以“FDP 没起来”确实是可能原因，但现在还不能排除 SAM3/LingBot。项目自带的 `probe_configured_service_health.py` 明确是被动 HTTP GET，我将用它做一次不写缓存、不初始化控制器的检查。

## 10. 助手

确认了，问题就是 FDP 服务端口未监听。

机器人上的被动健康检查结果：

- SAM3 `10.34.216.11:7861/health`：HTTP 200，正常
- LingBot `10.34.216.11:7865/health`：HTTP 200，正常
- FDP `10.34.216.13:7876/health`：`Connection refused`

因此不是认证失败或超时，而是 `10.34.216.13:7876` 当前没有服务监听，常见原因是 FDP 进程/容器没启动、崩溃，或端口映射改变。

本次任务在健康门禁阶段就退出了，没有进入 SAM、巡检、抓取或卸货，未发现运动命令执行。FDP 服务配置在远端 `10.34.216.13`，不是机器人本机上的进程。

先在 FDP 服务器上启动/恢复 `7876` 服务；恢复后在机器人上重新执行：

```bash
cd ~/ie_lab/projects/s1_continuous_destack
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/src \
python3 tools/probe_configured_service_health.py \
--config $PWD/config/missions/continuous_validation.yaml
```

确认三个服务都为 `healthy: true` 后，再使用新的 `RUN_ID` 运行任务；旧的 `random_20260917_100652` 已经被占用，不能直接复用。

## 11. 用户

galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/src \


> python3 tools/probe\_configured\_service\_health.py \
> \--config $PWD/config/missions/continuous\_validation.yaml
> {
> "actuating": true,
> "adapter": "s1",
> "command\_session\_active": false,
> "healthy": true,
> "runtime\_modules\_loaded": false,
> "schema": "s1\_destack.adapter\_health.v1",
> "sdk\_status": "not\_initialized",
> "services": {
> "healthy": true,
> "schema": "s1\_destack.service\_health.v1",
> "services": {
> "fdp": {
> "detail": {
> "activate\_2d\_tracker": false,
> "activate\_kalman\_filter": false,
> "default\_mesh": "/home/c/workspace/260611\_sjtu\_workbin\_movement/test\_project/源代码/DualMobManip/deps/mesh/EU4322\_midcut\_target175mm.obj",
> "est\_refine\_iter": 3,
> "fdp\_src": "/home/c/workspace/260611\_sjtu\_workbin\_movement/migrate\_back/g1\_workbin\_perception/third\_party/FoundationPose-plus-plus/src",
> "gpu": {
> "cuda\_available": true,
> "device\_count": 1,
> "device\_name": "NVIDIA GeForce RTX 4090",
> "torch": "2.4.1+cu121",
> "torch\_cuda": "12.1"
> },
> "import\_error": null,
> "ok": true,
> "service": "local\_fdp\_4090",
> "track\_refine\_iter": 1,
> "tracker\_count": 0
> },
> "error": null,
> "healthy": true,
> "name": "fdp",
> "status\_code": 200,
> "url": "[http://10.34.216.13:7876/health](http://10.34.216.13:7876/health)"
> },
> "lingbot": {
> "detail": {
> "checkpoint": "/workspace/LingBot-Depth-Weights/model.pt",
> "code\_dir": "/workspace/LingBot-Depth",
> "device": "cuda",
> "loaded": true,
> "ok": true,
> "service": "lingbot-depth"
> },
> "error": null,
> "healthy": true,
> "name": "lingbot",
> "status\_code": 200,
> "url": "[http://10.34.216.11:7865/health](http://10.34.216.11:7865/health)"
> },
> "sam3": {
> "detail": {
> "checkpoint": "/workspace/SAM3-Runtime/sam3.pt",
> "device": "cuda",
> "loaded": true,
> "ok": true,
> "service": "sam3"
> },
> "error": null,
> "healthy": true,
> "name": "sam3",
> "status\_code": 200,
> "url": "[http://10.34.216.11:7861/health](http://10.34.216.11:7861/health)"
> }
> }
> }
> }
> galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack$

给我看下这个



现在应该好了吧

## 12. 助手

是的，现在已经恢复正常，健康门禁应能通过。

关键结果：

- 总体 `healthy: true`
- FDP：HTTP 200，`ok: true`
- FDP 模型已加载，`import_error: null`
- GPU：RTX 4090，CUDA 可用
- SAM3、LingBot：均 HTTP 200、模型已加载

`tracker_count: 0` 不算问题，因为当前配置明确关闭了 tracker。

这次探针仍是只读的：`command_session_active: false`、`runtime_modules_loaded: false`，没有控制机器人。它能证明服务健康，但不能完全替代一次真实推理验证。

下一次运行必须使用新的 `RUN_ID`，不要复用 `random_20260917_100652`。执行前确认工作区、箱体和急停条件安全，因为后续任务会涉及底盘、机械臂、夹爪和升降的真实动作。

## 13. 用户

给我检查下这个新问题

[附件：4edcec1580190028--pasted-text.txt](../../assets/4edcec1580190028--pasted-text.txt)

## 14. 助手（进展）

我先读取你贴出的新日志，定位它是在执行前、哪个任务步骤，以及是否有新的服务/运动错误；先不连接机器人、不重复执行任务。

## 15. 助手（进展）

这次不是 FDP 健康问题了。日志明确显示新 run `random_20260917_135836` 进入 `state: BLOCKED`，退出码 `4`；但你贴出的片段没有包含真正的阻断原因，只有“就绪检查摘要”。我先把附件完整内容的行号和开头/结尾核对一下，看看前面是否有具体 `failure_code` 或 preflight 结果。

## 16. 助手

这是另一个问题，FDP 已经不是阻断点。

从日志能确定：

- 任务进入 `state: BLOCKED`，退出码 `4`，属于安全门禁阻断，不是程序崩溃。
- `motion_commands_sent: false`、`sdk_initialized: false`，本次没有运动。
- 静态就绪检查均为 `ready: true`。
- 真正可疑的是：

```text
semantic_state_observed: false
runtime_acceptance_condition:
preflight_probe.accepted_is_true_after_three_or_more_fresh_samples
```

也就是启动前的机器人语义状态 preflight 没有获得“连续至少 3 次有效接受”的结果，因此任务 fail-closed 停止。`host_map_attestation_verified_here: false` 和 `pending_physical_commissioning` 在此处是状态说明，不是直接失败项，因为整体 `ready: true`。

你贴的是日志后半段，具体拒绝原因在前面。请在机器人上执行：

```bash
M=/home/galbot/ie_lab/data/s1_continuous_destack/monitoring/random_20260917_135836

grep -n -i -E \
'blocked|error|failure|preflight|probe|accepted|reason|semantic|state' \
"$M/mission.log"

sed -n '1,220p' "$M/mission.log"
```

重点看 `preflight_probe`、`accepted` 和 `failure_codes`。目前不要重复执行真实任务。

## 17. 用户

你直接进S1给我找找

## 18. 助手（进展）

我现在按 S1 只读 SSH 规范进入机器人，直接检查 `random_20260917_135836` 的完整 `mission.log`、run 目录文件和 preflight 相关输出。只读 `grep/sed/find`，不启动服务、不初始化 SDK、不发运动命令。

## 19. 助手（进展）

已经找到真正原因：不是 FDP，也不是 preflight 语义状态。

这次运行中：

- FDP/SAM3/LingBot 健康检查全部通过；
- `preflight` 实际 `accepted: true`；
- 在 `survey_front` 第 3 次尝试失败：

```text
S1 passive primitive survey failed:
[Errno 111] Connection refused
```

我继续查这个 `survey` 原语实际连接的是 S1 哪个服务/端口，确认是导航服务、IMC 服务还是其他本机接口。到目前为止仍没有运动命令发出。

## 20. 助手（进展）

已定位到更具体的一层：`survey_front` 不是调用 FDP，而是调用配置里的 Workbin survey WebSocket。当前开发配置把 survey 指向：

```text
ws://10.34.216.13:8042
```

FDP 的 `7876` 只负责后续位姿服务，健康正常并不能代表 survey 的 `8042` 正常。我再从 S1 直接做 TCP 只读连通性检查，并核对 `8042/8040` 的客户端实现。

## 21. 助手

已经查清楚了：这次不是 FDP。

真正失败点是：

```text
blocked_reason:
StepFailed: step survey_front failed after 3 attempt(s):
S1 passive primitive survey failed: [Errno 111] Connection refused
```

我从 S1 做了端口检查：

```text
10.34.216.13:8042  Connection refused
10.34.216.13:8040  Connection refused
10.34.216.13:7876  CONNECTED
```

结论：

- FDP `7876` 正常；
- SAM3/LingBot/FDP 健康检查正常；
- `survey_front` 使用的是 Workbin survey WebSocket：
  `ws://10.34.216.13:8042`
- `8042` 服务没有监听，所以连续 3 次失败；
- `8040` 旧兼容端口也没有监听；
- 本次没有运动：`command_categories_sent: []`、`may_have_issued_motion: false`、`safe_pre_command_refusal: true`。

需要在 `10.34.216.13` 上恢复/启动 Workbin raw-surface bridge 的 `8042` 服务，或确认端口是否已经变更。当前启动脚本的健康门禁没有检查 `8042`，所以它先通过了健康门禁，直到 `survey_front` 才暴露问题。

服务恢复后先确认从 S1 能连通 `10.34.216.13:8042`，再考虑使用新的 `RUN_ID` 重试；不要复用这次的 `random_20260917_135836`。

## 22. 用户

8042 应该已经启动了，给我看看现在是什么新问题？

\_grasp\_close",
&#x20;       "secondary\_pregrasp",
&#x20;       "secondary\_safe\_clearance"
&#x20;     ],
&#x20;     "schema": "s1\_destack.source\_bound\_motion\_workflow\_readiness.v2",
&#x20;     "sdk\_initialized": false,
&#x20;     "threshold\_status": "conservative\_fail\_closed\_pending\_physical\_commissioning",
&#x20;     "uncommissioned\_operations": []
&#x20;   },
&#x20;   "startup\_semantic\_state\_readiness": {
&#x20;     "failure\_codes": [],
&#x20;     "motion\_commands\_sent": false,
&#x20;     "preflight\_minimum\_sample\_count": 3,
&#x20;     "preflight\_runtime\_probe\_required": true,
&#x20;     "provider\_available": true,
&#x20;     "provider\_readiness": {
&#x20;       "acceptance\_requires\_probe\_accepted": true,
&#x20;       "controller\_identity\_claimed": false,
&#x20;       "infers\_custody\_from\_gripper\_width": false,
&#x20;       "motion\_commands\_sent": false,
&#x20;       "operator\_no\_stop\_means\_empty": true,
&#x20;       "preflight\_probe\_required": true,
&#x20;       "provider\_available": true,
&#x20;       "ready": true,
&#x20;       "sample\_count": 3,
&#x20;       "schema": "s1\_destack.startup\_semantic\_state\_provider\_readiness.v1",
&#x20;       "sdk\_initialized": false,
&#x20;       "semantic\_state\_observed": false
&#x20;     },
&#x20;     "ready": true,
&#x20;     "reason": null,
&#x20;     "required": true,
&#x20;     "runtime\_acceptance\_condition": "preflight\_probe.accepted\_is\_true\_after\_three\_or\_more\_fresh\_samples",
&#x20;     "schema": "s1\_destack.startup\_semantic\_state\_readiness.v2",
&#x20;     "sdk\_initialized": false,
&#x20;     "semantic\_state\_observed": false
&#x20;   },
&#x20;   "unknown\_operations": [],
&#x20;   "unsupported\_operations": {}
&#x20; },
&#x20; "run\_dir": "/data/runs/random\_20260917\_140848",
&#x20; "run\_id": "random\_20260917\_140848",
&#x20; "schema": "s1\_destack.cli\_mission\_result.v1",
&#x20; "state": "BLOCKED",
&#x20; "success": false,
&#x20; "survey": {
&#x20;   "artifact": "sha256:a6738fbe2a4f779309ce2cc79ae3580e5bf5198b355f6bd6121e5ba525fe9588",
&#x20;   "candidate\_count": 4,
&#x20;   "crates\_visible": true,
&#x20;   "disposition": "target",
&#x20;   "reason\_code": null,
&#x20;   "request\_intrinsics\_used": true,
&#x20;   "request\_transform\_used": true,
&#x20;   "station": "front",
&#x20;   "successful": true,
&#x20;   "target": {
&#x20;     "center\_base\_link\_m": [
&#x20;       1.452272763106,
&#x20;       -0.164549433693,
&#x20;       0.578394463556
&#x20;     ],
&#x20;     "crate\_model": "4317",
&#x20;     "evidence\_artifact": "sha256:a6738fbe2a4f779309ce2cc79ae3580e5bf5198b355f6bd6121e5ba525fe9588",
&#x20;     "grasp\_mode": "10",
&#x20;     "height\_level": 4,
&#x20;     "identity\_key": "cfa5c50822c3a742c00b88aebeecb8f15d334027e537c6b2377ffcdc1274f977",
&#x20;     "station": "front"
&#x20;   }
&#x20; }
}
Mission exited (4); logs retained: /home/galbot/ie\_lab/data/s1\_continuous\_destack/monitoring/random\_20260917\_140848
galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack/deploy$&#x20;

## 23. 助手（进展）

我现在继续按只读 SSH 检查新 run `random_20260917_140848`：重点确认 `8042` 是否已恢复，以及 survey 成功后究竟在哪个后续步骤被阻断。不会启动任务或发送运动命令。

## 24. 助手（进展）

新 run 的准确阻断点已经变了：

```text
survey_front：成功
mode10_capture_primary：成功
mode10_lock_primary：连续 3 次失败
S1 passive primitive lock_primary_fdp failed: [Errno 111] Connection refused
```

当前从 S1 看 `8042` 已连通，说明 survey 服务恢复了；`7876` 现在也能建立 TCP 连接。我继续核对失败时 `lock_primary_fdp` 实际访问的 FDP URL/路径，并判断是不是 FDP 在任务中途短暂掉线后又恢复。

## 25. 助手

现在的新问题已经不是 8042：

- `8042` 已恢复：TCP `CONNECTED`，`survey_front` 成功并识别到目标。
- `7876` 当前也能连通，`/health` 返回 HTTP 200。
- 但本次运行在 `mode10_lock_primary` 失败：

```text
S1 passive primitive lock_primary_fdp failed:
[Errno 111] Connection refused
```

这个步骤实际调用的是 FDP：

```text
http://10.34.216.13:7876/first_frame_track
```

因此更像是 FDP 在任务运行期间短暂重启/掉线：启动前健康检查通过，但真正 POST 推理时端口拒绝连接；现在服务已经恢复。`8040` 旧兼容端口仍然未启动，但本次 raw-surface survey 用的是 `8042`，不是当前阻断点。

重要的是：本次前面的 `navigate_front`、`prepare_front_observation`、`mode10_prepare_fdp` 已经发送过运动命令；只有失败的 `lock_primary_fdp` 步骤本身没有发运动，记录为 `may_have_issued_motion: false`。不要直接重跑或恢复到“零位”，机器人可能停在 FDP 观测姿态。

下一步应查 `10.34.216.13` 上 FDP 服务在本次运行时间附近是否发生容器重启、OOM 或进程崩溃。单看 S1 目前只能确认：FDP 现在健康，但运行时曾短暂不可用。

## 26. 用户

```json
    "secondary_pregrasp",
    "secondary_safe_clearance"
  ],
  "schema": "s1_destack.source_bound_motion_workflow_readiness.v2",
  "sdk_initialized": false,
  "threshold_status": "conservative_fail_closed_pending_physical_commissioning",
  "uncommissioned_operations": []
},
"startup_semantic_state_readiness": {
  "failure_codes": [],
  "motion_commands_sent": false,
  "preflight_minimum_sample_count": 3,
  "preflight_runtime_probe_required": true,
  "provider_available": true,
  "provider_readiness": {
    "acceptance_requires_probe_accepted": true,
    "controller_identity_claimed": false,
    "infers_custody_from_gripper_width": false,
    "motion_commands_sent": false,
    "operator_no_stop_means_empty": true,
    "preflight_probe_required": true,
    "provider_available": true,
    "ready": true,
    "sample_count": 3,
    "schema": "s1_destack.startup_semantic_state_provider_readiness.v1",
    "sdk_initialized": false,
    "semantic_state_observed": false
  },
  "ready": true,
  "reason": null,
  "required": true,
  "runtime_acceptance_condition": "preflight_probe.accepted_is_true_after_three_or_more_fresh_samples",
  "schema": "s1_destack.startup_semantic_state_readiness.v2",
  "sdk_initialized": false,
  "semantic_state_observed": false
},
"unknown_operations": [],
"unsupported_operations": {}
```

},
"run\_dir": "/data/runs/random\_20260917\_141520",
"run\_id": "random\_20260917\_141520",
"schema": "s1\_destack.cli\_mission\_result.v1",
"state": "BLOCKED",
"success": false,
"survey": {
"artifact": "sha256:b0a74c25d7aa021a042877c716519003d052e873913e8499fb2cfcd2f211b180",
"candidate\_count": 4,
"crates\_visible": true,
"disposition": "target",
"reason\_code": null,
"request\_intrinsics\_used": true,
"request\_transform\_used": true,
"station": "front",
"successful": true,
"target": {
"center\_base\_link\_m": [
1.395149779415,
-0.16436185595,
0.579890162382
],
"crate\_model": "4317",
"evidence\_artifact": "sha256:b0a74c25d7aa021a042877c716519003d052e873913e8499fb2cfcd2f211b180",
"grasp\_mode": "10",
"height\_level": 4,
"identity\_key": "01503000bf805cdb4a3e60ce2f87515a1a0a875bc58aa718c35f3285d83dac01",
"station": "front"
}
}
}
Mission exited (4); logs retained: /home/galbot/ie\_lab/data/s1\_continuous\_destack/monitoring/random\_20260917\_141520
galbot\@galbot-echo:\~/ie\_lab/projects/s1\_continuous\_destack/deploy$



我现在重新运行了一下，看看这是什么新问题？

## 27. 助手（进展）

我直接检查新 run `random_20260917_141520` 的完整日志和失败步骤，重点看 `blocked_reason` 以及它是否已经越过 `mode10_lock_primary`。只读检查，不重复运行任务。

## 28. 助手

我查了新 run `random_20260917_141520`，主问题仍然是 FDP，不是 8042：

```text
blocked_reason:
step mode10_lock_primary failed after 3 attempts

S1 passive primitive lock_primary_fdp failed:
[Errno 111] Connection refused
```

当前状态：

- `8042`：已连通，survey 最终成功；
- survey 找到 4 个候选目标；
- `mode10_capture_primary`：成功；
- `7876` 当前可连通，但任务实际调用 FDP `first_frame_track` 时仍被拒绝；
- 说明 FDP 的健康接口可用，但实际推理服务在运行期间发生了短暂掉线、重启或 worker 崩溃。

另外，第一次 `survey_front` 还有一个次要问题：

```text
FDP_CAPTURE_SOURCE_NOT_STATIC: before state is not settled
```

但第二次 survey 已成功，所以不是这次最终阻断原因。

注意：本次 `navigate_front`、`prepare_front_observation`、`mode10_prepare_fdp` 已经执行过运动；失败的 `lock_primary_fdp` 步骤本身没有发送运动。不要直接盲目重跑。

要查清 FDP 为什么运行中拒绝连接，需要查看 `10.34.216.13` 上 FDP 服务在 14:15 左右的容器重启、OOM 或进程日志；S1 侧已经确认请求端点和故障步骤。
