# 连接 G1 主机查看内容

- 归档编号：`01a06714-2ee4-73f2-846e-bccb42390fad`
- 项目：VLA G1 项目
- 创建时间：2026-09-03T19:42:51.111000+08:00
- 更新时间：2026-09-04T16:45:56.708000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

ssh galbot@10.34.216.136
这个是G1的链接，你需要我给你啥？然后你就可以看到里面的内容来？

## 2. 助手

已有用户名和 IP。要让我查看 G1 里的内容，还需要：

- SSH 认证：最好是已配置好的公钥或 SSH Agent。
- 如果必须经过 VPN、跳板机或特定端口，请提供连接方式。
- 告诉我需要查看的目录或任务范围，例如项目代码、日志或运行状态。

不要把密码或私钥发到聊天里。最简单的是：如果你这台机器现在已经能直接运行 `ssh galbot@10.34.216.136` 登录，就回复我“连接看看”，我可以先尝试只读检查；若认证失败，我再根据报错告诉你如何配置公钥。

## 3. 用户

umask 077
ssh -fN -M -S /tmp/s1_codex_mux_20260903_fdp \
  -o ControlPersist=2h \
  galbot@10.34.216.17
galbot@10.34.216.17's password:

[REDACTED]

## 4. 助手

对，就是这种方式。请在你本机终端运行下面这段，密码只在终端里输入，不要发给我：

```bash
umask 077
ssh -fN -M \
  -S /tmp/g1_codex_mux_20260903_k7m2 \
  -o ControlPersist=2h \
  -o ServerAliveInterval=30 \
  -o ServerAliveCountMax=3 \
  galbot@10.34.216.136
```

输入密码并成功返回命令行后，可以检查连接：

```bash
ssh -S /tmp/g1_codex_mux_20260903_k7m2 \
  -O check \
  galbot@10.34.216.136
```

看到 `Master running` 后告诉我“已连接”，我就可以通过这个连接查看 G1。

## 5. 用户

lsy03@lsy03-System-Product-Name:~$ umask 077
ssh -fN -M \
  -S /tmp/g1_codex_mux_20260903_k7m2 \
  -o ControlPersist=2h \
  -o ServerAliveInterval=30 \
  -o ServerAliveCountMax=3 \
  galbot@10.34.216.136
galbot@10.34.216.136's password:
[REDACTED]

## 6. 助手（进展）

连接看起来已经建立。我先通过这个复用通道做只读检查，确认主机、当前目录和顶层文件。

## 7. 助手（进展）

连接本身没有报错；刚才是执行环境阻止访问你在 `/tmp` 创建的 SSH 控制套接字。我申请一次权限，通过现有通道进行只读查看。

## 8. 助手

连接成功，我现在可以通过该通道访问 G1。

当前信息：

- 主机名：`galbot-echo`
- 用户目录：`/home/galbot`
- 可能相关的项目目录：`GalbotSDK`、`workspace`、`Project`、`slam_ws`、`Galbot_slam`
- 还有 `galbot_g1_sdk.zip`、日志目录和传感器工具等

告诉我接下来需要查看、运行或修改什么即可。当前连接会保持约 2 小时。

## 9. 用户

# Files pasted by the user:

## "Skip to main content Galbot Logo 产品体系 开发助手 训练营 中文 English 目录 Galbot SDK G1 产品概述…": /home/lsy03/.codex/attachments/e377622b-40f8-47b6-b57a-2642ba7e70b6/pasted-text.txt

## "Skip to main content Galbot Logo 产品体系 开发助手 训练营 中文 English 目录 Galbot SDK G1 产品概述…": /home/lsy03/.codex/attachments/ecde0bc2-fad9-4ab5-90ac-3e7babdd1a3a/pasted-text.txt

## My request:
能不能给我生成 立即测试应用的控制机械臂的实验脚本？ 先给我计划，不要行动

## 10. 助手（进展）

我只会读取你附上的两份说明，整理实验方案；不会连接 G1、创建脚本、启动节点或让机械臂运动。计划里会把安全检查和分阶段验收放在前面。

## 11. 助手

可以。根据文档，我建议先做“单臂微动作闭环测试”，使用 Python SDK 的 `get_joint_positions()` 和 `set_joint_positions()`。

计划如下：

1. 只读检查 G1 环境
   确认实际 SDK 版本、Python 环境、官方示例位置、机械臂控制服务和控制器状态。

2. 确定最小实验范围
   默认测试左臂，仅控制 7 自由度机械臂；不控制底盘、腿部、头部、另一只手臂或夹爪。

3. 生成安全实验脚本
   脚本支持：

   - `--arm left/right`
   - 默认 `--dry-run`，只读取状态
   - 必须显式添加 `--execute` 才能运动
   - 运行时再次要求人工确认
   - 自动读取关节名称和当前位置，不硬编码关节顺序
   - 检查关节数量、限位、控制器状态和目标合法性
   - 低速阻塞执行，建议初始速度不超过 `0.05 rad/s`
   - 仅对一个经检查的关节做不超过约 `0.02 rad` 的微小偏移
   - 记录原始位置、目标位置、返回状态及反馈误差
   - 正常完成后返回原始姿态
   - 异常时停止发送运动命令，释放脚本取得的控制资源并销毁 SDK

4. 无运动验证
   依次验证语法、SDK 导入、初始化、关节组发现、当前位置读取和 dry-run 输出，全程不发送运动指令。

5. 现场分级实验
   现场人员确认周围约 2 米无遮挡、机器人状态稳定并随时可以操作急停后：

   - 第一阶段：发送“保持当前位置”命令，验证控制链路
   - 第二阶段：执行单关节微小偏移
   - 第三阶段：读取实际关节反馈
   - 第四阶段：低速返回原始姿态

   每一步都需要成功后才进入下一步，不自动重试运动失败。

6. 交付内容
   提供一个 Python 实验脚本、运行命令、dry-run 示例、紧急停止方法、验收标准和常见报错说明。

目前我没有连接 G1、创建文件或发送控制命令。你确认“按计划执行”后，我再从只读环境检查开始。

## 12. 用户

脚本是否需要放在 G1 上面？ 你能确定G1 python脚本的运行环境是什么吗？

## 13. 助手（进展）

脚本大概率应放在 G1 上运行，但“具体用哪个 Python、需要 source 哪个环境”不能只靠示例代码猜。我先只查你附带文档中的安装与运行环境说明，不访问或操作机器人。

## 14. 助手

建议脚本放在 G1 上并在 G1 内运行。因为文档中的 `galbot_sdk.g1` 依赖本机机器人通信环境，以及 `/data/galbot/bin/` 下的运动规划、状态发布等服务。本地电脑主要负责 SSH 登录和下发命令。

目前能从文档确认：

- SDK 文档版本标记为 `1.9.1`，但不代表 G1 实际安装版本。
- Python 包名为 `galbot_sdk.g1`。
- 机械臂规划需要 `service_motion_plan`、`robot_state_publish` 等服务。
- 官方示例位于 `examples/python/tutorials/`。

目前不能确定：

- G1 实际使用的 Python 版本。
- 使用系统 Python、Conda、venv，还是启动脚本配置的环境。
- `galbot_sdk` 的实际安装位置和版本。
- 是否需要先执行某个 `source` 脚本。
- 哪些机器人服务当前已经启动。

这些可以通过一次完全只读的 SSH 检查确定，包括查询 Python 路径、版本、SDK 模块位置、环境变量、官方示例和服务进程。检查过程中不会调用 `robot.init()`，也不会取得控制器或发送运动指令。

查清后，我会给出类似下面这种确定的运行方式，而不是猜测：

```bash
cd /home/galbot/workspace/g1_arm_test
source <实际环境脚本>
<实际Python解释器> arm_smoke_test.py --dry-run
```

因此下一步应先做“只读运行环境检查”，再决定脚本放置目录和启动命令。目前我还没有访问 G1 做这项检查。

## 15. 用户

现在进入G1的SSh链接还能用吗？
lsy03@lsy03-System-Product-Name:~/文档/ChatGPT/S1/experiments/20260822_s1_rviz_d
emo/ros2_ws$ ssh -S /tmp/g1_codex_mux_20260903_k7m2 \
  -O check \
  galbot@10.34.216.136
Control socket connect(/tmp/g1_codex_mux_20260903_k7m2): No such file or directory
lsy03@lsy03-System-Product-Name:~/文档/ChatGPT/S1/experiments/20260822_s1_rviz_d
emo/ros2_ws$

## 16. 助手

不能用了。`No such file or directory` 表示 SSH 控制套接字已经消失，通常是之前设置的 `ControlPersist=2h` 到期，并不代表 G1 离线。

重新建立一个今天的新连接：

```bash
umask 077
ssh -fN -M \
  -S /tmp/g1_codex_mux_20260904_r8p4 \
  -o ControlPersist=8h \
  -o ServerAliveInterval=30 \
  -o ServerAliveCountMax=3 \
  galbot@10.34.216.136
```

输入密码后检查：

```bash
ssh -S /tmp/g1_codex_mux_20260904_r8p4 \
  -O check \
  galbot@10.34.216.136
```

看到 `Master running` 就表示成功。密码仍然只在终端输入，不要发到聊天中。之后告诉我“已重新连接”。

## 17. 用户

lsy03@lsy03-System-Product-Name:~$ ssh -fN -M   -S /tmp/g1_codex_mux_20260904_r8p4   -o ControlPersist=8h   -o ServerAliveInterval=30   -o ServerAliveCountMax=3   galbot@10.34.216.136
galbot@10.34.216.136's password:
[REDACTED]

## 18. 助手（进展）

密码输入后已经正常返回，连接大概率建立成功。我只检查复用通道状态，不执行任何 G1 命令。

## 19. 助手

连接已确认可用：

```text
Master running (pid=37468)
```

我现在可以通过 SSH 访问 G1。尚未执行任何 G1 命令；你确认后，我可以先进行只读的 Python/SDK 运行环境检查。

## 20. 用户

等一下，我们现在是在干啥来着？

## 21. 助手

我们要做的是：为 G1 生成一个可立即测试的 Python 机械臂控制实验脚本。

目前进度：

- 已阅读你提供的 G1 SDK 文档。
- 已制定“单臂、单关节、低速微动作、可返回原位”的安全测试计划。
- 已建议脚本放在 G1 上运行。
- 刚重新建立并确认了 G1 的 SSH 连接。
- 还没有检查 Python/SDK 环境、创建脚本或控制机械臂运动。

下一步原计划是：先在 G1 上进行只读环境检查，确定实际 Python、SDK 版本、环境激活方式和服务状态，然后再生成脚本。任何机械臂运动都需要你之后明确确认。

## 22. 用户

好的行动

## 23. 助手（进展）

开始执行。第一阶段只做 G1 环境盘点和脚本构建验证，不发送任何机械臂运动命令；我会在真正运动前停下来，让你确认现场急停与净空条件。

## 24. 助手（进展）

我会并行核对系统、Python、SDK 模块、官方示例路径和后台服务。这里最多只做模块导入，不调用 `GalbotRobot.init()`。

## 25. 助手（进展）

首轮结果已经定位到关键点：G1 是 Ubuntu 20.04/aarch64、系统 Python 3.8.10；普通非交互 SSH 不会自动得到 SDK 路径，因此直接运行会报 `No module named galbot_sdk`。SDK 实际位于 `/opt/galbot/...`，相关运动服务当前已启动。我继续核对官方 `setup.sh`、已安装模块和远端示例，确定唯一正确的启动方式。

## 26. 助手（进展）

已确认真正运行环境应使用 `/usr/bin/python3`（3.8.10）并显式加载 Galbot SDK 1.9.0；交互 shell 当前优先使用 `/data/galbot/lib` 的机器人发布版。接下来我核对 API 签名与关节限位来源，随后生成默认只读、必须二次确认才允许运动的脚本。

## 27. 助手（进展）

我会把产物做成一个独立实验目录：主脚本、环境启动器、说明文档和纯软件测试。默认运行只初始化并读取状态；`--execute` 仍要求现场终端输入精确确认语句。

## 28. 助手（进展）

本地静态编译通过，单元测试加载器发现了一个测试夹具问题（动态导入模块时未先注册到 `sys.modules`），不是机械臂脚本逻辑错误。我先修正测试夹具再继续。

## 29. 助手（进展）

纯软件测试已通过，并额外验证了默认 dry-run 的执行路径对运动 API 的调用次数严格为 0。目标远端目录也确认不存在，因此不会覆盖现有文件；我正在做最后一轮安全审查。

## 30. 助手（进展）

安全审查未发现阻止上传的问题。我现在只在 G1 新建独立目录并复制脚本；随后先做 Python 3.8 编译、帮助页和单元测试，仍不初始化机器人。

## 31. 助手（进展）

G1 的 Python 3.8 编译和 6 个测试都通过；启动器验证发现官方 `setup.sh` 会覆盖通用变量名 `SCRIPT_DIR`，导致帮助页寻找了错误路径。这是环境启动器问题，我正在改用不会冲突的变量名并重新上传验证。

## 32. 助手（进展）

启动器现已在 G1 上通过帮助页验证。下一步运行默认 dry-run：它会初始化 SDK、连续读取左臂两次、检查稳定性和 URDF 限位，但代码路径已测试保证不会调用 `set_joint_positions()`。

## 33. 助手（进展）

真实 dry-run 成功，SDK 1.9.0、关节顺序、当前姿态和状态链路都已验证，且没有发送运动命令。它还发现左臂第 5/6 关节非常接近硬限位；因此我会进一步收紧脚本，只向选中的单个关节名发命令，避免把整条机械臂的近限位读数重新下发。

## 34. 助手（进展）

独立审查又指出两项必须在真机前补齐：反馈需要验证时间戳确实更新，且反馈容差必须显著小于 1°测试幅度，避免“实际没动也判成功”。我正在读取 SDK 的 `JointState` 字段和停止控制器接口定义；仍不会运动。

## 35. 助手（进展）

加固完成：现在会要求连续递增的 `JointState.timestamp_ns`、静止速度、单实例锁、SDK/配置白名单、控制器名称校验、最小 3°限位余量，并在运动异常时调用机械臂 `stop_controller()`。本地 9 项测试通过，准备在 G1 的 Python 3.8 上复验。

## 36. 助手（进展）

最终 dry-run 已在真机通过：状态帧确实更新、速度为零、SDK/配置/控制器匹配，并再次确认没有运动命令。最后我会验证一个防误触场景：从非交互 SSH 传入 `--execute` 必须在任何运动 API 前拒绝执行。

## 37. 助手（进展）

二次审查确认 `JointState.joint_name` 在网页文档里不明显，但真机 dry-run 已实际验证该字段存在。剩余关键问题我继续处理：将“静止”改为多帧窗口、回程前重新核验姿态、改成机器人级全局锁，并核对 SDK 明确承诺“停止后保持当前位置”的轨迹停止接口。

## 38. 助手（进展）

已确认 G1 的关节时间戳是 Unix 纳秒时间，距本机时钟约 40 ms、七轴同帧同步。现在的最终候选会拒绝超过 1 秒的旧数据，并要求 4 个连续新帧覆盖至少 0.3 秒；本地 14 项测试已通过。

## 39. 助手（进展）

最终独立审查已通过，没有剩余代码级阻断项。我同步最后两个文案修正，并再跑一次 G1 默认 dry-run；之后会停在现场运动确认门前。

## 40. 助手

脚本已生成并部署到 G1：

- G1 目录：`/home/galbot/g1_arm_smoke_test_20260904`
- [本地主脚本](</home/lsy03/文档/ChatGPT/VLA G1 项目/g1_arm_smoke_test/g1_arm_smoke_test.py>)
- [运行说明](</home/lsy03/文档/ChatGPT/VLA G1 项目/g1_arm_smoke_test/README.md>)

验证结果：

- G1 Python 3.8 编译通过
- 14 项软件测试通过
- 最终 dry-run 通过，退出码 `0`
- SDK 1.9.0、控制器、URDF、反馈时间戳均正常
- 尚未让机械臂运动

注意：当前左臂第 5/6 关节接近硬限位，脚本只会显式控制第 7 关节，不会向它们发送目标。

现场确认急停可触达、周围 2 米无人与障碍物、没有其他关节轨迹运行后，可在你的终端运行：

```bash
ssh -t \
  -S /tmp/g1_codex_mux_20260904_r8p4 \
  galbot@10.34.216.136 \
  'cd /home/galbot/g1_arm_smoke_test_20260904 &&
   ./run_g1_arm_smoke_test.sh \
     --execute \
     --arm left \
     --joint-index 7 \
     --delta-deg 1.0 \
     --speed-rad-s 0.03'
```

随后必须人工输入：

```text
MOVE LEFT_ARM_JOINT7 +1.000 DEG
```

异常运动应立即使用物理急停，不能依赖 `Ctrl+C`。
