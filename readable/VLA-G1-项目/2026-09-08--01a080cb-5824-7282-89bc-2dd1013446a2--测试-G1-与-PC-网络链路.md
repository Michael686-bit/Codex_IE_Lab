# 测试 G1 与 PC 网络链路

- 归档编号：`01a080cb-5824-7282-89bc-2dd1013446a2`
- 项目：VLA G1 项目
- 创建时间：2026-09-08T19:33:25.156000+08:00
- 更新时间：2026-09-08T20:52:17.477000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

当前侧会话无法访问你主机的真实网卡和 SSH socket，所以不能直接替你完成网络实测。请在 PC 终端执行：
```css
ip -br addr show eno1
ping -c 3 -W 1 -I eno1 192.168.1.66
ping -c 3 -W 1 -I eno1 192.168.1.88
```

再测试 G1 到 PC 的反向链路：
```bash
ssh -S /tmp/g1_codex_mux_20260907 \
  -o BatchMode=yes \
  galbot@10.34.216.136 \
  'ip route get 192.168.1.99; ping -c 3 -W 1 -I eth1 192.168.1.99'
```

双向 Ping 都通过后，在 G1 上重新运行 dry-run：
```css
cd /home/galbot/g1_arm_smoke_test_20260904

./run_g1_arm_smoke_test.sh \
  --arm left \
  --joint-index 1 \
  --delta-deg -0.5 \
  --speed-rad-s 0.03
```

暂时不要添加 `--execute`。把输出发来即可判断网线恢复后关节状态是否回来。

帮我测一下

## 2. 助手（进展）

我先在当前 PC 终端检查 `eno1` 的地址，再分别测试到两台 G1 地址的链路；如果本侧沙箱仍拦截原始网卡访问，我会把具体失败点返回。

## 3. 助手（进展）

当前执行被沙箱拒绝（`Cannot open netlink socket: Operation not permitted`），这是权限限制而不是网卡结果。我申请一次受控的终端权限来执行这些只读网络测试。

## 4. 助手（进展）

PC 侧结果正常：`eno1` 为 `192.168.1.99/24`，到 `192.168.1.66` 和 `192.168.1.88` 都是 3/3、0% 丢包。现在继续测 G1 到 PC 的反向链路，并在通过后执行指定的 dry-run。

## 5. 助手（进展）

反向链路也通过：G1 的 `eth1` 为 `192.168.1.88`，到 PC `192.168.1.99` 为 3/3、0% 丢包。dry-run 脚本仍在 G1 上运行，我正在等它输出完成。

## 6. 助手（进展）

dry-run 已完成且没有发送运动命令。输出目前只确认了 SDK/URDF 初始化；我再读取这次生成的日志，确认是否还有关节状态行，避免漏掉关键结果。

## 7. 助手

实测结果：

- PC `eno1`：`UP 192.168.1.99/24`
- PC → `192.168.1.66`：3/3，0% 丢包
- PC → `192.168.1.88`：3/3，0% 丢包
- G1 → PC：经 `eth1`、源地址 `192.168.1.88`，3/3，0% 丢包

dry-run 已执行，未使用 `--execute`。但关节状态没有恢复：

```text
模式：DRY-RUN（无运动命令）
初始化 GalbotRobot；此步骤尚未发送运动命令
安全中止：等待 left_arm 状态超时：关节名数量为 0，预期 7
清理：request_shutdown 完成
清理：wait_for_shutdown 完成
清理：destroy 完成
```

结论：网线/IP 双向链路正常，但 G1 的 `left_arm` 状态流仍未返回，当前不要执行实际运动。日志位于：

`/home/galbot/g1_arm_smoke_test_20260904/logs/g1_arm_smoke_test_20260908_193446.log`

## 8. 用户

给我测一下 right\_arm

## 9. 助手（进展）

我按刚才相同参数测试 `right_arm`：关节索引 1、`-0.5°`、速度 `0.03 rad/s`，继续只跑 dry-run，不加 `--execute`。

## 10. 助手（进展）

`right_arm` dry-run 仍在等待状态数据，我正在等脚本结束并获取最终日志。

## 11. 助手（进展）

`right_arm` 的标准输出只到初始化，日志里暂时没有“状态收到”或超时/清理行；我检查一下该进程和日志是否仍在更新，确认它是成功结束还是卡在等待状态。

## 12. 助手

`right_arm` 已按相同参数完成 dry-run，未使用 `--execute`。

结果：

```text
安全中止：等待 right_arm 状态超时：关节名数量为 0，预期 7
清理：request_shutdown 完成
清理：wait_for_shutdown 完成
清理：destroy 完成
```

结论：`right_arm` 关节状态同样没有回来；网络双向 Ping 正常，但 SDK 未收到 7 个关节状态。日志：

`/home/galbot/g1_arm_smoke_test_20260904/logs/g1_arm_smoke_test_20260908_193756.log`

## 13. 用户

现在我们是不是有本地的docker ，我们使用docker里面的SDK进行控制如何？



“ [Skip to main content](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment#__docusaurus_skipToContent_fallback)

[产品体系](https://developer.galbot.com/products)[开发助手](https://developer.galbot.com/platform)[训练营](https://developer.galbot.com/training)

**中文**English

- **目录**
- [**Galbot SDK**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment#)
  - [**G1**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/)
    - [**产品概述**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/overview)
    - [**安装与配置**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_and_configuration)
      - [**系统要求**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_system_requirements)
      - [**环境部署**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_and_configuration)
        - [**机器人端部署**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_robot_deployment)
        - [**PC 端部署**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_pc_deployment)
        - [**Docker 部署**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment)
      - [**版本兼容性检查**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_version_compatibility)
      - [**术语说明**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_terminology)
    - [**入门教程**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/tutorials)
    - [**Python 示例**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/examples_python)
    - [**C++ 示例**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/examples_cpp)
    - [**Python API 参考 - G1 机器**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/api_python_reference)
    - [**C++ API 参考 - G1 机器人**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/api_cpp_reference)
    - [**常规操作**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/routine_operations)
    - [**故障排除**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/troubleshooting)
    - [**Galbot SDK 许可证**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/license)
  - [**S1**](https://developer.galbot.com/docs/SDK/1.9.1/s1/zh/)
- [**Galbot G1 通用人形机器人**](https://developer.galbot.com/docs/g1/2.2.4/zh/g1)
- [**Galbot S1 工业重载机器人**](https://developer.galbot.com/docs/s1/2.0.0/zh/s1)
- [**遥操作主从臂**](https://developer.galbot.com/docs/tm01/1.4.0/zh/tm01)

*
* Galbot SDK
* [G1](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/)
* [安装与配置](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_and_configuration)
* [环境部署](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_and_configuration)
* Docker 部署

**latest(1.9.1)**

[**机器人端部署**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_robot_deployment)[程序运行在机器人 Orin 上，适合生产环境与实时性要求高的应用。](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_robot_deployment)[**PC端部署**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_pc_deployment)[程序运行在 PC 上，支持 Ubuntu、WSL2、VMWare 开发调试环境。](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_pc_deployment)[**Docker部署**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment)[SDK 与运行依赖放在容器内，适合需要隔离环境的场景。](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment)

# Docker部署

**适用场景：** 需要与宿主机隔离的环境

## 1. 环境配置

- 需安装 **Docker** 与 **Docker Compose**
- 可参考 **PC端部署** 中第一、第二章节，完成网线连接、修改机器人`embosa_ip_config.json` 等配置

## 2. 获取镜像

示例：
```bash
docker pull galbot/galbot_sdk:1.8.0
```

可根据实际需要版本，替换galbot\_sdk:1.8.0中版本号

## 3. 启动容器

进入SDK产出中docker路径，执行run脚本即可启动镜像
```bash
cd GalbotSDK/docker
./run.sh
```

容器内已安装好SDK动态库与运行环境

---

[**上一章**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_pc_vmware)

[**VMware**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_pc_vmware)

[**下一章**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_version_compatibility)

[**版本兼容性检查**](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_version_compatibility)

- [1. 环境配置](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment#1-%E7%8E%AF%E5%A2%83%E9%85%8D%E7%BD%AE)
- [2. 获取镜像](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment#2-%E8%8E%B7%E5%8F%96%E9%95%9C%E5%83%8F)
- [3. 启动容器](https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/installation_docker_deployment#3-%E5%90%AF%E5%8A%A8%E5%AE%B9%E5%99%A8)

© 2026 Galbot · developer.galbot.com

联系支持[隐私政策](https://developer.galbot.com/privacy)[服务条款](https://developer.galbot.com/terms)” 你还记得之前确实是按照这个来部署的是吗

## 14. 助手（进展）

我先核对当前 PC 的实际状态：Docker 是否安装/运行、是否已有 Galbot SDK 镜像或容器，以及工作区里是否留有之前的 Docker 部署文件。这样可以区分“以前按文档部署过”和“现在容器仍可直接使用”。

## 15. 助手（进展）

Docker 客户端已安装（29.1.3），但当前普通权限无法访问 `/var/run/docker.sock`。我申请读取 Docker 服务状态、镜像和容器列表；这些检查不会修改容器或镜像。

## 16. 助手（进展）

本机确实有 Docker，且当前运行的 `vla-g1-teleop:lerobot-0.4.4` 使用 `host` 网络；更关键的是，PC 端 Galbot SDK 已从项目 `.vendor/galbot-pc-sdk` 以只读方式挂载到容器 `/opt/galbot`。我再在容器内核对 SDK 版本和导入情况。

## 17. 助手

可以，当前 PC 确实有本地 Docker，而且已经挂载了 PC 端 Galbot SDK：

- Docker 服务：29.1.3，正常运行
- 当前容器：`vla-g1-teleop:lerobot-0.4.4`
- 网络模式：`host`，可直接访问 `192.168.1.88`
- PC SDK：`1.9.1`，挂载到容器 `/opt/galbot`
- SDK 导入已验证成功

但之前不是完全照抄官方示例，而是按同一思路做的定制部署：自定义 LeRobot 容器 + 只读挂载 x86_64 PC SDK，并不是官方的 `galbot/galbot_sdk:1.8.0` 镜像。记录见 [docker/README.md](/home/lsy03/文档/ChatGPT/VLA%20G1%20项目/docker/README.md) 和 [BASELINE_20260908.md](/home/lsy03/文档/ChatGPT/VLA%20G1%20项目/docker/BASELINE_20260908.md)。

可以改用 Docker 内的 PC SDK 继续排查，但要注意：

- 当前 `g1_arm_smoke_test.sh` 使用的是机器人端 `aarch64` SDK，不能直接在 PC 容器里运行。
- Docker 内要加载 `/opt/galbot/galbot_sdk/linux-x86_64-gcc940/setup.sh`。
- 目前容器代码只做过只读状态验证，没有实际运动控制入口。
- 之前 PC SDK 1.9.1 的只读基线曾成功读取 G1 左臂状态；当前应先在容器内重新测试左右臂状态，再考虑运动控制。

所以方向是可行的，而且很可能能绕开机器人端 SDK 当前“关节名数量为 0”的问题；但暂时仍不应发送实际运动命令。

[附件：21a7e32b1fa6abd2--README.md](../../assets/21a7e32b1fa6abd2--README.md)

[附件：abdb0442884e954d--BASELINE_20260908.md](../../assets/abdb0442884e954d--BASELINE_20260908.md)

## 18. 用户

先在容器内重新测试左右臂状态

## 19. 助手（进展）

我现在在现有 `vla-g1-teleop` 容器内加载 x86_64 PC SDK，初始化 `GalbotRobot` 后只调用两侧的关节名、关节状态和活动控制器读取接口；不调用任何运动 API。测试完成后会主动执行 SDK 清理。

## 20. 助手（进展）

容器内的只读探测已经启动，SDK 初始化可能需要等待 G1 状态服务；目前没有运动命令输出。我继续等待最终的左右臂关节数据。

## 21. 助手（进展）

上一次探测没有返回可见输出，我先确认容器执行通道本身是否正常，再重新跑探测；这不会触碰机器人控制接口。

## 22. 助手（进展）

容器执行通道正常，但 PC SDK 探测卡在 `GalbotRobot.init()`，只打印了 `probe_start`，没有进入左右臂读取；这不是关节数据为空的结论。我检查容器里的 SDK 配置与残留进程后，再按正确的 PC 端配置重试。

## 23. 助手（进展）

日志已经暴露出关键点：容器内 SDK 能发现 `galbot-xcu` 的 `singorix/wbcs/sensor`，但初始化等待 `target_server` 和 `controller_server` 各约 20 秒，说明不是简单的 IP 不通，而是 G1 的 WBC 服务没有完成匹配/响应。我用完整 50 秒窗口重新获取 `init()` 的明确返回值。

## 24. 助手（进展）

当前容器的日志显示它能发现 `singorix/wbcs/sensor`，但初始化还卡在 WBC RPC 服务；为避免改动现有容器，我将用同一镜像临时启动一个只读测试容器，补上官方部署常见的 `host IPC`，再测左右臂状态。测试容器退出后自动删除，不发送运动命令。

## 25. 助手

容器内双臂状态测试未能进入读取阶段。

结果：

```text
Galbot SDK：1.9.1，导入成功
GalbotRobot.init()：超过 50 秒未返回
```

SDK 日志显示：

```text
singorix/wbcs/sensor：已匹配 galbot-xcu，但接收帧数 0
target_server：20 秒连接超时
controller_server：20 秒连接超时
```

因此目前无法确认左右臂的关节状态——不是读到了 0 个关节，而是 PC SDK 初始化未完成。整个过程没有调用任何运动 API。

网络配置本身正确，PC `192.168.1.99`、XCU `192.168.1.66`、HPU `192.168.1.88` 均可 Ping 通。下一步应先解决 PC SDK 与 G1 WBC 服务的 DDS/RPC 匹配问题。

## 26. 用户

我现在已经重启了一下，我们现在重新核实一下，给我命令我来执行吧

## 27. 助手

可以。请在 PC 终端按顺序执行，整个过程只读，不会发送运动命令。

1. 核实网卡和双向网络：

```bash
ip -br addr show eno1
ping -c 3 -W 1 -I eno1 192.168.1.66
ping -c 3 -W 1 -I eno1 192.168.1.88
```

2. 核实 Docker 和镜像：

```bash
docker info --format 'Docker={{.ServerVersion}} OS={{.OperatingSystem}}'
docker image ls
docker ps -a
```

3. 在 Docker 内测试左右臂状态：

```bash
cd "/home/lsy03/文档/ChatGPT/VLA G1 项目"

timeout 75s docker run --rm -i \
  --network host \
  --ipc host \
  --cap-drop ALL \
  --cap-add NET_RAW \
  --env GALBOT_CONFIG_DIR=/data/config \
  --env PYTHONPATH=/opt/galbot/galbot_sdk/linux-x86_64-gcc940/lib/python:/workspace \
  --env LD_LIBRARY_PATH=/opt/galbot/galbot_sdk/linux-x86_64-gcc940/lib \
  --volume "$PWD/.vendor/galbot-pc-sdk:/opt/galbot:ro" \
  --volume "$PWD/.vendor/galbot-pc-sdk/config:/data/config:ro" \
  --volume "$PWD:/workspace" \
  --workdir /workspace \
  --entrypoint /opt/vla-venv/bin/python3 \
  vla-g1-teleop:lerobot-0.4.4 -u - <<'PY'
import galbot_sdk
from galbot_sdk.g1 import GalbotRobot

robot = GalbotRobot()
initialized = False

try:
    print("probe_start", flush=True)
    print("sdk_version=", getattr(galbot_sdk, "__version__", "unknown"), flush=True)

    initialized = bool(robot.init())
    print("init=", initialized, flush=True)
    print("is_running=", bool(robot.is_running()), flush=True)

    if not initialized:
        raise RuntimeError("GalbotRobot.init() returned false")

    print("device=", robot.get_device_information(), flush=True)

    for arm in ("left_arm", "right_arm"):
        names = list(robot.get_joint_names(True, [arm]))
        states = list(robot.get_joint_states([], names)) if names else []

        print(f"{arm} joint_names={names}", flush=True)
        print(f"{arm} state_count={len(states)}", flush=True)
        print(
            f"{arm} states=",
            [
                {
                    "name": str(s.joint_name),
                    "position_rad": float(s.position),
                    "velocity_rad_s": float(s.velocity),
                    "timestamp_ns": int(s.timestamp_ns),
                }
                for s in states
            ],
            flush=True,
        )
        print(
            f"{arm} active_controller={robot.get_active_controller(arm)}",
            flush=True,
        )

finally:
    if initialized:
        robot.request_shutdown()
        robot.wait_for_shutdown()
        robot.destroy()
        print("cleanup=ok", flush=True)
PY
```

正常情况下应看到：

```text
init=True
left_arm state_count=7
right_arm state_count=7
cleanup=ok
```

请把第 1 步和第 3 步的完整输出发给我。不要运行 `--execute`、`set_joint_positions` 或其他运动接口。

## 28. 用户

lsy03\@lsy03-System-Product-Name:\~$ ip -br addr show eno1
ping -c 3 -W 1 -I eno1 192.168.1.66
ping -c 3 -W 1 -I eno1 192.168.1.88
eno1             UP             192.168.1.99/24 fe80::ea45:ad6d:995a:bf90/64&#x20;
PING 192.168.1.66 (192.168.1.66) from 192.168.1.99 eno1: 56(84) bytes of data.
64 bytes from 192.168.1.66: icmp\_seq=1 ttl=64 time=0.398 ms
64 bytes from 192.168.1.66: icmp\_seq=2 ttl=64 time=0.359 ms
64 bytes from 192.168.1.66: icmp\_seq=3 ttl=64 time=0.352 ms

\--- 192.168.1.66 ping statistics ---
3 packets transmitted, 3 received, 0% packet loss, time 2028ms
rtt min/avg/max/mdev = 0.352/0.369/0.398/0.020 ms
PING 192.168.1.88 (192.168.1.88) from 192.168.1.99 eno1: 56(84) bytes of data.
64 bytes from 192.168.1.88: icmp\_seq=1 ttl=64 time=0.271 ms
64 bytes from 192.168.1.88: icmp\_seq=2 ttl=64 time=0.159 ms
64 bytes from 192.168.1.88: icmp\_seq=3 ttl=64 time=0.154 ms

\--- 192.168.1.88 ping statistics ---
3 packets transmitted, 3 received, 0% packet loss, time 2044ms
rtt min/avg/max/mdev = 0.154/0.194/0.271/0.054 ms
lsy03\@lsy03-System-Product-Name:\~$ docker info --format 'Docker={{.ServerVersion}} OS={{.OperatingSystem}}'
docker image ls
docker ps -a
Docker=29.1.3 OS=Ubuntu 22.04.4 LTS
&#x20;                                                          i Info →   U  In Use
IMAGE                         ID             DISK USAGE   CONTENT SIZE   EXTRA
synthnova-runtime:20260801    713b0beb95e4       57.8GB         22.3GB    U  &#x20;
vla-g1-teleop:lerobot-0.4.4   59382abde71f       59.2GB         22.6GB    U  &#x20;
CONTAINER ID   IMAGE                         COMMAND                   CREATED       STATUS                     PORTS     NAMES
6f9863feba8d   vla-g1-teleop:lerobot-0.4.4   "/usr/bin/tini -- ba…"   4 hours ago   Up 4 hours                           compassionate\_hellman
7ba5f7ad8442   59d6f920bf92                  "/bin/sh -c 'python3…"   6 hours ago   Exited (0) 6 hours ago               dreamy\_banach
b4c08653e599   synthnova-runtime:20260801    "/usr/bin/tini -- /u…"   4 days ago    Exited (137) 6 hours ago             synthnova
lsy03\@lsy03-System-Product-Name:\~$ cd "/home/lsy03/文档/ChatGPT/VLA G1 项目"

timeout 75s docker run --rm -i \\
&#x20; \--network host \\
&#x20; \--ipc host \\
&#x20; \--cap-drop ALL \\
&#x20; \--cap-add NET\_RAW \\
&#x20; \--env GALBOT\_CONFIG\_DIR=/data/config \\
&#x20; \--env PYTHONPATH=/opt/galbot/galbot\_sdk/linux-x86\_64-gcc940/lib/python:/workspace \\
&#x20; \--env LD\_LIBRARY\_PATH=/opt/galbot/galbot\_sdk/linux-x86\_64-gcc940/lib \\
&#x20; \--volume "$PWD/.vendor/galbot-pc-sdk:/opt/galbot:ro" \\
&#x20; \--volume "$PWD/.vendor/galbot-pc-sdk/config:/data/config:ro" \\
&#x20; \--volume "$PWD:/workspace" \\
&#x20; \--workdir /workspace \\
&#x20; \--entrypoint /opt/vla-venv/bin/python3 \\
&#x20; vla-g1-teleop:lerobot-0.4.4 -u - <<'PY'
import galbot\_sdk
from galbot\_sdk.g1 import GalbotRobot

robot = GalbotRobot()
initialized = False

try:
PY      print("cleanup=ok", flush=True)robot.get\_active\_controller(arm)}",lush=True)
probe\_start
sdk\_version= 1.9.1
init= True
is\_running= True
device= {'model': 'G1', 'serial\_number': 'R001GBDDAAAA0592', 'firmware\_version': 'GBS\_1.17.0.0.rc7', 'hardware\_version': '2.2', 'manufacturer': 'Galbot'}
left\_arm joint\_names=[]
left\_arm state\_count=0
left\_arm states= []
left\_arm active\_controller=left\_arm\_pvt\_ctrl
right\_arm joint\_names=[]
right\_arm state\_count=0
right\_arm states= []
right\_arm active\_controller=right\_arm\_pvt\_ctrl
cleanup=ok
lsy03\@lsy03-System-Product-Name:\~/文档/ChatGPT/VLA G1 项目$&#x20;

看一下这个

## 29. 助手

这次结果很关键：Docker 和 PC SDK 已经成功连上 G1，但双臂仍没有返回“活动关节”。

- `init=True`、`is_running=True`：PC SDK 1.9.1 初始化成功。
- `device=G1`：已通过 SDK 识别到机器人，网络和 DDS 基础连接正常。
- 左右臂 `joint_names=[]`：你调用的是 `get_joint_names(True, ...)`，只查询活动关节；这表示当前没有活动关节被报告，不一定代表所有关节数据绝对不存在。
- `active_controller=...`：只是 SDK 当前记录/默认的控制器名称，不能证明已经收到硬件反馈。

所以问题已经从“网络/Docker 无法连接”缩小到：G1 的双臂 WBC/状态发布没有激活，或者 `only_active_joint=True` 过滤掉了全部关节。暂时不要发送运动命令。

下一步请在同一个探测程序中把两处改成：

```python
names = list(robot.get_joint_names(False, [arm]))
states = list(robot.get_joint_states([arm], []))
```

并额外打印：

```python
print("groups=", robot.get_joint_group_names())
print("left_all=", robot.get_joint_names(False, ["left_arm"]))
print("right_all=", robot.get_joint_names(False, ["right_arm"]))
print("left_states=", len(robot.get_joint_states(["left_arm"], [])))
print("right_states=", len(robot.get_joint_states(["right_arm"], [])))
```

这一步仍然是只读，可以区分“关节未激活”和“状态流完全没有数据”。

## 30. 用户

给我命令我来复制

## 31. 助手

请完整复制下面这一段到 PC 终端执行。它会启动临时 Docker 容器，只读取左右臂状态，不调用任何运动接口：

```bash
cd "/home/lsy03/文档/ChatGPT/VLA G1 项目"

timeout 75s docker run --rm -i \
  --network host \
  --ipc host \
  --cap-drop ALL \
  --cap-add NET_RAW \
  --env GALBOT_CONFIG_DIR=/data/config \
  --env PYTHONPATH=/opt/galbot/galbot_sdk/linux-x86_64-gcc940/lib/python:/workspace \
  --env LD_LIBRARY_PATH=/opt/galbot/galbot_sdk/linux-x86_64-gcc940/lib \
  --volume "$PWD/.vendor/galbot-pc-sdk:/opt/galbot:ro" \
  --volume "$PWD/.vendor/galbot-pc-sdk/config:/data/config:ro" \
  --volume "$PWD:/workspace" \
  --workdir /workspace \
  --entrypoint /opt/vla-venv/bin/python3 \
  vla-g1-teleop:lerobot-0.4.4 -u - <<'PY'
import galbot_sdk
from galbot_sdk.g1 import GalbotRobot

robot = GalbotRobot()
initialized = False

try:
    print("probe_start", flush=True)
    print("sdk_version=", getattr(galbot_sdk, "__version__", "unknown"), flush=True)

    initialized = bool(robot.init())
    print("init=", initialized, flush=True)
    print("is_running=", bool(robot.is_running()), flush=True)

    if not initialized:
        raise RuntimeError("GalbotRobot.init() returned false")

    print("device=", robot.get_device_information(), flush=True)
    print("joint_groups=", list(robot.get_joint_group_names()), flush=True)

    for arm in ("left_arm", "right_arm"):
        all_names = list(robot.get_joint_names(False, [arm]))
        active_names = list(robot.get_joint_names(True, [arm]))
        states = list(robot.get_joint_states([arm], []))

        print(f"{arm} all_joint_names={all_names}", flush=True)
        print(f"{arm} active_joint_names={active_names}", flush=True)
        print(f"{arm} state_count={len(states)}", flush=True)
        print(
            f"{arm} state_names={[str(s.joint_name) for s in states]}",
            flush=True,
        )
        print(
            f"{arm} positions_rad={[float(s.position) for s in states]}",
            flush=True,
        )
        print(
            f"{arm} velocities_rad_s={[float(s.velocity) for s in states]}",
            flush=True,
        )
        print(
            f"{arm} active_controller={robot.get_active_controller(arm)}",
            flush=True,
        )

finally:
    if initialized:
        robot.request_shutdown()
        robot.wait_for_shutdown()
        robot.destroy()
        print("cleanup=ok", flush=True)
PY
```

把完整输出发给我，尤其是 `all_joint_names`、`active_joint_names` 和 `state_count`。

## 32. 用户

timeout 75s docker run --rm -i \\
&#x20; \--network host \\
&#x20; \--ipc host \\
&#x20; \--cap-drop ALL \\
&#x20; \--cap-add NET\_RAW \\
&#x20; \--env GALBOT\_CONFIG\_DIR=/data/config \\
&#x20; \--env PYTHONPATH=/opt/galbot/galbot\_sdk/linux-x86\_64-gcc940/lib/python:/workspace \\
&#x20; \--env LD\_LIBRARY\_PATH=/opt/galbot/galbot\_sdk/linux-x86\_64-gcc940/lib \\
&#x20; \--volume "$PWD/.vendor/galbot-pc-sdk:/opt/galbot:ro" \\
&#x20; \--volume "$PWD/.vendor/galbot-pc-sdk/config:/data/config:ro" \\
&#x20; \--volume "$PWD:/workspace" \\
&#x20; \--workdir /workspace \\
&#x20; \--entrypoint /opt/vla-venv/bin/python3 \\
&#x20; vla-g1-teleop:lerobot-0.4.4 -u - <<'PY'
import galbot\_sdk
from galbot\_sdk.g1 import GalbotRobot

robot = GalbotRobot()
initialized = False

try:
PY      print("cleanup=ok", flush=True)robot.get\_active\_controller(arm)}",",sh=True)
probe\_start
sdk\_version= 1.9.1
init= True
is\_running= True
device= {'model': 'G1', 'serial\_number': 'R001GBDDAAAA0592', 'firmware\_version': 'GBS\_1.17.0.0.rc7', 'hardware\_version': '2.2', 'manufacturer': 'Galbot'}
joint\_groups= []
left\_arm all\_joint\_names=[]
left\_arm active\_joint\_names=[]
left\_arm state\_count=0
left\_arm state\_names=[]
left\_arm positions\_rad=[]
left\_arm velocities\_rad\_s=[]
left\_arm active\_controller=left\_arm\_pvt\_ctrl
right\_arm all\_joint\_names=[]
right\_arm active\_joint\_names=[]
right\_arm state\_count=0
right\_arm state\_names=[]
right\_arm positions\_rad=[]
right\_arm velocities\_rad\_s=[]
right\_arm active\_controller=right\_arm\_pvt\_ctrl
cleanup=ok
lsy03\@lsy03-System-Product-Name:\~/文档/ChatGPT/VLA G1 项目$&#x20;

## 33. 助手

这次结果已经把问题定位得更清楚了：

- Docker、PC SDK 1.9.1、网络和 G1 设备发现都正常：
  - `init=True`
  - `is_running=True`
  - 成功识别 G1 和固件版本
- 但 `joint_groups=[]`
- `get_joint_names(False, ...)` 左右臂仍然都是空
- 左右臂 `state_count=0`

因为这次已经使用 `False` 查询全部关节，所以可以排除“仅仅是活动关节过滤”的原因。

结论：PC SDK 能连接到 G1 的设备信息服务，但没有拿到关节组/关节反馈数据。问题集中在 G1 侧 WBC/关节状态发布服务、控制模式或配置，不是 Docker 或网线问题。

`left_arm_pvt_ctrl`、`right_arm_pvt_ctrl` 只是 SDK 的默认/缓存控制器名称，不能证明关节状态已经在线。

目前仍不要调用 `set_joint_positions` 或其他运动 API；需要先检查 G1 侧 `singorix_wbcs_main` 和关节状态发布服务。

## 34. 用户

请问我应该如何 检查 G1 侧 `singorix_wbcs_main` 和关节状态发布服务 ？

## 35. 助手

要分两台机检查：官方架构中，`singorix_wbcs_main` 在 XCU，关节执行/实时控制也在 XCU；HPU/Orin 侧检查 `robot_state_publisher` 和 launcher。

先只做下面的只读检查，不要执行 `systemctl restart launcher`，也不要运行运动命令。

### 1. XCU：检查 `singorix_wbcs_main`

从 PC 登录 XCU：

```bash
ssh root@192.168.1.66
```

登录后执行：

```bash
hostname
date
ip -br addr

ps -ef | grep -E '[s]ingorix_wbcs_main|[s]ingorix|[w]bc|[l]auncher'

systemctl is-active launcher
systemctl status launcher --no-pager -l
systemctl --failed --no-pager

grep -nE 'using_wbc|enable_modify_embosa_cfg|CUR_VERSION' \
  /data/config/default/SingoriX/galbot_g1-v2.2/robot_config.toml \
  /data/config/system.cfg 2>/dev/null

sed -n '1,160p' /data/config/embosa_ip_config.json 2>/dev/null

ls -l /userdata/log/SingoriX/info/singorix_wbcs_main.INFO
tail -n 200 /userdata/log/SingoriX/info/singorix_wbcs_main.INFO

grep -iE 'error|fail|fault|estop|brake|joint|wbc|sensor|state|controller|target' \
  /userdata/log/SingoriX/info/singorix_wbcs_main.INFO | tail -n 120
```

另外检查整机模式：

```bash
/data/bin/sys_tool
```

进入交互界面后输入：

```text
q
```

正常应看到：

```text
mode_type: WORKING_MODE
```

### 2. HPU/Orin：检查状态发布相关服务

从 PC 登录 HPU：

```bash
ssh galbot@192.168.1.88
```

执行：

```bash
hostname
date
ip -br addr

ps -ef | grep -E '[r]obot_state_publisher|[s]tate_publish|[l]auncher'

systemctl is-active launcher
systemctl status launcher --no-pager -l
systemctl --failed --no-pager

grep -nE 'enable_modify_embosa_cfg|CUR_VERSION' \
  /data/config/system.cfg 2>/dev/null

sed -n '1,160p' /data/config/embosa_ip_config.json 2>/dev/null

find /userdata/log -maxdepth 2 -type f 2>/dev/null \
  | grep -Ei 'robot.*state|state.*publish|joint|wbc|singorix' \
  | sort | tail -n 50

journalctl -u launcher -b --no-pager -n 200 2>&1 \
  | grep -iE 'error|fail|fault|state|joint|wbc|launcher'
```

重点把 XCU 的以下输出发来：

- `ps -ef` 中是否存在 `singorix_wbcs_main`
- `systemctl status launcher`
- `using_wbc`
- `enable_modify_embosa_cfg`
- `singorix_wbcs_main.INFO` 最后 200 行
- `sys_tool` 的 `q` 结果

如果 XCU 上 `singorix_wbcs_main` 正常运行但日志出现关节初始化、急停、抱闸、MCU 或 WBC 错误，基本就能解释为什么 PC SDK 能识别 G1，却拿不到左右臂关节状态。

## 36. 用户

[folder\_mkdirs]: folder [/userdata/log/sys\_tool] has already exists
\##############################################
m: 切换整机模式
q: 查询整机模式
o: 查询整机Onboard Config配置信息
v: 查询基线软件版本信息
p: 启动pipeline任务
c: 结束pipeline任务
cr: 触发回充
cu: 触发出桩
ci: 打断充电
pack: 切换打包姿态
exit: 退出
r: 重启Launcher
\##############################################
please input cmd\_type: q
current mode: SN: "R001GBDDAAAA0592"
timestamp: 1788871393970
mode\_type: WORKING\_MODE
mode\_switch\_state {
&#x20; status: SUCCESS
}
monitor\_metrics {
&#x20; bms\_soc: 78
&#x20; bms\_voltage: 52.5
&#x20; charging\_type: INVALID\_CHARGE
&#x20; bms\_wireless\_charge\_current: 0
}
mode\_process\_list {
&#x20; xcu\_process\_name: "devinfo\_server"
&#x20; xcu\_process\_name: "singorix\_wbcs\_main"
&#x20; hpu\_process\_name: "surround\_cameras\_capture"
&#x20; hpu\_process\_name: "galbot\_fusion\_main"
&#x20; hpu\_process\_name: "eyehand\_calib\_publish"
&#x20; hpu\_process\_name: "robot\_state\_publish"
&#x20; hpu\_process\_name: "front\_head\_camera\_capture"
&#x20; hpu\_process\_name: "swallows"
&#x20; hpu\_process\_name: "service\_motion\_plan"
&#x20; hpu\_process\_name: "left\_arm\_camera\_capture"
&#x20; hpu\_process\_name: "Perception\_Algorithm\_Service"
&#x20; hpu\_process\_name: "localization\_server"
&#x20; hpu\_process\_name: "service\_navigation\_plan"
&#x20; hpu\_process\_name: "teleoperation"
&#x20; hpu\_process\_name: "relocalization\_server"
&#x20; hpu\_process\_name: "map\_manager\_node"
&#x20; hpu\_process\_name: "service\_lidar\_capture"
&#x20; hpu\_process\_name: "right\_arm\_camera\_capture"
}




\##############################################
m: 切换整机模式
q: 查询整机模式
o: 查询整机Onboard Config配置信息
v: 查询基线软件版本信息
p: 启动pipeline任务
c: 结束pipeline任务
cr: 触发回充
cu: 触发出桩
ci: 打断充电
pack: 切换打包姿态
exit: 退出
r: 重启Launcher
\##############################################
please input cmd\_type:&#x20;

[附件：25b218dc6f7bc7b5--pasted-text.txt](../../assets/25b218dc6f7bc7b5--pasted-text.txt)

## 37. 助手

这个结果很好，说明：

- 当前整机模式是 `WORKING_MODE`
- 模式切换状态 `SUCCESS`
- 电量约 `78%`
- 配置中包含：
  - XCU：`singorix_wbcs_main`
  - HPU：`robot_state_publish`
- 不是整机处于 `ABNORMAL`，也不是 launcher 配置完全缺失

但要注意：`mode_process_list` 只是 launcher 期望运行的进程列表，不能证明进程实际存活或正在发布关节数据。

先在 `sys_tool` 里输入：

```text
exit
```

然后在 XCU 上执行：

```bash
hostname
ps -eo pid,ppid,stat,etime,cmd | grep -E '[s]ingorix_wbcs_main|[s]ingorix|[w]bc|[l]auncher'

systemctl status launcher --no-pager -l
systemctl --failed --no-pager

grep -nE 'using_wbc|enable_modify_embosa_cfg|CUR_VERSION' \
  /data/config/default/SingoriX/galbot_g1-v2.2/robot_config.toml \
  /data/config/system.cfg 2>/dev/null

ls -l /userdata/log/SingoriX/info/singorix_wbcs_main.INFO
tail -n 200 /userdata/log/SingoriX/info/singorix_wbcs_main.INFO

grep -iE 'error|fail|fault|estop|brake|joint|wbc|sensor|state|controller|target|init' \
  /userdata/log/SingoriX/info/singorix_wbcs_main.INFO | tail -n 150
```

然后回到 HPU/G1 终端执行：

```bash
hostname
ps -eo pid,ppid,stat,etime,cmd | grep -E '[r]obot_state_publish|[r]obot_state_publisher|[l]auncher'

systemctl status launcher --no-pager -l
systemctl --failed --no-pager

find /userdata/log -maxdepth 2 -type f 2>/dev/null \
  | grep -Ei 'robot.*state|state.*publish|joint|wbc' \
  | sort | tail -n 50
```

重点发我 XCU 的 `singorix_wbcs_main.INFO` 最后 200 行，以及 `using_wbc` 的值。暂时不要在 `sys_tool` 中输入 `r`，因为 `r` 会重启 Launcher。

## 38. 用户

[附件：ff0ac23855c5f381--pasted-text.txt](../../assets/ff0ac23855c5f381--pasted-text.txt)

## 39. 助手（进展）

我先读取你刚上传的 `singorix_wbcs_main.INFO` 输出，再根据实际日志判断是关节初始化、WBC、急停/抱闸还是状态发布问题。

## 40. 助手（进展）

日志已经给出实质性故障：`singorix_wbcs_main` 进程虽然存在，但 WBCS 一直处于 `INITIALIZING`，并且左臂反复报 `DriverError`。我再确认附件末尾是否包含右臂或 MCU 相关行，避免只根据左臂片段下结论。

## 41. 助手

这份日志已经找到核心故障了：

```text
service state: INITIALIZING
Error component: WBCSManager
description: Sync left_arm ...
left_arm_driver_state
error code: 45
description: DriverError
left_arm_max_error_level
error code: 30000
description: ErrorSafetyMedium
```

并且已经连续重试到：

```text
sync initialization retry 6749/31536000
```

这说明：

- `singorix_wbcs_main` 进程虽然存在，但没有初始化完成。
- 左臂驱动处于 `DriverError`，尤其是 `left_arm_joint1_custom_error=59`。
- `WORKING_MODE` 只是整机高层模式正常，不代表 WBC 和关节驱动正常。
- 左臂 WBC 一直未同步完成，很可能导致 PC SDK 左右两侧都拿不到关节组和状态。
- 目前不要调用任何运动 API，也不要在当前 `sys_tool` 中输入 `r`，因为这里的 `r` 是重启 Launcher。

请先在 XCU 上执行下面几条只读命令：

```bash
pgrep -af 'singorix_wbcs_main|singorix|wbc'

grep -iE 'right_arm|left_arm|driver_state|max_error_level|custom_error|error code' \
  /userdata/log/SingoriX/info/singorix_wbcs_main.INFO | tail -n 200

grep -RniE 'left_arm|right_arm|driver|mcu|motor|fault|estop|brake|error' \
  /userdata/log/mp_core /userdata/log/secure_core 2>/dev/null | tail -n 200

grep -nE 'using_wbc|enable_modify_embosa_cfg' \
  /data/config/default/SingoriX/galbot_g1-v2.2/robot_config.toml \
  /data/config/system.cfg 2>/dev/null
```

重点把这几类结果发来：

1. 是否有 `right_arm` 错误；
2. `mp_core`、`secure_core` 是否报告 MCU/电机/驱动故障；
3. `using_wbc` 的值。

当前最可能的方向已经不是网络，而是左臂底层驱动或关节初始化故障。

## 42. 用户

[附件：6a34c7613828f550--pasted-text.txt](../../assets/6a34c7613828f550--pasted-text.txt)

## 43. 助手（进展）

我读取你上传的 `secure_core` 日志片段，重点筛选左/右臂、MCU、电机驱动、急停和错误码，确认它是否能解释 `DriverError`。

## 44. 助手

这份日志已经明确指向 XCU 侧的左臂驱动故障：

```text
WBCSManager: Sync left_arm ...
service state: INITIALIZING
left_arm_driver_state: error code 45, DriverError
left_arm_joint1_custom_error: error code 59
left_arm_max_error_level: ErrorSafetyMedium
```

含义是：

- `singorix_wbcs_main` 进程本身还活着，但 WBCS 没有初始化完成。
- 它已经持续重试同步左臂，属于卡在初始化，不是短暂等待。
- 左臂至少有一个底层驱动错误，重点是 `joint1`。
- 这可以解释为什么 Docker PC SDK 能识别 G1，但左右臂的 `joint_groups`、关节名和状态全部为空。
- `WORKING_MODE` 只代表整机高层模式正常，不能覆盖底层 `DriverError`。

你上传的 `secure_core` 片段主要显示的是腿部和头部的急停/初始化记录，没有直接解释左臂故障；`error code 59` 在当前日志中也没有进一步解码。

请先在 XCU 执行下面两条只读命令，确认右臂是否也有错误，以及配置值：

```bash
grep -iE 'left_arm|right_arm|driver_state|max_error_level|custom_error|Sync' \
  /userdata/log/SingoriX/info/singorix_wbcs_main.INFO | tail -n 120
```

```bash
grep -nE 'using_wbc|enable_modify_embosa_cfg|CUR_VERSION' \
  /data/config/default/SingoriX/galbot_g1-v2.2/robot_config.toml \
  /data/config/system.cfg 2>/dev/null
```

目前不要输入 `sys_tool` 的 `r`，不要清错、不要上使能，也不要调用任何运动 API。重点应转向左臂 J1 驱动/通信、抱闸/电源和 MCU 底层诊断。

## 45. 用户

[附件：6f94ab4d220e8d43--pasted-text.txt](../../assets/6f94ab4d220e8d43--pasted-text.txt)

## 46. 助手（进展）

我读取这次上传的日志/配置输出，核对右臂状态以及 `using_wbc`、`enable_modify_embosa_cfg` 的实际值。

## 47. 助手

这批结果非常关键：

- `singorix_wbcs_main` 进程仍在运行：PID `3692`
- `CUR_VERSION=GBS_1.17.0.0.rc7`，与当前 PC SDK 1.9.1 兼容
- `enable_modify_embosa_cfg=false`：很好，说明重启不会自动覆盖 EmbOSA 网络配置
- 但左臂仍持续：

```text
Sync left_arm ...
left_arm_driver_state: error code 45, DriverError
left_arm_joint1_custom_error: error code 59
retry 7009/31536000
```

因此现在基本可以排除网络配置被重置和 SDK 版本不匹配。核心问题是 XCU 内部左臂驱动/WBC 初始化失败。

另外，`secure_core` 中看到的 `leg`、`head` 急停记录只涉及腿和头，不能证明左右臂正常。`right_arm` 在当前日志片段中没有报错，但也没有证据表明它已经正常，因为 WBC 仍被左臂同步失败卡住。

你之前的命令没有输出 `using_wbc`，说明该字段可能不在查询的配置文件中，或者实际配置路径不同。请在 XCU 继续执行，只读检查：

```bash
find /data/config /userdata/user_config -type f \
  \( -name 'robot_config.toml' -o -name '*service*config*.toml' \) \
  -print 2>/dev/null
```

```bash
grep -RniE 'using_wbc|singorix_wbcs_main|left_arm|right_arm' \
  /data/config /userdata/user_config 2>/dev/null | head -n 200
```

```bash
grep -nE 'Sync left_arm|left_arm_driver_state|left_arm_joint1_custom_error|right_arm' \
  /userdata/log/SingoriX/info/singorix_wbcs_main.INFO | head -n 80
```

目前不要修改配置、不要清错、不要上使能，也不要输入 `sys_tool` 的 `r`。

## 48. 用户

现在右臂是否有错误？

## 49. 助手

根据你目前发来的日志，暂时没有看到右臂的明确错误；当前反复报错的全部是左臂：

```text
Sync left_arm
left_arm_driver_state: DriverError
left_arm_joint1_custom_error: error code 59
```

但这不能证明右臂正常，因为日志是 `tail -n 120`，左臂持续刷屏可能把右臂较早的错误挤出去了；而 PC SDK 目前左右臂状态都为空。

请在 XCU 单独查询右臂：

```bash
grep -iE 'right_arm|right.*driver|right.*joint|right.*error|right.*fault|right.*sync' \
  /userdata/log/SingoriX/info/singorix_wbcs_main.INFO | tail -n 100
```

如果没有输出，表示当前日志文件中没有右臂专属错误记录；但 WBC 整体仍因左臂 `DriverError` 未就绪，右臂状态也暂时不能用于控制。
