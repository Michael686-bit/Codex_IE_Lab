# 9_10 遥操1:制定有线连接与状态确认计划

- 归档编号：`01a089d5-0402-7b41-8727-93f2c06078dc`
- 项目：VLA G1 项目
- 创建时间：2026-09-10T13:40:33.922000+08:00
- 更新时间：2026-09-10T14:08:45.643000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

我们在“制定有线控制与采集计划 ” 对话的基础上继续，我们先进行 ：1恢复有线连接、Docker 和双端状态读取确认 GP001、G1 的数据有效且持续更新 。 先给我计划，明确步骤流程和目标，现在我网线已经联通我本地主机和G1机器人了。

## 2. 助手（进展）

我先查看“制定有线控制与采集计划”对话中的方案和设备信息，再整理这一步的检查流程、目标与通过标准。

## 3. 助手

**这一步的目标：恢复“PC 有线网络＋Docker”的读取链路，确认 GP001 主臂和 G1 真机反馈都有效、持续更新，并能同时稳定读取至少 60 秒。**

沿用“制定有线控制与采集计划”最后确定的顺序：**先恢复读取 → 再做主从映射标定 → 联合预览 → 小幅真机验证。**本轮先给计划，尚未执行设备检查。

你已经接好 PC 与 G1 的网线，接下来按以下流程推进：

| 步骤 | 具体操作 | 目标与通过标准 |
|---|---|---|
| **1．确认有线网络** | 检查 PC 网卡链路、IP、路由；测试 XCU、HPU 的连通性，确认数据实际走有线网卡 | 有线接口正常；机器人地址可达；访问路由明确走有线 |
| **2．恢复 Docker 环境** | 复用现有镜像，检查 host 网络、PC SDK 挂载、Python、LeRobot 和 SDK 导入；核验当前机器人与 SDK 的兼容性 | 容器可重复启动；依赖加载成功；使用 x86_64 PC SDK |
| **3．单独读取 G1** | 先做状态快照，再连续读取 10 秒；检查双臂关节名称、数量、位置、速度、源时间戳及活动控制器 | 双臂 14 个关节完整，数值有限且合理；源时间戳持续推进，无回退；可正常退出 |
| **4．单独读取 GP001** | 检查 USB、实际串口名、权限与容器映射；加载现有厂商模块，先连续读取 10 秒 | 双臂各 7 维数据完整；帧持续到达，无无效值、序号回退或异常断流 |
| **5．联合读取 60 秒** | 在同一 Docker 环境中同时读取 GP001 与 G1，分别记录各自的时间信息和质量指标 | 两路同时稳定更新，互不阻塞；没有持续返回旧反馈、异常退出或超时 |
| **6．整理验收结果** | 保存原始读取日志、统计报告、环境信息和复现命令 | 明确给出通过／未通过、异常所在环节，以及能否进入标定 |

历史有线配置是 **PC `eno1 / 192.168.1.99/24`、XCU `192.168.1.66`、HPU `192.168.1.88`**；GP001 曾使用 `/dev/ttyACM0`。这些是检查起点，执行时会重新核对当前实际配置。[历史基线](<../../assets/abdb0442884e954d--BASELINE_20260908.md>)

**“持续更新”的判定要特别注意：**

- **G1 静止时，角度不变是正常现象。**重点看源时间戳是否推进，不能仅凭循环一直打印数值判断正常。
- 分别统计**读取频率与不同源反馈帧的更新频率**。历史读取约 97.5 Hz，但不同反馈约 60 Hz，不能混为一谈。
- 记录两路的有效帧数、间隔 P50/P95/最大值、重复帧、回退及断流。跨设备时钟未经核验，不直接相减计算延迟。
- GP001 可在联合读取期间配合一次轻微手动活动，确认输入随实际活动变化；单位、方向和比例的正式标定放在下一步。

**出现异常时的排查顺序：**

- G1 读不到：先查有线路由与容器 SDK；必要时对照机器人端只读状态和服务日志，区分 PC 通信问题与机器人状态发布问题。此前出现过“网络可达但关节反馈为空”，所以 Ping 通不能算验收。
- GP001 读不到：依次查 USB 识别、串口占用与权限、容器映射、厂商模块授权。主臂问题不阻塞 G1 的独立检查。
- 单独读取正常、联合读取异常：检查读取阻塞、资源竞争及日志开销，再复测完整 60 秒。

**本阶段完成标志：两路真实状态的 60 秒联合读取报告通过，复现入口可用，具备进入主臂辨识与主从映射标定的条件。**本阶段只运行状态读取，不下发 G1 运动指令。

[附件：abdb0442884e954d--BASELINE_20260908.md](../../assets/abdb0442884e954d--BASELINE_20260908.md)

## 4. 用户

我们开始执行

## 5. 助手（进展）

现在开始执行第一轮恢复检查。我会先按 `galbot-136-ssh` 技能的远程操作规范读取说明，然后依次检查本机有线链路、Docker、G1 只读状态和 GP001；这一轮只读，不发送运动指令。

## 6. 助手（进展）

技能说明确认：远程操作只使用已保存凭据和主机密钥校验；我会把 G1 的 `--status` 作为只读检查，不启动控制器、不下发目标。现在并行检查本机主机前置条件、Docker 容器验证和 G1 只读快照。

## 7. 助手（进展）

第一轮在沙箱内被系统权限挡住了：本机当前会话看不到 `docker` 组和 `/dev/ttyACM0`，SSH 也被禁止建立网络 socket。这不能判断设备本身故障；我将用只读权限提升重新执行同样的检查，并额外列出实际串口和有线网卡。

## 8. 助手（进展）

目前已确认两件事：

- 有线网卡 `eno1` 已拿到 `192.168.1.99/24`，Docker 服务版本为 `29.1.3`；G1 远程只读快照能正常读取双臂 14 关节，当前静止且无越限。
- GP001 的预期设备 `/dev/ttyACM0` 不存在；因此 Docker 入口按设计拒绝启动，双端联合读取还不能验收。

我继续查实际串口、XCU/HPU 有线连通性，并在不依赖 GP001 的情况下验证 Docker 镜像和 PC SDK 是否可加载。

## 9. 助手（进展）

有线网络本身已通过：`eno1 → 192.168.1.66/88` 均为 0% 丢包，且 G1 的 `eth1` 正是 `192.168.1.88/24`。Docker 镜像也存在。现在做容器内的依赖、PC SDK 导入和只读单元检查；这一步暂时不需要 GP001 串口。

## 10. 助手（进展）

容器内依赖和项目测试已通过：Python 3.11.15、LeRobot 0.4.4、PC Galbot SDK 1.9.1 均可加载，49 项读取/映射/适配测试通过。下一步执行真正的 PC Docker → G1 连续只读探针，记录 10 秒反馈频率、源时间戳推进和反馈年龄；仍不会调用运动接口。

## 11. 助手（进展）

连续只读探针已启动，正在等待 10 秒采样和清理阶段完成；如果 SDK 的退出屏障异常，我会单独区分“采样成功”和“清理退出”两个结果。

## 12. 助手（进展）

探针第一次初始化没有通过，原因已明确：我复现命令漏挂载了 SDK 的配置目录，容器提示找不到 `/data/config/system.cfg`；这不是 G1 数据故障。现有配置已确认包含 PC `192.168.1.99`、XCU `192.168.1.66` 和 G1 `192.168.1.88`，我补上该只读挂载后重跑。

## 13. 助手（进展）

补挂配置后，PC Docker → G1 的 10 秒连续读取已通过：975 帧、97.5 Hz，P95 间隔 10.44 ms、无超过 20 ms 的停顿、源时间戳无回退，且 `motion_api_called=false`。我现在按计划把 G1 只读探针延长到 60 秒，完成这一端的持续性验收；GP001 仍待串口恢复。

## 14. 助手（进展）

60 秒 G1 只读探针仍在运行，目前没有报错；它会在完成后一次性输出完整统计和保存报告。

## 15. 助手（进展）

60 秒 G1 连续读取已完成并通过：5,859 帧、97.65 Hz，P95 采样间隔 10.44 ms，最大 13.47 ms，0 次超过 20 ms，源时间戳 0 次回退；探针明确未调用运动接口。现在只剩 GP001 物理串口未出现，我做最后一次状态确认后给出当前验收结论和下一步动作。

## 16. 助手（进展）

补充验证一下 G1 双臂：PC Docker 的 60 秒探针按现有脚本读取左臂 7 关节；之前远端快照已读到双臂 14 关节。现在短时间重复远端双臂只读快照，确认两臂反馈都能连续刷新，然后结束本轮。

## 17. 助手

已执行。当前结论是：**有线网络、Docker、PC→G1 状态读取均通过；GP001 尚未被系统识别，因此双端联合读取还不能完成。**

- 有线链路：`eno1=192.168.1.99`，XCU `192.168.1.66`、G1 `192.168.1.88` 均 0% 丢包。
- Docker：镜像、Python 3.11.15、LeRobot 0.4.4、PC SDK 1.9.1 均正常；49 项项目测试通过。
- G1：PC Docker 连续读取 60 秒，5,859 帧、97.65 Hz，P95 间隔 10.44 ms，0 次停顿超过 20 ms，源时间戳 0 次回退，未调用运动接口。双臂 14 关节重复只读快照也正常。
- GP001：当前没有 `/dev/ttyACM*`、`/dev/ttyUSB*` 或 `/dev/serial/by-id` 设备，Docker 检查因此停止。

报告已保存：

[60 秒 G1 读取报告](<../../assets/8a56bca834bcef06--g1_wired_probe_20260910_60s.json>)

请现在接入或重新插拔 GP001 的 USB 数据线，确认设备上电后告诉我“已接入”；我将继续执行 GP001 10 秒读取和 GP001＋G1 联合 60 秒验收。

[附件：8a56bca834bcef06--g1_wired_probe_20260910_60s.json](../../assets/8a56bca834bcef06--g1_wired_probe_20260910_60s.json)

## 18. 用户

已接入

## 19. 助手（进展）

收到。我现在重新检测 GP001 的实际串口名和权限，然后运行主机前置检查与容器验证；确认串口可用后立即做 10 秒 GP001 采集，再与 G1 做联合读取。

## 20. 助手（进展）

GP001 已恢复：系统识别为 `Galbot Remote Operate ComPort`，稳定映射到 `/dev/ttyACM0`，权限和容器映射均正常；主机检查、Docker 完整验证和 49 项测试全部通过。现在开始 10 秒真实 GP001 只读采集，不连接 G1 运动接口。

## 21. 助手（进展）

GP001 串口和 Docker 映射已经恢复，但采集程序还需要厂商受保护的 `remote_control_lite` 模块；该模块不在项目镜像中，当前会话也没有授权模块路径。我不会从历史文件读取或复制凭据，先只查找本机是否已有已安装模块或可复用的运行容器。

## 22. 助手（进展）

已找到本机已安装的厂商模块目录：

`/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir/synthnova_delivery/module`

我将只通过 `docker/run.sh` 挂载该目录并尝试 10 秒 GP001 读取；不读取、不打印任何授权凭据，也不发送 G1 命令。

## 23. 助手（进展）

GP001 设备本身已连通，但厂商采集模块拒绝启动，明确报错“未配置客户授权账号”。我不会从编辑器历史中提取凭据；先检查当前用户的标准 shell 配置是否已有授权变量（只判断是否存在，不输出值），若没有，就需要你在当前终端启用厂商授权后再继续。

## 24. 助手

GP001 的硬件和 Docker 链路已恢复：

- 设备识别为 `Galbot Remote Operate ComPort`，映射 `/dev/ttyACM0`。
- 有线网络、主机检查、Docker 完整验证和 49 项测试均通过。
- 尝试 10 秒采集时，厂商模块报错：未配置 `ENCMGR_APP_ID` 和 `ENCMGR_APP_SECRET`，因此尚未读取 GP001 数据。
- 整个过程没有发送 G1 运动命令。

请在启动 Codex 的同一终端中按厂商方式配置这两个授权变量（不要把值发到对话中），然后回复“授权已配置”。我会立即重跑 GP001 10 秒采集和 GP001＋G1 联合读取验收。

## 25. 用户

[$synthnova-auth](/home/lsy03/.codex/skills/synthnova-auth/SKILL.md)

## 26. 用户

<skill>
<name>synthnova-auth</name>
<path>/home/lsy03/.codex/skills/synthnova-auth/SKILL.md</path>
---
name: synthnova-auth
description: 使用本机保存的 Synthnova 客户端凭据配置 ENCMGR_APP_ID 和 ENCMGR_APP_SECRET。用于用户要求 Synthnova 认证、配置这两个变量，或调用此技能时。
---

# Synthnova 客户端凭据

用户已提供 Synthnova 的客户 APP ID 和 APP SECRET。这是客户端凭据，尚未确认是网页登录账号密码，不要自动填入普通用户名/密码表单。

## 本机配置

凭据保存在 `/home/lsy03/.config/synthnova/credentials.env`，文件权限应为 `600`，父目录为 `700`。技能本身不包含凭据值。

执行用户要求的 Synthnova 命令时，在同一 shell 中载入环境变量再运行目标命令：

```bash
set +x
set -a
source /home/lsy03/.config/synthnova/credentials.env
set +a
# 在此运行已确认的 Synthnova 客户端命令，继承上述环境变量。
```

不要用 cat、printenv、env 或日志输出凭据，也不要将密钥写入命令行参数、项目文件或版本库。需要程序读取时直接读取凭据文件或使用继承的环境变量，无需把密钥读入对话。

## 使用范围

根据当前用户任务确认客户端命令、服务地址和认证方式。当前没有保存官网地址、API 端点或签名规则；优先检查用户提供的项目配置或官方文档，不要猜测接口，也不要把密钥发送给未确认的服务。

保存凭据不表示已验证其有效性，也不授权购买、上传数据或修改远端资源。执行当前用户已授权的操作，不重复请求已有授权。文件缺失或认证失败时，报告具体问题，不回显凭据。

</skill>

## 27. 助手（进展）

我将使用你明确调用的 `synthnova-auth` 技能：它会在同一 shell 中载入本机保存的 Synthnova 客户端凭据，不回显、不写入项目，然后重试 GP001 只读采集。接着继续联合读取验收；不会发送 G1 运动命令。

## 28. 助手（进展）

Synthnova 授权已生效，GP001 10 秒采集成功：937 个有效帧、0 个无效帧，约 93.63 Hz，输出已保存。现在对采集质量做报告，然后启动 GP001＋G1 同时读取的联合只读验收。

## 29. 助手（进展）

GP001 质量报告已通过：937 有效帧、0 无效帧、序号连续、P50 间隔 10.26 ms、P95 10.52 ms。现在开始 60 秒 GP001＋G1 联合只读预览，记录两端同一时间段的数据和映射计算；映射结果只写日志，不下发机器人。

## 30. 助手（进展）

联合预览第一次没有进入采样，原因是 `docker/run.sh` 本身只挂载 PC SDK，不自动 `source` SDK 环境；容器报 `galbot_sdk` 未找到，GP001 没有被读取。我补上 SDK setup 后用新报告名重跑，仍不涉及运动。

## 31. 助手（进展）

60 秒联合只读预览已完成：5,419 个联合有效帧、0 个拒绝，GP001 和 G1 在同一容器进程内同时读取，且 `motion_api_called=false`。我现在计算联合日志的频率、时间戳连续性、数值有限性和目标范围，完成验收报告。

## 32. 助手（进展）

联合日志中的有效采样窗口实际为 56.74 秒（初始化 G1 花费了约 3 秒），虽然数据连续性已通过，但未达到计划中的严格 60 秒窗口。为完成验收，我将把联合运行时长设为 65 秒，确保有效联合采样超过 60 秒。

## 33. 助手（进展）

65 秒联合读取正在运行，期间 GP001 驱动打印了一条原始错误帧诊断；这类原始串口诊断在历史采集里也出现过，我会以采集器的有效/无效帧计数为准，并在结束后核对是否形成无效数据。

## 34. 助手

第一阶段“恢复有线连接、Docker 和双端状态读取”已完成。

- 网络：`eno1 → 192.168.1.88` 有线连通，XCU/G1 均 0% 丢包。
- Docker：环境、PC SDK 1.9.1、LeRobot 0.4.4 和测试全部通过。
- GP001：10 秒采集 937 帧，0 无效帧，P95 间隔 10.52 ms，质量报告 `quality_ok=true`。
- GP001＋G1 联合读取：有效窗口 60.66 秒，5,788 帧，0 拒绝；G1 源反馈约 59.1 Hz，时间戳 0 次回退，所有数值有限。
- 全程 `motion_api_called=false`，没有发送任何 G1 运动命令。

联合记录：[GP001＋G1 60 秒只读预览](</home/lsy03/文档/ChatGPT/VLA G1 项目/lerobot_runs/gp001_g1_wired_preview_20260910_65s.jsonl>)

期间驱动打印过原始错误帧诊断，但没有形成无效采集帧或联合拒绝。现在具备进入下一阶段“GP001→G1 主从映射辨识与标定”的条件。
