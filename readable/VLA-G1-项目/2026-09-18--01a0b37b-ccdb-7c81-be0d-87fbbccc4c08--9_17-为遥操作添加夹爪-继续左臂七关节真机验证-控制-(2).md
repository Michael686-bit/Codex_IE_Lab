# 9_17 为遥操作添加夹爪 继续左臂七关节真机验证 控制 (2)

- 归档编号：`01a0b37b-ccdb-7c81-be0d-87fbbccc4c08`
- 项目：VLA G1 项目
- 创建时间：2026-09-18T16:23:43.667000+08:00
- 更新时间：2026-09-22T09:14:42.430000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

bash teleoperation/run\_arm7\_direct.sh --execute --seconds 120 \\
&#x20; \--start-control left-button --align-speed-rad-s 0.4

现在我们是不是还没有添加夹爪控制，我们来使用左扳机键进行控制，给我在这个基础上重新写个脚本，我们随后来进行验证

## 2. 助手（进展）

我先检查左扳机的数据格式和 G1 夹爪接口，再基于现有七关节遥操作新增一个带夹爪控制的独立脚本，并做离线验证。

## 3. 助手（进展）

已确认左扳机是 `axes[3]`：松开约 0.7，按到底约 0。新脚本将按压程度映射为左夹爪开度，按下闭合、松开张开；仅在第二次确认进入正式跟随后启用，并要求先松开扳机再操作。

SDK 提供非阻塞夹爪指令，但没有独立的夹爪停止接口。我会限制每次目标变化，并在暂停或退出时尝试保持当前开度；是否能可靠停住，需要随后现场验证。

## 4. 助手（进展）

新入口已完成，夹爪默认开度范围为 0–100mm、速度 20mm/s，每次目标变化不超过 1mm。离线测试已确认：慢速对齐阶段不控制夹爪；启动时一直按住扳机不会触发闭合；暂停、断流和急停会停止正常夹爪指令并尝试保持开度。接下来完成回归测试和使用说明。

## 5. 助手（进展）

已补上夹爪故障退出测试，并调整停止顺序：先发送机械臂／底盘停止请求，再处理夹爪保持，避免夹爪接口延迟阻挡停止请求。新脚本仍使用 +104ms 补偿，夹爪和急停后的反馈检查也已接入。

## 6. 助手

原脚本没有夹爪控制。已新增独立夹爪版，使用：

```bash
bash teleoperation/run_arm7_direct_gripper.sh --execute --seconds 120 \
  --start-control left-button --align-speed-rad-s 0.4
```

- 两次确认进入 **FOLLOWING** 后，先松开左扳机启用夹爪。
- **按下逐渐闭合，松开逐渐张开**，按压程度控制开度。
- 默认开度 **0–100mm**、速度 **20mm/s**。
- 保留原机械臂保护及 **+104ms 时间补偿**。
- 暂停或退出时尝试保持当前开度；保持请求成功不等于已确认停稳。

**164 项离线测试通过，未进行真机运动验证。** 去掉 `--execute` 可先只读检查扳机和夹爪反馈。

[完整使用说明](<../../assets/0939e73d9d2b350d--ARM7_DIRECT_GRIPPER_USAGE.md>)

[附件：0939e73d9d2b350d--ARM7_DIRECT_GRIPPER_USAGE.md](../../assets/0939e73d9d2b350d--ARM7_DIRECT_GRIPPER_USAGE.md)

## 7. 用户

```
-seconds 120  这个能调到600吗
```

## 8. 助手（进展）

我会把夹爪版的时长上限调到 600 秒，并检查参数校验。原七关节入口的上限保持不变。

## 9. 助手

可以，已把夹爪版上限改为 **600 秒（10 分钟）**，参数校验通过：

```bash
bash teleoperation/run_arm7_direct_gripper.sh --execute --seconds 600 \
  --start-control left-button --align-speed-rad-s 0.4
```

从第一次按键进入慢速对齐开始计时，暂停时间也计入。注意参数是双横线 `--seconds`。

## 10. 用户

你看看日志，为什么我触碰扳机，夹爪没有云顶？

[附件：93b18439e62c68fd--pasted-text.txt](../../assets/93b18439e62c68fd--pasted-text.txt)

## 11. 用户

你看看日志，为什么我触碰扳机，夹爪没有运动？

[附件：93b18439e62c68fd--pasted-text.txt](../../assets/93b18439e62c68fd--pasted-text.txt)

## 12. 助手（进展）

我先查看你贴的日志和对应运行记录，确认是否进入正式跟随、扳机是否被识别，以及夹爪指令是否实际发出。

## 13. 助手

原因查清了：**左扳机已被识别，但本轮没有进入正式跟随，所以夹爪一直被程序锁定。**

最新完整日志显示：

- 扳机数值在 **0.031～0.700** 之间变化，说明按压输入正常。
- 状态停留在 `ALIGNING / ALIGN_READY`，**没有 `FOLLOWING` 事件**。
- 没有夹爪启用事件，也没有发送任何夹爪运动指令。

`ALIGN_READY` 只是“可以进行第二次确认”，机械臂此时仍在慢速跟随，因此容易误以为已经正式启动。

下次操作顺序：

1. 等待显示 `ALIGN_READY`，尽量保持主臂稳定。
2. **松开左固定键，再按一次左固定键**，确认出现 `FOLLOWING / 正式快速跟踪已开启`。
3. 松开左扳机，等出现“左夹爪已就绪”，再按压控制夹爪。

另外，本轮最后退出是 **J2 越过保守范围**，与夹爪无关。这次还没有实际验证到夹爪运动接口。

## 14. 用户

&#x20;J3   | -42.13 | -42.09 | -0.04     | [-164.13, +164.13] | 范围内   |
\| J4   | -52.48 | -52.48 | -0.00     | [-144.14, +104.14] | 范围内   |
\| J5   | -80.66 | -80.77 | +0.11     | [-164.13, +164.13] | 范围内   |
\| J6   | -2.39  | -2.28  | -0.11     | [-44.14, +39.14]   | 范围内   |
\| J7   | +4.06  | +4.07  | -0.01     | [-45.00, +45.00]   | 范围内   |
+------+--------+--------+-----------+--------------------+----------+
左扳机=0.031 | 左夹爪实测=97.5mm | 夹爪已启用

FOLLOWING | 单位：度 | 差值=GP映射目标-G1实测
两次确认：误差<15°按键慢速对齐；全部<3°再次按键正式跟随；正式跟随中按键停止
+------+--------+--------+-----------+--------------------+----------+
\| 关节 | GP主臂 | G1从臂 | 目标-从臂 | 保守绝对限位       | 检查结果 |
+------+--------+--------+-----------+--------------------+----------+
\| J1   | +24.08 | +24.83 | -0.75     | [-169.14, +169.14] | 范围内   |
\| J2   | -88.46 | -88.44 | -0.02     | [-89.14, +89.14]   | 范围内   |
\| J3   | -41.99 | -42.31 | +0.32     | [-164.13, +164.13] | 范围内   |
\| J4   | -54.24 | -53.80 | -0.44     | [-144.14, +104.14] | 范围内   |
\| J5   | -81.33 | -81.12 | -0.22     | [-164.13, +164.13] | 范围内   |
\| J6   | -1.75  | -2.11  | +0.36     | [-44.14, +39.14]   | 范围内   |
\| J7   | +3.90  | +3.99  | -0.10     | [-45.00, +45.00]   | 范围内   |
+------+--------+--------+-----------+--------------------+----------+
左扳机=0.700 | 左夹爪实测=94.5mm | 夹爪已启用
停止请求成功，左臂静止反馈已确认。
本轮结束：SafetyReject: left\_arm\_joint2越过保守范围
lsy03\@lsy03-System-Product-Name:\~/文档/ChatGPT/VLA G1 项目$&#x20;


再给我排查一下，夹爪为什么不动？，是否正确调用来SDK？

## 15. 助手（进展）

这次已进入正式跟随，日志中的夹爪反馈也从 97.5mm 变到了 94.5mm。我会核对完整日志里的目标开度、SDK 返回值和实际反馈，再对照 SDK 接口确认调用是否正确。

## 16. 助手

**SDK 调用方式正确，也确实发出了指令。问题主要是我加的“每步最多 1mm、目标最多领先反馈 1mm”限制太保守，夹爪响应很慢。**

核对了[本轮完整日志](</home/lsy03/文档/ChatGPT/VLA G1 项目/lerobot_runs/arm7_direct_gripper_20260918_163155_285908.jsonl>)：

- **254 条夹爪运动指令全部返回 `ControlStatus.SUCCESS`**。
- SDK 反馈显示，启用后夹爪从 **50mm 张开到 100mm，耗时约 8.47 秒**，平均只有约 **5.9mm/s**，远低于配置的 20mm/s。
- 第一次按压持续约 **1.64 秒**，反馈从 **100mm 闭合到 91mm**。
- 最后一次按压持续约 **0.87 秒**，反馈从 **100mm 闭合到 95.5mm**，随后松开又开始张开。

实际调用是：

```python
robot.set_gripper_command(
    'left_gripper', goal_width_m, 0.02, 5.0, False
)
```

参数顺序、米制单位和非阻塞方式均符合本地 SDK 接口声明。

**所以从反馈看，夹爪有运动，但每次扣扳机只闭合几毫米，松开马上反向张开，看起来不明显。** 代码虽然计算出了接近闭合的目标，却只向 SDK 发送离当前位置约 1mm 的小目标，这是当前跟手性差的主要原因。

下一步应调整这段小步控制策略，而不是更换 SDK 接口或盲目增大夹持力。最后的退出仍由 **J2 越限**触发，与夹爪指令返回失败无关。本次仅排查，未修改控制参数或发送运动指令。

## 17. 用户

现在夹爪的控制和扳机是如何映射的？我需要把扳机的程度映射到夹爪的开合度上，是否能做到？

## 18. 助手

**可以，而且现在已经按“扳机程度 → 夹爪开度”做了线性映射。问题在于实际夹爪追赶目标太慢。**

当前左扳机读数：松开约 **0.7**，按到底接近 **0**。默认夹爪范围是 **0–100mm**：

```text
夹爪目标开度 = 扳机读数 ÷ 0.7 × 100mm
```

| 扳机按压程度 | 扳机读数 | 夹爪目标开度 |
|---|---:|---:|
| 完全松开 | 0.70 | 100mm，张开 |
| 按下约一半 | 0.35 | 50mm |
| 接近按到底 | 0.07 | 10mm |
| 完全按到底 | 0.00 | 0mm，闭合 |

**这是位置对应，不是按多久就闭合多少。** 一直保持半按，目标就一直是 50mm；松开后，目标回到 100mm。

但目前程序没有直接把这个最终开度发给 SDK，而是每次只向目标靠近最多 **1mm**，并且最多领先实测位置 **1mm**，所以比例映射虽然正确，实际动作却不跟手。

要实现你想要的效果，应当**保留比例映射，调整后面的目标下发策略，让夹爪以受限速度追踪对应开度**。另外，你实测按到底时出现过 `0.031`，当前会对应约 **4.4mm**；若希望扣到底就完全闭合，还需要校准扳机端点并设置端点死区。

## 19. 用户

好的，给我调整脚本

## 20. 助手（进展）

我会保留“扳机程度对应夹爪开度”的映射，移除每次只能追赶 1mm 的限制，让 SDK 按设定速度追踪目标开度，并加入扳机端点死区，让扣到底能对应完全闭合。随后做离线测试，不启动机器人。

## 21. 助手（进展）

已改为直接发送映射开度：读数 ≤0.04 对应闭合，0.35 对应半开，≥0.66 对应张开。默认速度仍为 20mm/s，终端会同时显示目标开度和实测开度。

离线测试已确认，即使反馈尚未追上，程序也会发送正确的最终目标。这个改动也意味着：若通信中断且保持指令失败，夹爪可能继续执行此前的完整开合目标；相关提示和说明已更新。

## 22. 助手

已调整，继续使用原夹爪版命令：

```bash
bash teleoperation/run_arm7_direct_gripper.sh --execute --seconds 600 \
  --start-control left-button --align-speed-rad-s 0.4
```

现在直接向 SDK 发送扳机对应的开度，不再每次只追赶 1mm：

- 松开（≥0.66）→ **100mm**
- 半按（0.35）→ **50mm**
- 扣到底（≤0.04）→ **0mm**

默认速度仍为 **20mm/s**，终端同时显示目标和实测开度。进入 `FOLLOWING` 后仍需先松开扳机解锁。

**166 项离线测试通过，未启动机器人。** 新策略下，若断流且保持指令失败，夹爪可能继续执行此前的完整开合目标，不能依赖软件保证停住。

## 23. 用户

现在有两个问题，第一个我不明白为什么我在，第一次按压固定键进入低速跟随模式下，然后进入align\_ready状态的时候按压固定键，为什么不进入高速跟随状态？给我解释一下
第二个，一旦主臂角度超过限位，系统就停止遥操作了，能不能给我改成一旦主臂角度超过限位，就单纯限制活动角度就行了，不会停止遥操作，如何？

[附件：d1f0356c914709a0--pasted-text.txt](../../assets/d1f0356c914709a0--pasted-text.txt)

## 24. 助手（进展）

我先核对这次日志里的按键时序和状态变化，解释二次确认为何没有切换；再检查限位逻辑，把主臂越界改为目标角度限幅，同时保留从臂实测越界保护。

## 25. 助手（进展）

原因已经确认：本轮两次二次确认都碰上了“SDK 返回同一帧反馈”。当时仍是 `ALIGN_READY`，但代码把“这一轮没有新帧”直接当成确认失败并锁定了，并不一定是你按键方式有问题。我会让有效的二次确认短暂等待下一帧，最多 300ms；误差或其他条件不满足时仍不提速。

另外，你贴出的本轮最终退出原因是补偿后反馈年龄达到 **307.1ms**，不是关节越限。主臂越界限幅会单独修改，不会放宽这项反馈超时检查。

## 26. 助手（进展）

修改后的限幅行为已通过离线测试：例如 J7 主臂到 60°，从臂目标会保持在 45°；主臂回到 40°，目标就恢复为 40°，不会因主臂越界退出。启动前的对齐要求、从臂实测越界和其他异常保护仍保留。

二次确认测试也已通过：遇到重复反馈会等待下一帧；若等待期间误差重新超过 3°，则不会进入高速跟随。正在完成整体回归。

## 27. 助手

两个问题都查清并调整了。

**1. 为什么在 `ALIGN_READY` 按键却没有提速？**

本轮两次失败确认时，SDK 恰好返回了与上一轮相同的反馈帧。旧代码要求“按键这一轮必须有新帧”，否则直接停止锁定。**重复一帧不等于反馈已经过期，这是原确认逻辑过于严格。**

已修正：有效的二次确认遇到重复帧，会等待下一帧，最多 **300ms**。误差仍全部小于 3°、反馈检查通过后进入 `FOLLOWING`；提前按键、误差变大或超时仍不会提速。

**2. 主臂越限能否只限幅、不退出？**

已实现，适用于慢速和正式跟随。例如：

- J7 主臂到 **60°** → 从臂目标限制为 **45°**，继续遥操作。
- 主臂回到 **40°** → 目标恢复为 **40°**。

终端会显示限幅前后的目标。**从臂实测越界、超速、断流等保护仍保留**，启动前仍要求主从在范围内。

继续使用原命令即可：

```bash
bash teleoperation/run_arm7_direct_gripper.sh --execute --seconds 600 \
  --start-control left-button --align-speed-rad-s 0.4
```

**170 项离线测试通过，未启动机器人。** 另外，你贴出的本轮最终退出原因是反馈年龄 **307.1ms 超时**，不是关节越限；本次没有放宽时间戳保护。

## 28. 用户

刚刚我又运行了一次新版，这次应该没有啥问题了吧

[附件：82bd0a569f98fa8d--pasted-text.txt](../../assets/82bd0a569f98fa8d--pasted-text.txt)

## 29. 助手（进展）

我先核对这次终端输出和对应完整日志，确认退出原因、指令返回以及新版限幅和按键逻辑的表现。

## 30. 助手

**这轮主要功能都跑通了，日志没有异常中止。** 最后是收到急停信号后正常结束，`result=0`。

完整日志确认：

- **二次确认修复生效**：遇到重复帧后等待约 **11ms**，成功进入 `FOLLOWING`。
- **越界限幅生效**：J2、J6、J7 共触发 **11 次限幅**，均继续跟随，回到范围内后恢复正常映射。
- **SDK 指令全部成功**：机械臂 **2534 条**、夹爪 **254 条**。
- **夹爪完整开合已有反馈证据**：实测开度覆盖 **0～100mm**。
- **急停流程完成**：关节轨迹和底盘停止请求均成功，随后确认全身关节静止。夹爪保持请求成功，但未单独确认夹爪停稳。

还有一点需要区分：**功能跑通不代表跟踪完全无误差**。本轮状态采样中，“有效目标－实测”的最大瞬时误差约 **22.1°**，说明部分快速动作仍有明显滞后；它与程序检查的“已下发轨迹－实测”偏差不是同一个指标。

所以，这次可以确认之前的**二次确认、主臂越界退出、夹爪开合**问题已有实测改善；下一步主要看快速动作的跟手性。
