# 了解数采软件目录用途

- 归档编号：`01a06710-9ce5-75d1-95c5-6beb503bf8ae`
- 项目：无项目对话
- 创建时间：2026-09-03T19:38:57.125000+08:00
- 更新时间：2026-09-04T11:22:19.446000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

给我看下 /home/lsy03/Lsy03_document/仿真软件/ 这个目录下的软件是拿来做啥的？

## 2. 助手（进展）

我先查看这个目录的结构、说明文档和主要入口文件，再根据实际代码判断它的用途、运行方式和核心功能；全程只读，不会改动文件。

## 3. 助手（进展）

目录里不是已安装的软件，而是一套约 24GB 的 `SynthNova` 交付压缩包，另有一份 AES-256 加密备份和校验文件。初步看它包含机器人模型、场景资产、物理/渲染仿真、遥操作、运动规划等示例；我继续读取包内 README 和安装配置，确认适用对象与使用门槛。

## 4. 助手

这个目录里放的是一套 **Galbot（银河通用）机器人仿真与合成数据平台——SynthNova** 的交付包，不是普通桌面软件。

它主要用来：

- 在 **MuJoCo** 中进行机器人动力学、碰撞和执行器控制仿真。
- 在 **Isaac Sim** 中进行高质量场景渲染和多相机图像生成。
- 仿真 Galbot G1、S1 机器人的底盘、头部、升降机构、双臂和夹爪。
- 通过 **GP001 遥操设备**远程控制虚拟机器人。
- 使用 **CuRobo** 做逆运动学、避碰和抓取/搬运运动规划。
- 录制机器人状态为 HDF5 Snapshot，再生成头部、左右腕相机的 RGB 图片和 MP4 视频。
- 通过 Seed3D/Rodin 等服务从图片或文字生成 3D 物体，并整理成可仿真的 SimReady 资产。

附带的典型 Demo 包括：

1. 加载机器人、桌子、箱子等仿真场景。
2. 控制底盘、头部、双臂和夹爪。
3. 遥操机器人把箱子从一张桌子搬到另一张桌子。
4. 自动规划“抓箱—搬运—放箱”的完整动作。
5. 同步物理仿真和渲染仿真，生成相机训练数据。

目录中的文件用途：

- `synthnova_delivery_20260801_fixed.tar.gz`：可直接解压的完整交付包，约 24GB。
- `*.tar.gz.gpg`：同一交付包的 AES-256 加密版本。
- 两个 `*.sha256`：用于检查文件有没有损坏或被修改。

运行环境要求比较高：

- Ubuntu 22.04/24.04，x86_64
- NVIDIA RTX GPU，建议至少 16GB 显存
- 至少 32GB 内存
- Docker Engine 和 NVIDIA Container Toolkit
- 解压、导入和运行建议再预留 50GB 以上空间
- 运行时需要联网授权，并提供客户自己的 `ENCMGR_APP_ID` 和 `ENCMGR_APP_SECRET`

所以一句话概括：**这是一套面向具身智能/机器人开发的数字孪生仿真工具，用来验证机器人控制、遥操作、抓取规划，并批量生成视觉训练数据。**核心业务代码是加密的，只能在配套 Docker 环境和授权条件下运行。

## 5. 用户

给我代码，我来解压

## 6. 助手

使用未加密的 `.tar.gz` 解压即可。建议先校验，再解压到单独目录：

```bash
cd '/home/lsy03/Lsy03_document/仿真软件'

# 校验压缩包完整性
sha256sum -c synthnova_delivery_20260801_fixed.tar.gz.sha256

# 创建解压目录
mkdir -p extracted

# 解压
tar -xzf synthnova_delivery_20260801_fixed.tar.gz -C extracted

# 查看解压结果
ls -lah extracted/synthnova_delivery_20260801/delivery_dir
```

然后进行环境检查和启动：

```bash
cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'

chmod +x check_delivery.sh run_synthnova.sh

./check_delivery.sh
./run_synthnova.sh
```

建议确保该磁盘至少还有 **50GB 可用空间**。`.gpg` 是加密备份，不需要同时解压。

## 7. 用户

lsy03@lsy03-System-Product-Name:~/Lsy03_document/仿真软件$ cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'

chmod +x check_delivery.sh run_synthnova.sh

./check_delivery.sh
./run_synthnova.sh
SynthNova delivery check
Runtime image: synthnova-runtime:20260801

[PASS] 运行时镜像 SHA-256 完整性校验通过
[FAIL] 加密 SynthNova 目录不完整、已损坏或与运行脚本版本不一致
       expected: b7489528ddc4c682cecba6dbd3cf50bdcc15a67ed2f8a1ca7fd72dad0c4e37c9
       actual:   80c76fba42c90332fdf19f8906bb8f731570af9dc8f8121635cb8cf3d644681c
[PASS] CPU 架构：x86_64
[PASS] 操作系统：Ubuntu 22.04
[PASS] 内存：125 GB
[PASS] 交付目录所在磁盘剩余：731 GB
[PASS] GPU：NVIDIA GeForce RTX 4090，显存 24564 MB
[PASS] NVIDIA 驱动：595.84
[WARN] 未连接 GP001；仅 GP001 遥操示例不可用，不影响 Docker 和其他示例
[FAIL] 未安装 Docker Engine，请按照 README.md 安装

检查结果：2 个失败，1 个警告。
请根据 README.md 修复失败项后重新检查。
错误：未安装 Docker Engine
lsy03@lsy03-System-Product-Name:~/Lsy03_document/仿真软件/extracted/synthnova_de
livery_20260801/delivery_dir$


给我看下

## 8. 助手（进展）

结果里有两个独立问题：Docker 确实未安装；另一个是交付目录的内容哈希不匹配。我先检查校验脚本、清单文件和当前解压目录，判断是解压不完整、文件被改动，还是交付包自身的校验值有问题。只做读取，不修改。

## 9. 助手（进展）

我发现一个值得注意的异常：教程目录里的几段 `.mp4` 被解压成了“目标为乱码的符号链接”，这很可能正是目录哈希不一致的来源，也不像正常交付结果。接下来我会把原压缩包中的 `synthnova_delivery` 单独做一次干净对照解压，确认问题来自原包还是本次解压过程。

## 10. 助手（进展）

对照解压仍在进行中（这部分约 5.6GB，压缩包整体很大）。当前已确认运行时镜像本身校验通过，所以 GPU、驱动、内存和镜像文件都没问题；真正需要定位的是业务目录哈希与 Docker 环境。

## 11. 助手（进展）

对照结果已经确定：原始 24GB 压缩包的 SHA-256 校验成功，但从原包重新解出的业务目录仍得到同一个错误哈希 `80c76f…`，不是你解压操作造成的。也就是说，这份交付包内部的校验脚本/内容版本本身不一致；同时确认 Docker 和 NVIDIA Container Toolkit 都尚未安装。

## 12. 助手

检查完了，结论是：**不是你解压错了，而是交付包自身存在版本/打包问题。**

具体情况：

- 外层压缩包 SHA-256 校验成功，说明下载或复制没有损坏。
- 我从原压缩包重新单独解压业务目录，得到的仍然是：
  - 脚本期望：`b7489528...`
  - 实际内容：`80c76fba...`
- 教程中的 7 个 `.mp4` 被打包成了指向乱码目标的损坏符号链接，进一步说明交付包内容有异常。
- [check_delivery.sh](</home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir/check_delivery.sh>) 和 [run_synthnova.sh](</home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir/run_synthnova.sh>) 记录的是旧的 `b748...` 版本哈希。

另外，机器环境方面：

- CPU、Ubuntu、内存、RTX 4090 和显卡驱动都合格。
- GP001 未连接只影响遥操作示例，可以忽略。
- Docker Engine 和 NVIDIA Container Toolkit 均未安装。

建议先联系交付方，要求提供：

1. 哈希一致的重新打包版本；
2. 或确认正确业务目录哈希是否应为 `80c76fba42c90332fdf19f8906bb8f731570af9dc8f8121635cb8cf3d644681c`；
3. 确认那 7 个损坏的 MP4 链接是否属于打包错误。

**不建议直接把脚本中的期望哈希改成 `80c76f...` 来绕过检查**，因为目前确实发现了异常文件。Docker 可以先安装，但最好等交付方确认包的完整性后再正式运行。

## 13. 用户

是否可以使用模拟的哈希来绕过验证？

## 14. 助手

可以，但严格来说不是“模拟哈希”，而是**修改或跳过校验逻辑**。这只能让检查显示通过，不能修复异常文件。

根据当前脚本：

- `check_delivery.sh` 会计算并比较目录哈希。
- `run_synthnova.sh` **不会调用该校验脚本，也不会重新校验业务目录哈希**。
- 你现在真正阻塞启动的是 Docker 未安装，而不是哈希失败。

因此，测试启动时无需修改哈希：

```bash
# 安装 Docker 和 NVIDIA Container Toolkit 后
./run_synthnova.sh
```

不建议把期望值直接改成实际值，因为已经发现 7 个 MP4 是损坏的乱码符号链接。即使将：

```bash
EXPECTED_DELIVERY_SHA256="b748..."
```

替换为：

```bash
EXPECTED_DELIVERY_SHA256="80c76fba42c90332fdf19f8906bb8f731570af9dc8f8121635cb8cf3d644681c"
```

也只是让 `check_delivery.sh` “变绿”，不能证明软件完整。可以临时运行其他功能做测试，但不要把绕过后的结果当成正式验收通过。

## 15. 用户

Docker 未安装 如何解决？ 给我命令我来执行

## 16. 助手（进展）

我会按你当前的 Ubuntu 22.04 + NVIDIA 4090 环境，给一套官方仓库安装 Docker Engine 和 NVIDIA Container Toolkit 的命令，并包含安装后的 GPU 容器验证。先核对最新版官方步骤。

## 17. 助手

按下面顺序执行。适用于你的 Ubuntu 22.04 + RTX 4090，命令来自 [Docker 官方 Ubuntu 安装文档](https://docs.docker.com/engine/install/ubuntu/) 和 [NVIDIA Container Toolkit 官方文档](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/install-guide.html)。

### 1. 安装 Docker Engine

```bash
sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg2

sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources >/dev/null <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: jammy
Components: stable
Architectures: amd64
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt-get update

sudo apt-get install -y \
  docker-ce \
  docker-ce-cli \
  containerd.io \
  docker-buildx-plugin \
  docker-compose-plugin

sudo systemctl enable --now docker
```

验证 Docker：

```bash
sudo docker run --rm hello-world
```

### 2. 允许当前用户直接执行 Docker

SynthNova 的脚本直接调用 `docker`，因此需要把当前用户加入 Docker 用户组：

```bash
sudo usermod -aG docker "$USER"
```

然后注销当前桌面账号并重新登录。也可以临时在当前终端执行：

```bash
newgrp docker
```

验证无需 `sudo`：

```bash
docker version
docker info
```

注意：`docker` 用户组实际上具有接近 root 的系统权限。

### 3. 安装 NVIDIA Container Toolkit

```bash
curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey |
  sudo gpg --dearmor --yes \
    -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg

curl -fsSL \
  https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list |
  sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' |
  sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list >/dev/null

sudo apt-get update
sudo apt-get install -y nvidia-container-toolkit

sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
```

### 4. 验证容器能够访问显卡

NVIDIA 官方推荐用下面的测试命令：

```bash
sudo docker run --rm --runtime=nvidia --gpus all ubuntu nvidia-smi
```

正常情况下会显示 RTX 4090、驱动版本和显存信息。

### 5. 重新运行 SynthNova

```bash
cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'

./check_delivery.sh
./run_synthnova.sh
```

首次启动会导入约 22GB 的 Docker 镜像，可能需要几分钟。检查时业务目录哈希问题仍会存在，但 Docker 和 NVIDIA runtime 两项应该通过。`run_synthnova.sh` 会交互式要求输入授权的 App ID 和 Secret。

## 18. 用户

lsy03@lsy03-System-Product-Name:~$ sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg2

sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources >/dev/null <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: jammy
Components: stable
Architectures: amd64
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt-get update

sudo apt-get install -y \
  docker-ce \
  docker-ce-cli \
  containerd.io \
  docker-buildx-plugin \
sudo systemctl enable --now docker
[sudo] lsy03 的密码：
[REDACTED] 完成
E: 无法获得锁 /var/lib/apt/lists/lock。锁正由进程 474542（apt-get）持有
N: 请注意，直接移除锁文件不一定是合适的解决方案，且可能损坏您的系统。
E: 无法对目录 /var/lib/apt/lists/ 加锁
正在读取软件包列表... 完成
正在分析软件包的依赖关系树... 完成
正在读取状态信息... 完成
ca-certificates 已经是最新版 (20260601~22.04.1)。
gnupg2 已经是最新版 (2.2.27-3ubuntu2.5)。
建议安装：
  libcurl4-doc libidn11-dev libkrb5-dev libldap2-dev librtmp-dev libssh2-1-dev
下列软件包将被升级：
  curl libcurl4 libcurl4-openssl-dev
升级了 3 个软件包，新安装了 0 个软件包，要卸载 0 个软件包，有 481 个软件包未被升级。
需要下载 194 kB/873 kB 的归档。
解压缩后会消耗 0 B 的额外空间。
获取:1 http://mirrors.aliyun.com/ubuntu jammy-updates/main amd64 curl amd64 7.81.0-1ubuntu1.27 [194 kB]
已下载 194 kB，耗时 1秒 (315 kB/s)
(正在读取数据库 ... 系统当前共安装有 271058 个文件和目录。)
准备解压 .../libcurl4-openssl-dev_7.81.0-1ubuntu1.27_amd64.deb  ...
正在解压 libcurl4-openssl-dev:amd64 (7.81.0-1ubuntu1.27) 并覆盖 (7.81.0-1ubuntu1.26) ...
准备解压 .../curl_7.81.0-1ubuntu1.27_amd64.deb  ...
正在解压 curl (7.81.0-1ubuntu1.27) 并覆盖 (7.81.0-1ubuntu1.26) ...
准备解压 .../libcurl4_7.81.0-1ubuntu1.27_amd64.deb  ...
正在解压 libcurl4:amd64 (7.81.0-1ubuntu1.27) 并覆盖 (7.81.0-1ubuntu1.26) ...
正在设置 libcurl4:amd64 (7.81.0-1ubuntu1.27) ...
正在设置 curl (7.81.0-1ubuntu1.27) ...
正在设置 libcurl4-openssl-dev:amd64 (7.81.0-1ubuntu1.27) ...
正在处理用于 man-db (2.10.2-1) 的触发器 ...
正在处理用于 libc-bin (2.35-0ubuntu3.6) 的触发器 ...
curl: (35) OpenSSL SSL_connect: 连接被对方重置 in connection to download.docker.com:443
chmod: 无法访问 '/etc/apt/keyrings/docker.asc': 没有那个文件或目录
正在读取软件包列表... 完成
E: 无法获得锁 /var/lib/apt/lists/lock。锁正由进程 474542（apt-get）持有
N: 请注意，直接移除锁文件不一定是合适的解决方案，且可能损坏您的系统。
E: 无法对目录 /var/lib/apt/lists/ 加锁
正在读取软件包列表... 完成
正在分析软件包的依赖关系树... 完成
正在读取状态信息... 完成
没有可用的软件包 docker-ce，但是它被其它的软件包引用了。
这可能意味着这个缺失的软件包可能已被废弃，
或者只能在其他发布源中找到

E: 软件包 docker-ce 没有可安装候选
E: 无法定位软件包 docker-ce-cli
E: 无法定位软件包 containerd.io
E: 无法按照 glob ‘containerd.io’ 找到任何软件包
E: 无法按照正则表达式 containerd.io 找到任何软件包
E: 无法定位软件包 docker-buildx-plugin
E: 无法定位软件包 docker-compose-plugin
Failed to enable unit: Unit file docker.service does not exist.
lsy03@lsy03-System-Product-Name:~$

## 19. 助手（进展）

这里是三个连锁问题：后台另一个 `apt-get` 正占用锁；访问 `download.docker.com` 时 TLS 被重置，导致仓库密钥没有下载；因此 Docker 官方仓库没有生效，后续所有 `docker-ce` 包自然都找不到。另外你粘贴的最后一段少了一行，反斜杠把 `sudo systemctl...` 也接到了安装命令里。

我先确认 Ubuntu 镜像源能提供的 Docker 版本以及 NVIDIA 工具包是否已有候选，然后给你一套避开 `download.docker.com` 的命令。

## 20. 助手

后台占锁的 `apt-get` 进程已经结束。由于 `download.docker.com` 连接被重置，直接改用你当前阿里云 Ubuntu 源里的 `docker.io`，候选版本是 `29.1.3`，满足 SynthNova 要求。

按模块逐段执行，某段报错就先停下。

### 1. 禁用未配置成功的 Docker 官方源

这是可恢复操作，没有删除文件：

```bash
sudo mv \
  /etc/apt/sources.list.d/docker.sources \
  /etc/apt/sources.list.d/docker.sources.disabled
```

### 2. 从 Ubuntu/阿里云源安装 Docker

```bash
sudo apt-get update
sudo apt-get install -y docker.io

sudo systemctl enable --now docker
sudo systemctl status docker --no-pager
```

检查版本：

```bash
sudo docker version
```

### 3. 允许当前用户直接运行 Docker

```bash
sudo usermod -aG docker "$USER"
newgrp docker
```

`newgrp docker` 会进入一个新的 shell。然后验证：

```bash
docker version
docker info
```

### 4. 添加 NVIDIA Container Toolkit 仓库

先下载密钥：

```bash
curl -fsSL \
  https://nvidia.github.io/libnvidia-container/gpgkey \
  -o /tmp/nvidia-container-toolkit.gpgkey
```

只有上面成功后，再执行：

```bash
sudo gpg --dearmor --yes \
  -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg \
  /tmp/nvidia-container-toolkit.gpgkey
```

添加仓库：

```bash
curl -fsSL \
  https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list \
  -o /tmp/nvidia-container-toolkit.list
```

只有下载成功后，再执行：

```bash
sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' \
  /tmp/nvidia-container-toolkit.list |
  sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list >/dev/null

sudo apt-get update
sudo apt-get install -y nvidia-container-toolkit
```

以上步骤对应 [NVIDIA 官方安装说明](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/install-guide.html)。

### 5. 将 NVIDIA runtime 注册到 Docker

```bash
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
```

检查是否存在 `nvidia` runtime：

```bash
docker info --format '{{json .Runtimes}}'
```

结果里应该能看到 `"nvidia"`。

如果 Docker Hub 网络正常，可进一步测试 GPU：

```bash
sudo docker run --rm --runtime=nvidia --gpus all ubuntu nvidia-smi
```

该测试命令来自 [NVIDIA 官方验证说明](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/sample-workload.html)。如果只是拉取 `ubuntu` 镜像失败，通常是 Docker Hub 网络问题，不代表显卡配置失败。

### 6. 重新检查并启动 SynthNova

```bash
cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'

./check_delivery.sh
./run_synthnova.sh
```

首次运行会从本地导入约 22GB 的镜像，无需从 Docker Hub 下载。目录哈希那一项仍可能失败，这是之前确认的交付包问题；Docker 和 NVIDIA runtime 两项应该转为通过。

## 21. 用户

[附件：48ad1afb5a2fe0f7--pasted-text.txt](../../assets/48ad1afb5a2fe0f7--pasted-text.txt)

## 22. 助手（进展）

我先读取你贴出的完整终端输出，确认 Docker、用户组和 NVIDIA runtime 分别进行到了哪一步，再给你下一条准确命令。

## 23. 助手（进展）

Docker 已经安装成功，服务正常，当前用户也能免 `sudo` 使用。剩下只有 NVIDIA Container Toolkit：它的仓库文件已写入，但那次 `apt-get update` 被锁打断，所以 apt 尚未读取仓库索引，`nvidia-ctk` 自然还不存在。我检查一下锁现在是否释放，以及 NVIDIA 源文件是否有效。

## 24. 助手

Docker 已完全安装成功：

- Docker 29.1.3 正常运行
- 当前用户可以免 `sudo` 使用
- Docker 服务已开机启动

NVIDIA 仓库和密钥也已经配置好，只是 `apt-get update` 当时被锁中断。现在锁进程已经结束，直接执行：

```bash
sudo apt-get update -o Acquire::Retries=3
```

确认输出中出现类似 `nvidia.github.io/libnvidia-container`，然后安装：

```bash
apt-cache policy nvidia-container-toolkit

sudo apt-get install -y nvidia-container-toolkit
```

配置 NVIDIA runtime：

```bash
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
```

验证：

```bash
command -v nvidia-ctk
docker info --format '{{json .Runtimes}}'
```

第二条输出中应该出现 `"nvidia"`。然后测试 GPU 容器：

```bash
docker run --rm --runtime=nvidia --gpus all ubuntu nvidia-smi
```

如果拉取 `ubuntu` 镜像因网络失败，可以暂时跳过，直接重新检查 SynthNova：

```bash
cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'

./check_delivery.sh
```

如果 `sudo apt-get update` 再次提示锁被占用，不要删除锁文件，执行：

```bash
ps -ef | grep -E '[a]pt|[d]pkg'
```

把输出发给我再判断。

## 25. 用户

正在读取状态信息... 完成
E: 无法定位软件包 nvidia-container-toolkit
lsy03@lsy03-System-Product-Name:~$ sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
sudo: nvidia-ctk：找不到命令
lsy03@lsy03-System-Product-Name:~$ sudo apt-get update -o Acquire::Retries=3
正在读取软件包列表... 完成
E: 无法获得锁 /var/lib/apt/lists/lock。锁正由进程 474542（apt-get）持有
N: 请注意，直接移除锁文件不一定是合适的解决方案，且可能损坏您的系统。
E: 无法对目录 /var/lib/apt/lists/ 加锁
lsy03@lsy03-System-Product-Name:~$

## 26. 助手（进展）

这个锁现在反复由同一个 PID `474542` 占用，不能继续盲等或删除锁文件。我要先确认该进程到底是正常的系统更新、卡死的旧 `apt-get`，还是处于不可中断状态，再决定安全处理方式。

## 27. 助手

同一个 PID `474542` 一直占锁，说明另一条 `apt-get` 仍在后台运行或卡住。**不要删除锁文件。**

先在你的终端执行：

```bash
ps -o pid,ppid,user,tty,lstart,etime,stat,cmd -p 474542
sudo lsof /var/lib/apt/lists/lock
```

如果显示它正在正常下载或安装，就关闭其他“软件更新器/终端”，或者等待它完成。可以让当前命令最多等待锁 5 分钟：

```bash
sudo apt-get -o DPkg::Lock::Timeout=300 update
```

如果 PID `474542` 已经卡住超过 10 分钟、没有任何下载或磁盘活动，先温和终止：

```bash
sudo kill -TERM 474542
sleep 5
ps -p 474542
```

如果最后一条没有输出，说明进程已经结束。修复可能未完成的软件包状态：

```bash
sudo dpkg --configure -a
sudo apt-get -f install
```

然后重新更新并安装：

```bash
sudo apt-get update -o Acquire::Retries=3
apt-cache policy nvidia-container-toolkit
sudo apt-get install -y nvidia-container-toolkit
```

安装成功后继续：

```bash
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker

docker info --format '{{json .Runtimes}}'
```

输出中应包含 `"nvidia"`。如果 `sudo kill -TERM 474542` 后该进程仍存在，先不要使用 `kill -9`，把 `ps` 和 `lsof` 的输出发来确认。

## 28. 用户

lsy03@lsy03-System-Product-Name:~$ ps -o pid,ppid,user,tty,lstart,etime,stat,cmd -p 474542
sudo lsof /var/lib/apt/lists/lock
    PID    PPID USER     TT                        STARTED     ELAPSED STAT CMD
 474542  474514 root     ?        Fri Aug 28 07:45:03 2026  7-02:01:02 S    apt-
lsof: WARNING: can't stat() fuse.gvfsd-fuse file system /run/user/1000/gvfs
      Output information may be incomplete.
lsof: WARNING: can't stat() fuse.portal file system /run/user/1000/doc
      Output information may be incomplete.
COMMAND    PID USER   FD   TYPE DEVICE SIZE/OFF     NODE NAME
apt-get 474542 root    4uW  REG  259,2        0 56098891 /var/lib/apt/lists/lock
lsy03@lsy03-System-Product-Name:~$

## 29. 助手

确认了：这是一个已经卡住 **7 天**的 `apt-get` 进程，状态为休眠，并一直独占软件源锁。可以安全地先温和终止它。

执行：

```bash
sudo kill -TERM 474542
sleep 5
ps -p 474542
```

如果 `ps` 仍然显示该进程，再强制结束：

```bash
sudo kill -KILL 474542
sleep 2
ps -p 474542
```

然后修复软件包状态。**不需要删除任何 lock 文件：**

```bash
sudo dpkg --configure -a
sudo apt-get -f install
```

重新刷新 NVIDIA 仓库并安装：

```bash
sudo apt-get update -o Acquire::Retries=3
apt-cache policy nvidia-container-toolkit
sudo apt-get install -y nvidia-container-toolkit
```

配置 NVIDIA runtime：

```bash
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
```

最后验证：

```bash
command -v nvidia-ctk
docker info --format '{{json .Runtimes}}'
```

输出中看到 `"nvidia"` 即配置成功。`lsof` 关于 `gvfs` 和 `fuse.portal` 的警告与本问题无关，可以忽略。

## 30. 用户

lsy03@lsy03-System-Product-Name:~$ sudo kill -TERM 474542
sleep 5
ps -p 474542
    PID TTY          TIME CMD
lsy03@lsy03-System-Product-Name:~$ sudo dpkg --configure -a
sudo apt-get -f install
正在读取软件包列表... 完成
正在分析软件包的依赖关系树... 完成
正在读取状态信息... 完成
升级了 0 个软件包，新安装了 0 个软件包，要卸载 0 个软件包，有 481 个软件包未被升级。
lsy03@lsy03-System-Product-Name:~$ sudo apt-get update -o Acquire::Retries=3
apt-cache policy nvidia-container-toolkit
sudo apt-get install -y nvidia-container-toolkit
正在读取软件包列表... 完成
E: 无法获得锁 /var/lib/apt/lists/lock。锁正由进程 750765（apt-get）持有
N: 请注意，直接移除锁文件不一定是合适的解决方案，且可能损坏您的系统。
E: 无法对目录 /var/lib/apt/lists/ 加锁
N: 无法定位软件包 nvidia-container-toolkit
正在读取软件包列表... 完成
正在分析软件包的依赖关系树... 完成
正在读取状态信息... 完成
E: 无法定位软件包 nvidia-container-toolkit
lsy03@lsy03-System-Product-Name:~$

## 31. 助手

现在是新的 PID `750765` 占锁，说明有后台自动更新任务重新启动了 `apt-get`。先确认它的来源：

```bash
ps -ww -o pid,ppid,user,tty,lstart,etime,stat,wchan:30,cmd -p 750765
sudo tr '\0' ' ' </proc/750765/cmdline
echo
sudo cat /proc/750765/cgroup
```

如果输出显示 `apt.systemd.daily`、`apt-daily` 或普通的 `apt-get update`，可以临时停止自动更新并结束它：

```bash
sudo systemctl stop apt-daily.timer apt-daily-upgrade.timer
sudo systemctl stop apt-daily.service apt-daily-upgrade.service

sudo kill -TERM 750765
sleep 5
ps -p 750765
```

确认最后没有显示进程后，逐条执行；不要一次性粘贴全部后不看错误：

```bash
sudo dpkg --configure -a
```

```bash
sudo apt-get update -o Acquire::Retries=3
```

只有 `apt-get update` 成功完成后，再检查候选版本：

```bash
apt-cache policy nvidia-container-toolkit
```

如果显示 `Candidate`/“候选”版本，再安装：

```bash
sudo apt-get install -y nvidia-container-toolkit
```

接着配置：

```bash
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
docker info --format '{{json .Runtimes}}'
```

完成后恢复自动更新定时器：

```bash
sudo systemctl start apt-daily.timer apt-daily-upgrade.timer
```

如果 `750765` 的完整命令不是 `apt-get update`，先别杀，把前三条诊断命令的输出发来。

## 32. 用户

你给我执行吧

## 33. 助手（进展）

我来处理。先在宿主机确认 PID `750765` 的完整命令和所属服务；若确实是卡住的 apt 更新，我会停止相关自动更新任务、修复 apt 状态、安装 NVIDIA Container Toolkit、配置 Docker runtime，并做验证。不会删除锁文件。

## 34. 助手（进展）

宿主机提权审批已通过，但系统的 `sudo` 仍要求在真实终端输入你的密码；我不能读取或代填密码。为了让你只操作一次，我会生成一个自动修复脚本：它会暂停 apt 自动更新、等待/处理锁、安装 NVIDIA 工具包、配置 Docker，并在退出时恢复自动更新。你只需在自己的终端用 `sudo` 运行它。

## 35. 助手

我已经生成并校验了自动修复脚本。由于 `sudo` 密码只能在你的真实终端输入，请执行这一条：

```bash
sudo bash '/home/lsy03/Documents/Codex/2026-09-03/gei/outputs/fix_nvidia_docker.sh'
```

脚本会自动：

- 暂停 apt 自动更新任务；
- 等待软件源锁释放；
- 修复 apt/dpkg 状态；
- 安装 NVIDIA Container Toolkit；
- 注册 NVIDIA Docker runtime；
- 重启并验证 Docker；
- 最后恢复 apt 自动更新定时器。

脚本文件：[fix_nvidia_docker.sh](/home/lsy03/Documents/Codex/2026-09-03/gei/outputs/fix_nvidia_docker.sh)

执行完成后把最后一段输出发给我。

## 36. 用户

[附件：f1d778894f11ebba--pasted-text.txt](../../assets/f1d778894f11ebba--pasted-text.txt)

## 37. 助手（进展）

我检查这次脚本的完整输出，确认安装、runtime 注册和最终验证是否全部成功。

## 38. 助手

安装成功了：

- NVIDIA Container Toolkit：`1.20.0`
- `nvidia-ctk`：`/usr/bin/nvidia-ctk`
- Docker：`29.1.3`
- Docker 已注册 `"nvidia"` runtime

现在运行 SynthNova：

```bash
cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'

./check_delivery.sh
```

业务目录哈希预计仍会失败，这是交付包自身的问题；Docker 和 NVIDIA runtime 应当通过。

如果确认仍要继续测试，直接启动：

```bash
./run_synthnova.sh
```

首次启动会：

1. 导入约 22GB 的本地 Docker 镜像；
2. 要求输入 `ENCMGR_APP_ID`；
3. 要求输入 `ENCMGR_APP_SECRET`；
4. 创建并启动 `synthnova` 容器。

导入可能需要几分钟。授权密钥不要发到聊天里，直接在终端提示处输入即可。

## 39. 用户

明确一下我们的目的，和我们接下来的计划

## 40. 助手

我们的核心目的不是单纯“绕过报错”，而是：

1. 在这台 RTX 4090 工作站上把 SynthNova 正常启动。
2. 区分环境问题与交付包问题。
3. 完成一个最小仿真 Demo，确认物理仿真、渲染和 GPU 都能工作。
4. 保留对交付包完整性的判断，避免把“能启动”误认为“交付验收通过”。

当前状态：

- Docker Engine：已安装并正常运行。
- NVIDIA Container Toolkit：已安装。
- Docker NVIDIA runtime：已注册成功。
- CPU、内存、GPU、驱动、磁盘：满足要求。
- GP001：未连接，只影响遥操作。
- 交付目录：哈希不一致，并存在 7 个异常 MP4 链接；这是交付包问题，尚不能正式验收。

接下来按这个顺序进行：

### 第一步：重新检查环境

```bash
cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'

./check_delivery.sh
```

预期结果：

- Docker、NVIDIA runtime、GPU 应通过。
- GP001 仍是警告，可以忽略。
- 业务目录哈希仍可能失败，暂时记录下来。

### 第二步：测试启动 SynthNova

```bash
./run_synthnova.sh
```

它会导入约 22GB 的本地镜像，并要求输入授权账号。密钥只在终端输入，不发到聊天中。

启动后查看状态：

```bash
./run_synthnova.sh --status
docker ps
```

### 第三步：验证服务

检查这几个本地页面：

- Assets Manager：`http://127.0.0.1:18000`
- Physics Simulator 文档：`http://127.0.0.1:18001`
- Render Simulator 文档：`http://127.0.0.1:18002`
- 教学网站：`http://127.0.0.1:19000`

### 第四步：运行最小仿真

进入容器后执行：

```bash
cd /workspace/synthnova/example/02_simulation_scene/01_physics_simulator_usage

python 00_g1_default_scene.py --steps 300
```

成功标准是：机器人场景能够加载、仿真完成 300 步、没有 GPU/runtime/授权错误。

### 最终判断

- 如果容器和基础 Demo 正常：说明软件主要功能可以测试使用。
- 如果授权或加密模块报错：收集日志，联系交付方。
- 即使能够运行，正式验收前仍应让交付方解释哈希不一致并补发损坏的媒体文件。

因此眼下最直接的下一步就是：**重新运行 `./check_delivery.sh`，把新结果发来，然后启动容器。**

## 41. 用户

你帮我运行好了

## 42. 助手（进展）

我来把它启动到可验证状态：先检查 Docker/GPU runtime、镜像与现有容器，再运行交付检查；环境没问题后导入本地 22GB 镜像并启动容器。授权账号若尚未配置，我会停在需要你本人输入凭据之前，不会读取或输出密钥。

## 43. 助手（进展）

检查结果确认：Docker 与 NVIDIA runtime 都正常；镜像尚未导入、容器尚未创建，并且当前环境没有授权账号变量。我先运行完整交付检查，随后可以先导入镜像；真正创建业务容器仍需要你的 App ID/Secret。

## 44. 助手（进展）

运行时镜像的 SHA-256 已通过，正在计算 5.6GB 业务目录的逐文件哈希。完成后我会根据结果决定是否继续导入镜像；已知的交付包哈希异常不会被静默修改。

## 45. 助手（进展）

完整检查已结束：除已确认的业务目录哈希问题和未连接 GP001 外，宿主环境全部通过。我现在先导入本地 22GB 运行时镜像并核对镜像 ID、GPU 可见性；这一步不需要授权密钥。之后创建 SynthNova 容器时才需要凭据。

## 46. 助手（进展）

镜像仍在导入，22GB 的 `docker load` 通常会长时间没有进度输出，这是正常现象。导入过程中我不会中断它。

## 47. 助手（进展）

导入还在进行，当前没有报错。这个镜像解包后会占用较多 Docker 存储，耗时主要取决于磁盘写入与校验速度。

## 48. 助手（进展）

镜像导入已持续约 3 分钟，进程仍在正常运行且没有错误输出。我继续等待完成，然后立即核对它是否正是脚本要求的镜像 ID。

## 49. 助手

我已经完成了无需凭据的全部操作：

- 已导入 22GB 镜像 `synthnova-runtime:20260801`
- 镜像 ID 与交付脚本要求完全一致：
  `sha256:713b0beb95e4196d8650cb88a6bfc3cc3827be9a8559d146e5375eb96cba272f`
- 容器内已成功识别 RTX 4090
- NVIDIA runtime 工作正常
- 镜像内容结构检查通过

现在只差授权凭据。为了避免 App Secret 出现在聊天或工具日志中，请在你自己的终端执行：

```bash
cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'
./run_synthnova.sh
```

然后按提示输入：

- `ENCMGR_APP_ID`
- `ENCMGR_APP_SECRET`

不要把凭据发给我。脚本启动完成后告诉我，或者把不含凭据的终端输出贴过来，我再帮你检查容器、四个网站和最小仿真 Demo。

## 50. 用户

lsy03@lsy03-System-Product-Name:~/Lsy03_document/仿真软件/extracted/synthnova_de
livery_20260801/delivery_dir$ cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'
./run_synthnova.sh
请输入客户 ENCMGR_APP_ID:

这个有没有办法绕开？

## 51. 助手

这个不能安全、合法地绕开。`ENCMGR_APP_ID` 和 `ENCMGR_APP_SECRET` 是加密业务模块的授权凭据，不只是启动脚本里的形式检查；即使删掉输入提示，容器内的加密代码仍可能因授权失败而无法运行。

现在可按 `Ctrl+C` 退出。正确做法是向交付方索取：

- 客户 App ID 和 Secret；
- 或官方试用账号；
- 或离线授权方案；
- 同时确认业务目录哈希不一致的问题。

可以直接发给交付方：

> 请提供 SynthNova 20260801 交付版本所需的 ENCMGR_APP_ID / ENCMGR_APP_SECRET，或离线/试用授权方式。另外，交付脚本期望目录哈希为 b7489528…，实际为 80c76fba…，请确认或补发正确交付包。

拿到凭据后重新运行 `./run_synthnova.sh` 即可，不要把 Secret 发到聊天里。

## 52. 用户

能否看到我新插进来的USB？

## 53. 助手（进展）

可以。我会检查当前 USB 总线、USB 存储设备、串口设备，以及最近几分钟的内核插拔记录，判断新插入的是 U 盘、串口控制器还是其他 USB 设备。只读取状态。

## 54. 助手（进展）

我目前能看到一个新接入的 USB 大容量设备：`SanDisk 3.2Gen1`，设备节点是 `/dev/sdb`，但系统报告容量为 `0B`，没有分区、文件系统或挂载点；这通常表示读卡器没插介质、U 盘初始化失败，或设备刚插入尚未正确枚举。另外还有一个 USB RNDIS 网络设备。接下来读取宿主机 USB 标识和内核日志，确认具体是哪一个刚插入及为何是 0B。

## 55. 助手

能看到。目前检测到两个可能是你新插入的 USB 设备：

- `vivo Y50`：USB ID `2d95:600a`，当前以 RNDIS USB 网络设备工作，不是存储盘。
- `SanDisk 3.2Gen1`：USB ID `0781:55a3`，设备节点 `/dev/sdb`。

但 SanDisk 当前状态异常：

- 容量：`0B`
- 无分区
- 无文件系统
- 未挂载
- 内核识别到设备，但无法读取存储容量

另外，当前仍然**没有检测到 GP001 所需的 `/dev/serial/by-id/...` 串口设备**。

如果你刚插入的是 SanDisk，建议拔下后换一个 USB 口重新插入；不要对 `/dev/sdb` 执行格式化。插好后告诉我，我可以立即重新检查。

## 56. 用户

另一个 YUNTENG 的设备是否能检测？

## 57. 助手（进展）

我重新扫描 USB 总线和 HID 输入设备，重点查找厂商名、产品名或序列号中包含 `YUNTENG` 的设备，并检查它是否只以通用键盘/遥控器身份出现。

## 58. 助手（进展）

系统没有出现名为 `YUNTENG` 的 USB 描述符，但刚刚新增识别到一个非常关键的设备：`Galbot Remote Operate ComPort`（USB ID `0483:5745`，设备号 75），很可能就是你连接的遥操作设备。现在确认它对应的串口节点及是否满足 SynthNova 的 GP001 命名规则。

## 59. 助手

检测结果：

- 没有发现名称或描述中包含 `YUNTENG` 的 USB/HID/已配对蓝牙设备。
- 新检测到的是 Galbot 遥操设备：
  - USB ID：`0483:5745`
  - 名称：`Galbot Remote Operate ComPort`
  - 串口：`/dev/ttyACM0`
  - SynthNova GP001 检测：成功

对应链接：

```text
/dev/serial/by-id/usb-Galbot_Galbot_Remote_Operate_ComPort_206F33685052-if00
→ /dev/ttyACM0
```

所以现在 GP001 已经可以被 SynthNova 使用；如果 `YUNTENG` 是另一台独立设备，它目前没有作为 USB 数据设备出现。若它是蓝牙遥控器，USB 接口可能仅用于充电，需要通过蓝牙配对。

## 60. 用户

我能否直接读取串口的数据？

## 61. 助手（进展）

可以，但有两点要注意：直接读取会“消费”串口字节，可能与 SynthNova 遥操程序抢数据；而且原始内容大概率是二进制协议，不会直接显示成可读文字。我先确认串口是否被占用、当前权限和项目里规定的波特率/协议，再做一个几秒钟的只读采样。

## 62. 助手（进展）

当前串口未被任何进程占用，但你的账号不在 `dialout` 组，所以宿主机直接访问会报“权限不够”。我先通过已有 Docker 权限对 `/dev/ttyACM0` 做 5 秒只读采样，不修改设备文件或启动 SynthNova。采样期间请拨动一次摇杆或按一个按钮，便于判断它是否主动发送数据。

## 63. 助手

可以，已经成功直接读取了 `/dev/ttyACM0`：

- 5 秒内读到了至少 512 字节。
- 数据是持续输出的二进制帧，不是可读文本。
- 内容看起来包含浮点数、状态位和重复帧头，可能对应机械臂、摇杆、按键等遥操状态。
- 需要 GP001 协议或 `remote_control_lite` 才能正确解析字段。

目前你的账号不在 `dialout` 组。要在宿主机直接读取，执行：

```bash
sudo usermod -aG dialout "$USER"
newgrp dialout
```

然后可以查看十六进制数据：

```bash
timeout 5s dd if=/dev/ttyACM0 bs=512 count=1 status=none |
  od -An -v -tx1
```

注意：

- 不要在 SynthNova 遥操程序运行时同时读取，否则两个程序会抢串口数据。
- 本次读取的是 `Galbot Remote Operate ComPort`，不是 YUNTENG；YUNTENG 当前没有对应的串口节点。
- 原始字节可以读取，但在缺少协议说明时无法可靠判断每个字段的含义。

## 64. 用户

GP001 协议或 remote_control_lite 是啥？能不能给我破解了？

## 65. 助手（进展）

可以解释并帮你做合规接入，但不能破解授权、解密受保护源码或绕过访问控制。为了给你说清楚，我会只读取交付包里明确提供的明文遥操示例，整理 `remote_control_lite` 的公开调用方式和 GP001 数据流，不分析或还原加密模块。

## 66. 助手

GP001 协议是遥操设备通过 USB 串口发送的二进制数据格式，定义了帧结构、机械臂关节值、摇杆轴、按键、急停状态等。刚才读到的原始十六进制数据就是这种数据流。

`remote_control_lite` 是 SynthNova 提供的解析/驱动模块，作用是：

```python
sink = QueueSink(maxsize=8)
driver = GalbotDriver(robot="golf", port="/dev/ttyACM0")
driver.attach_sink(sink)
frame = sink.get()
```

它将原始数据转换为类似：

```python
frame["joint_states"]["left_arm"]["position"]
frame["joint_states"]["right_arm"]["position"]
frame["action_list"]["buttons"]
frame["action_list"]["axes"]
```

明文示例还表明：

- `axes[0:3]`：底盘前后、横移、旋转
- `axes[3:5]`：左右扳机/夹爪
- `axes[5:8]`：升降、前后调整、偏航
- 按键动作包含急停、恢复、夹爪开合、底盘启停等

但我不能帮你破解加密模块、绕过授权，或反向还原受保护的私有协议。这也违反该交付包附带的禁止反向工程声明。

我可以继续协助的合规方向是：

- 基于明文示例使用 `GalbotDriver` 和 `QueueSink`；
- 写一个授权环境下的 GP001 状态监视器；
- 保存原始串口数据用于你方内部诊断；
- 如果厂商提供协议文档，按文档实现独立解析器；
- 向厂商申请 SDK、协议说明或离线授权。

## 67. 用户

lsy03@lsy03-System-Product-Name:~$ cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'
./run_synthnova.sh
请输入客户 ENCMGR_APP_ID:
这个授权凭据是在哪里能拿到？现有资料文件夹之中没有吗

## 68. 助手（进展）

我会在交付目录里检查凭据相关的文件名、环境变量引用和说明文档，但只列出“是否存在/在哪引用”，不会打印任何可能的密钥内容。根据 README 的初步说明，这套包明确不内置客户 App ID 或 Secret，我再做一次完整确认。

## 69. 助手

确认过了：**现有交付资料里没有 `ENCMGR_APP_ID` 或 `ENCMGR_APP_SECRET`。**

交付包的 README 明确写着：

> 交付物中不内置客户 appid 或 secret。

目录中也没有发现 `.env`、credential、auth 或授权配置文件。出现相关字段的地方只有：

- `README.md`：说明需要客户自己的联网授权账号；
- `run_synthnova.sh`：读取凭据并传进 Docker 容器。

这组凭据需要从以下渠道获取：

- Galbot / SynthNova 交付负责人；
- 你所在项目的管理员或采购联系人；
- 已购买授权的客户账号管理员；
- 申请官方试用或离线授权。

它不是 GPG 密码、Docker 密码、Seed3D API Key，也不是 GP001 设备序列号。

可以向交付方发送：

> 我们已完成 SynthNova 20260801 环境部署，Docker、NVIDIA runtime 和 GP001 均已识别。启动需要 ENCMGR_APP_ID 与 ENCMGR_APP_SECRET，请提供该项目对应的联网授权凭据，或说明试用/离线授权申请方式。同时请确认交付目录哈希不一致问题。

当前提示处按 `Ctrl+C` 退出即可。拿到凭据后再次执行 `./run_synthnova.sh`，Secret 会以隐藏方式输入。

## 70. 用户

# SynthNova 交付说明

**Copyright © 2026 Galbot. All rights reserved.**

> **使用前请先阅读 [`使用与安全声明.md`](使用与安全声明.md)。** 本交付物仅限
> 获得授权的客户使用；未经许可，不得进行反向工程、反编译或反汇编。

交付目录：

```text
delivery_dir/
├── synthnova_runtime_20260801.tar    # 纯运行时 Docker 镜像
├── synthnova_delivery/                    # 加密代码、资产、明文示例和网站
├── check_delivery.sh                      # 完整性与宿主机检查
├── run_synthnova.sh                       # 持久容器启动/进入工具
├── README.md
└── 使用与安全声明.md                      # 授权边界与禁止行为
```

运行时镜像 `synthnova-runtime:20260801` 不包含 SynthNova 业务代码。启动脚本会把旁边的
`synthnova_delivery/` 挂载到容器 `/workspace/synthnova`。

## 1. 准备宿主机

建议使用 Ubuntu 22.04/24.04 x86_64、NVIDIA RTX GPU（显存至少 16 GB）、
内存至少 32 GB，并预留 50 GB 以上磁盘空间。

宿主机需要安装 NVIDIA 驱动、Docker Engine 和 NVIDIA Container Toolkit；
不需要安装完整 CUDA Toolkit 或 `nvcc`。

- Docker：<https://docs.docker.com/engine/install/ubuntu/>
- NVIDIA Container Toolkit：<https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/install-guide.html>

## 2. 检查交付文件

```bash
chmod +x check_delivery.sh run_synthnova.sh
./check_delivery.sh
```

未连接 GP001 只会产生警告，不影响 Docker 和普通示例。

## 3. 启动并进入

设置客户自己的联网授权账号，然后运行：

```bash
export ENCMGR_APP_ID="客户 App ID"
export ENCMGR_APP_SECRET="客户 Secret"
./run_synthnova.sh
```

如果没有提前设置，脚本会交互式询问。首次运行会自动完成以下工作：

1. 导入 `synthnova_runtime_20260801.tar`；
2. 创建名为 `synthnova` 的持久容器；
3. 把完整的 `synthnova_delivery/` 挂载到容器；
4. 自动配置 GUI、可选 GP001 和四个网站；
5. 进入 `/workspace/synthnova`。

在容器中执行 `exit` 只退出当前终端，容器和网站继续运行。再次进入只需：

```bash
./run_synthnova.sh
```

常用管理命令：

```bash
./run_synthnova.sh --status
./run_synthnova.sh --stop
./run_synthnova.sh --recreate   # 环境镜像更新后重建容器，合成数据不会删除
```

## 4. 数据与网站

整个 `synthnova_delivery/` 位于 Docker 外部并以可写方式挂载，因此不需要单独处理
合成数据目录：容器中的 `/workspace/synthnova/synthetic_data` 会直接写入宿主机
`synthnova_delivery/synthetic_data/`。Assets Manager 的状态同样直接保存在
`synthnova_delivery/sythnova_assets/.assets_manager/`。停止或重建容器不会删除这些文件。

容器启动时自动提供：

- Assets Manager：<http://127.0.0.1:18000>
- Physics Simulator 文档：<http://127.0.0.1:18001>
- Render Simulator 文档：<http://127.0.0.1:18002>
- SynthNova 教学网站：<http://127.0.0.1:19000>

示例：

```bash
cd example/02_simulation_scene/01_physics_simulator_usage
python 00_g1_default_scene.py
```

`synthnova_delivery/` 中 7 个业务模块的 `src` 已加密；顶层 `example/`、模块
`examples/` 和网站保持明文。Seed3D 默认账号位于加密载荷中，如需替换可设置
`SYNTHNOVA_AIGC` 后使用 `--recreate`。

export ENCMGR_APP_ID="客户 App ID"
export ENCMGR_APP_SECRET="客户 Secret"
./run_synthnova.sh
这是我自己设置联网帐号的意思吗

## 71. 助手

不是让你随便创建或填写一个普通联网账号。

这里的：

```bash
export ENCMGR_APP_ID="客户 App ID"
export ENCMGR_APP_SECRET="客户 Secret"
```

是占位示例。实际值应当由 **Galbot/SynthNova 交付方或你们公司的授权管理员签发**，用于在线解密和授权那 7 个加密业务模块。

它不是：

- Linux 登录账号
- Docker Hub 账号
- Wi-Fi/网络账号
- GP001 序列号
- 可以自行编写的任意字符串

不要原样输入 `"客户 App ID"` 和 `"客户 Secret"`，那样授权会失败。

拿到真实凭据后，最安全的方式是直接运行：

```bash
./run_synthnova.sh
```

然后在提示处输入 App ID 和 Secret；Secret 输入时不会显示。避免使用 `export ENCMGR_APP_SECRET="..."`，因为可能进入 shell 历史记录。

README 最后一段提到的“Seed3D 默认账号”是另一个用于 3D 内容生成的服务账号，与 `ENCMGR_APP_ID/SECRET` 的软件运行授权不是同一套凭据。
