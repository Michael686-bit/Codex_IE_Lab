# 确认 Linux 用户能否使用 Astra

- 归档编号：`01a0796b-05cb-7573-acea-fb4f6596797e`
- 项目：无项目对话
- 创建时间：2026-09-07T09:10:52.108000+08:00
- 更新时间：2026-09-07T09:16:52.203000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

现在 linux 用户是否能够使用Astra?

## 2. 助手（进展）

我先确认你说的 Astra 具体指哪个产品/功能，以及目前对 Linux 的支持状态。

## 3. 助手

可以。按目前官方信息，Linux 用户能够通过 Codex CLI 使用 Astra，前提是：

- Codex CLI 版本至少为 **0.153.0**
- 账号已获得 Astra 的分批开放权限
- 订阅计划符合条件（Plus、Pro、Business、Enterprise 等）

Codex CLI 官方支持 Linux；Astra 的开放主要取决于账号、套餐和逐步 rollout，并非 Linux 系统限制。[Codex CLI 平台支持](https://help.openai.com/en/articles/11096431)、[Astra 使用要求](https://help.openai.com/en/articles/20001275/)

可先检查并升级：

```bash
codex --version
codex --upgrade
```

如果版本和套餐都符合但仍看不到 Astra，通常是账号尚未完成灰度开放，或企业管理员尚未启用。

## 4. 用户

source ~/.bashrc

codex-desktop-proxy

现在我的打开命令是这个，环境是不是得重新配？，给我写个更新脚本

## 5. 助手（进展）

我会先检查 `codex-desktop-proxy` 的实际位置、当前 Codex 版本和 `.bashrc` 配置，再按现有启动方式写一个可重复执行的更新脚本，尽量不破坏你已有的代理配置。

## 6. 助手（进展）

我已经确认你当前环境确实需要更新：Codex CLI 是 `0.148.0`，低于 Astra 要求的 `0.153.0`；ChatGPT Desktop 是 `26.810.52044`，也低于当前 Linux 客户端所需的新版本。你的代理启动器本身可以保留，不需要重写。

## 7. 助手（进展）

更新脚本已经写好并通过了语法检查。它会沿用你现有的 Clash 地址，自动更新 CLI 和 apt 安装的桌面客户端；加 `--start` 时更新完直接调用原来的 `codex-desktop-proxy`。我再补一个细节，让脚本即使不先 `source ~/.bashrc` 也能找到 Codex。

## 8. 助手

不需要重新配置 `.bashrc` 或 `codex-desktop-proxy`。你当前的问题是版本偏旧：

- Codex CLI：`0.148.0`，需要至少 `0.153.0`
- ChatGPT Desktop：`26.810.52044`，建议升级到 `26.818.31338+`

Linux 官方支持 Codex CLI，Astra 的可用性主要取决于版本、账号套餐和灰度开放状态。[Linux 平台支持](https://help.openai.com/en/articles/11096431)、[Astra 使用要求](https://help.openai.com/en/articles/20001275/)

更新脚本已生成：

[下载 update-codex-astra.sh](/home/lsy03/Documents/Codex/2026-09-07/xian/outputs/update-codex-astra.sh)

执行：

```bash
bash /home/lsy03/Documents/Codex/2026-09-07/xian/outputs/update-codex-astra.sh --check
bash /home/lsy03/Documents/Codex/2026-09-07/xian/outputs/update-codex-astra.sh --start
```

第二条命令会自动检测现有 Clash 代理、更新 Codex CLI 和 ChatGPT Desktop，然后启动原来的 `codex-desktop-proxy`。更新桌面客户端时会请求 `sudo` 权限。
