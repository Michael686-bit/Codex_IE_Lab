# 指导进入 SynthNova 后续操作

- 归档编号：`01a06b17-c10c-73e1-981e-e69c0f12ccaf`
- 项目：遥操主臂 和 synthnova
- 创建时间：2026-09-04T14:25:14.006000+08:00
- 更新时间：2026-09-04T14:56:42.570000+08:00
- 状态：活动
- 导出范围：用户可见消息与助手可见回复；不含隐藏推理和工具原始输出

## 1. 用户

lsy03@lsy03-System-Product-Name:~$ cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'
./run_synthnova.sh
请输入客户 ENCMGR_APP_ID: eacfa93fbcc009a8
请输入客户 ENCMGR_APP_SECRET:
未连接 GP001：Docker 仍会启动，仅遥操示例不可用。
SynthNova 容器已运行。exit 只退出当前终端，容器会继续运行。
root@lsy03-System-Product-Name:/workspace/synthnova#

我现在好像在自己电脑上进来了，接下来该怎么做？

## 2. 助手（进展）

你已经成功进入 SynthNova 的 Docker 容器了；现在这个 `root@...:/workspace/synthnova#` 是容器里的命令行，不是宿主机根环境。先别输入 `exit`，我帮你查看交付目录里的说明和可运行入口，再给你准确的下一步命令。

## 3. 助手（进展）

从交付说明看，启动已经完成，且“未连接 GP001”只是表示遥操手柄示例不能用，普通物理仿真、渲染、机器人控制和教学网站都不受影响。我正在确认最适合第一次运行的最小示例，以及窗口/退出方式。

## 4. 用户

你看下现在好像我重新链接了

## 5. 助手（进展）

我看一下你当前终端的最新输出，确认是重新进入了原来的容器，还是又创建/重启了一次。

## 6. 助手

确认了，你已经重新连接成功：

- 容器：`synthnova`
- 状态：`running`
- 服务就绪：`yes`
- 当前目录：`/workspace/synthnova`

现在可在当前终端运行最小测试：

```bash
cd example/02_simulation_scene/01_physics_simulator_usage
python 00_g1_default_scene.py --steps 300
```

正常情况下会打开仿真窗口，运行 300 步后自动结束。若有报错，把完整输出发给我即可。

## 7. 用户

没搞懂，我们现在是在做啥？

lsy03@lsy03-System-Product-Name:~$ cd '/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir'
./run_synthnova.sh
请输入客户 ENCMGR_APP_ID: eacfa93fbcc009a8
请输入客户 ENCMGR_APP_SECRET:
未连接 GP001：Docker 仍会启动，仅遥操示例不可用。
SynthNova 容器已运行。exit 只退出当前终端，容器会继续运行。
root@lsy03-System-Product-Name:/workspace/synthnova# cd example/02_simulation_scene/01_physics_simulator_usage
python 00_g1_default_scene.py --steps 300
TIME 2026-09-04 06:28:44.460 | PID 238:140026400035904 | TAG hdf5
[INFO] Logger loaded with config: {'log_level': 'info', 'use_file_log': True, 'log_path': None}
TIME 2026-09-04 06:28:44.474 | PID 238:140026400035904 | TAG hdf5
[SUCCESS] Physics Simulator (MuJoCo) initialized.
TIME 2026-09-04 06:28:45.430 | PID 238:140026400035904 | TAG hdf5
[INFO] Saved keyframe 'initial_pose' for robot galbot_one_golf
TIME 2026-09-04 06:28:45.431 | PID 238:140026400035904 | TAG hdf5
[INFO] Restored keyframe 'initial_pose' for robot galbot_one_golf
ground: /World/default_ground_plane
robot: /World/Galbot
Xlib:  extension "NV-GLX" missing on display ":1".
root@lsy03-System-Product-Name:/workspace/synthnova/example/02_simulation_scene/
01_physics_simulator_usage#

## 8. 助手

你刚才做的是一次“最小启动测试”，还没有正式做机器人任务。

过程可以理解为：

```text
你的 Ubuntu 电脑
  → 启动 SynthNova 的 Docker 环境
  → 进入容器
  → 启动物理模拟器 MuJoCo
  → 加载地面和 Galbot G1 机器人
  → 自动模拟 300 步
  → 测试完成并退出
```

从输出看，测试成功了：

- `Physics Simulator initialized`：物理模拟器启动成功
- `ground: /World/default_ground_plane`：地面加载成功
- `robot: /World/Galbot`：机器人加载成功
- 最后重新出现 `root@...#`：300 步执行完毕

`Xlib: extension "NV-GLX" missing` 是图形显示警告。如果刚才出现过机器人仿真窗口，暂时可以忽略；如果完全没有窗口，后面再解决图形加速配置。

你现在仍在 SynthNova 容器里。建议先用电脑浏览器打开：

[http://127.0.0.1:19000](http://127.0.0.1:19000)

这是配套教学网站，可以先了解 SynthNova 是如何创建场景、控制机器人和生成数据的。

如果想持续观察刚才的机器人场景，在当前终端执行：

```bash
python 00_g1_default_scene.py
```

这次不带 `--steps 300`，程序会一直运行；按 `Ctrl+C` 停止。

简单说：刚才只是证明“软件能启动、机器人能加载、物理仿真能运行”，还没有开始遥操、运动控制或数据生成。

## 9. 用户

给我看下SynthNova，现在能否可以通过USB读取到数据？

## 10. 助手（进展）

我按你说的 USB 是指 GP001 遥操设备来检查。会分三层确认：电脑是否识别、是否生成串口设备、SynthNova 容器是否映射到了它；先做只读检查，不会发送控制指令。

## 11. 助手（进展）

检查结果已经很明确：电脑本身识别到了 GP001（`Galbot Remote Operate ComPort`，设备是 `/dev/ttyACM0`），但当前 SynthNova 容器的设备映射列表是空的，所以现在容器还读不到它。原因通常是容器创建后才插上 USB，单纯重新进入容器不会补加设备映射。

## 12. 用户

我是否需要重启容器还是你给我加入USB映射？

## 13. 助手

需要“重建容器并加入 USB 映射”，普通重启不够。Docker 不能给已经创建的容器动态追加 `--device`。

请保持 GP001 插着，在当前终端执行：

```bash
exit
./run_synthnova.sh --recreate
```

然后重新输入 App ID 和 Secret。数据目录不会删除，但容器会重新创建。成功时应该看到：

```text
已连接 GP001：... -> /dev/ttyACM0
```

Secret 需要你亲自在终端输入，所以这一步由你执行更安全。进入新容器后告诉我，我再替你检查映射并做一次只读取、不控制机器人的 USB 数据测试。

## 14. 用户

lsy03@lsy03-System-Product-Name:~/Lsy03_document/仿真软件/extracted/synthnova_de
livery_20260801/delivery_dir$ ./run_synthnova.sh --recreate
请输入客户 ENCMGR_APP_ID: eacfa93fbcc009a8
请输入客户 ENCMGR_APP_SECRET:
已连接 GP001：/dev/serial/by-id/usb-Galbot_Galbot_Remote_Operate_ComPort_206F33685052-if00 -> /dev/ttyACM0
SynthNova 容器已运行。exit 只退出当前终端，容器会继续运行。
root@lsy03-System-Product-Name:/workspace/synthnova#

现在应该可以了你给我看看能不能读到数据

## 15. 助手（进展）

现在启动输出已经表明 USB 被映射进新容器了。我先核对容器内的设备节点和权限，再用官方 `basic_usage.py` 监听约 10 秒；这个测试只打印 GP001 数据帧，不会控制仿真机器人。测试期间请活动一下 GP001 左臂。

## 16. 助手

可以，已经确认 USB 数据链路正常。

我刚在 SynthNova 容器里进行了 10 秒只读测试：

- `/dev/ttyACM0` 已正确映射，具有读写权限
- 成功收到约 900 帧，约 90 帧/秒
- 序号从 `seq=4` 持续增加到 `seq=900`
- 时间戳也持续更新
- 测试正常结束，没有控制仿真机器人

例如：

```text
seq=4   t=13184.567 q0=-5.184
...
seq=900 t=13194.361 q0=-5.184
```

`q0` 没变化只是因为测试期间对应的左臂第一关节没有移动，不代表读取失败。

结论：**GP001 → USB → Docker → SynthNova Remote Control Lite 已经完全连通。** 下一步可以启动 G1 仿真遥操。

## 17. 用户

好呀，我们来测试一下，首先使用我的数采设备来通过仿真软件可视化显示，给我写个脚本

## 18. 助手（进展）

我会以当前已经打通的 GP001 为输入，做一个“安全的可视化启动脚本”：默认驱动 SynthNova 里的 G1 仿真机器人并打开可视窗口，不连接真实机器人；同时保留 USB 自动识别、无窗口自检和限时运行选项。先复用交付包已有且已验证的映射逻辑，避免重新猜测关节定义。

## 19. 助手（进展）

脚本已经写好并通过语法检查。我把第一版限制为“仅双臂可视化”：GP001 的左右 7 关节只驱动虚拟 G1 双臂，摇杆、底盘、升降和夹爪全部忽略，也不会覆盖已有的快照数据。接下来把它放进 SynthNova 的挂载示例目录，再做 5 秒无窗口实机验证。

## 20. 助手（进展）

第一次容器测试已经找到一个兼容性小问题：这里的 `QueueSink` 只支持 `get(timeout=0)`，不像标准队列那样提供 `get_nowait()`。USB 首帧和仿真场景都已成功加载；我现在修正这一处接口并重新跑完整 5 秒测试。

## 21. 助手（进展）

修正版已通过：5 秒内消费了 494 帧（约 93–96 Hz），无坏帧、无越界值，虚拟 G1 场景也正常初始化和清理。最后我再短暂运行一次带窗口的版本，确认这个脚本本身的 3D Viewer 路径正常。

## 22. 助手

脚本已经写好并通过实机测试：[gp001_visualize_g1.py](/home/lsy03/Lsy03_document/仿真软件/extracted/synthnova_delivery_20260801/delivery_dir/synthnova_delivery/example/04_teleoperation/gp001_visualize_g1.py)。

在当前容器终端运行：

```bash
cd /workspace/synthnova/example/04_teleoperation
python gp001_visualize_g1.py
```

启动前先保持数采设备双臂稳定；出现 MuJoCo 窗口后，移动左右数采臂，虚拟 G1 的双臂会同步运动。关闭窗口或按 `Ctrl+C` 结束。

目前脚本特意限制为：

- 只控制仿真 G1 的左右双臂
- 不控制真实机器人
- 忽略摇杆、底盘、升降、按钮和夹爪
- 不录制或覆盖原有 Snapshot 数据

验证结果：USB 数据约 `93–97 Hz`，无无效帧、无关节越界，GUI 测试也正常结束。若启动瞬间偶尔出现一条 `error hex`，随后出现 `First valid GP001 arm frame received`，表示驱动已自动重新同步，可以继续使用。
